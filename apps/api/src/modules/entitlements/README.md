# Entitlements Module

Owns product entitlement policy: core expense, settlement, import, and export commands are not capped for scarcity monetization.

Does not own billing, subscription checkout, payment collection, or feature implementation. Future paid automation features should call this module for a decision, but Phase 1 core ledger writes must remain allowed.
